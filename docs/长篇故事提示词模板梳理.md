# 长篇故事 · 提示词模板全景梳理（仅分析，未改代码）

> 分析范围：漫画工作台 → 长篇故事（LongProject）全管线。
> 涉及模板类型（TemplateType 中共 8 种，长篇故事使用其中 6 种；`story`/`style` 属于其它工作流）：
> `analysis` / `script` / `storyboard` / `extract` / `asset-prompt` / `panel-prompt`

---

## 一、六个模板使用环节总览

| # | 环节 | 模板类型 | UI 入口（选择模板的地方） | 拼装函数（文件） |
|---|------|---------|--------------------------|------------------|
| 1 | 原文分析 | `analysis` | LongProject.vue「原文」页签底部 PromptRunBar | `buildAnalysisPrompt`（chapterDocService.ts） |
| 2 | 漫画剧本 | `script` | LongProject.vue「剧本」页签底部 PromptRunBar | `buildScriptPrompt`（chapterDocService.ts） |
| 3 | 分镜生成 | `storyboard` | LongProjectStoryboardTab.vue 右栏「分镜内容」PanelContentEditor 的 PromptRunBar | `buildStoryboardPrompt`（storyboardService.ts） |
| 4 | 资产提取 | `extract` | LongProjectStoryboardTab.vue 顶栏「提取资产」PromptRunBar | `buildAssetExtractionPrompt`（assetExtractionService.ts） |
| 5 | 资产绘画提示词 | `asset-prompt` | LongProjectAssetWorkbench.vue 弹窗（AssetPromptGenerateModal：批量一次性 / 批量逐条）+ 单条重写弹窗 | `buildAssetPromptPrompt`（批量一次性）/ `buildSingleAssetPrompt`（逐条与单条）（assetPromptService.ts） |
| 6 | 分镜画面描述 | `panel-prompt` | LongProjectStoryboardTab.vue 画面描述推导弹窗（PanelPromptGenerateModal：单镜 / 批量） | `buildPanelPromptPrompt`（panelPromptService.ts） |

**模板选择的默认值策略**（各处一致）：取该类型下 `sortOrder` 最小的第一个模板；模型默认取第一个 LLM 类模型。**例外：分镜画面描述环节没有内置兜底** —— 模板 id 为空或未命中时不再使用任何内置文案（弹窗里模板必选，列表为空时给「新建模板（填入推荐内容）」一键落库推荐模板）。

**发送前确认机制**（所有环节一致）：PromptRunBar / 生成弹窗点击执行 → 回调 `buildPrompt()` 生成最终提示词 → 勾选「发送前确认」则弹 PromptPreviewDialog 可临时编辑 → 以（可能已编辑的）最终文本执行，并持久化到对应 run 的 `prompt` 字段（失败重试沿用上次 prompt，不再重新拼装）。

---

## 二、各环节变量明细

### 1. 原文分析（analysis）

| 变量 | 含义 | 来源 |
|------|------|------|
| `{{chapter_content}}` | 章节原文（当前编辑草稿） | LongProject.vue `draftContent` |

- 返回格式约定**写在模板内容里**（推荐模板末尾的【返回格式】段，见 `OUTPUT_FORMAT_SPECS.analysis`）：中文 Markdown、`## 人物 / ## 场景 / ## 道具 / ## 事件与时间线…` 小节组织。**本环节结果不需要解析**（整段 Markdown 直接入库）。
- 设置页提供「填入推荐原文分析模板」（`DEFAULT_ANALYSIS_TEMPLATE`），已含全部变量。

### 2. 漫画剧本（script）

| 变量 | 含义 | 来源 |
|------|------|------|
| `{{chapter_content}}` | 章节原文 | `draftContent` |
| `{{analysis}}` | 本章原文分析结果（**上一环节产物**） | `analysisDoc.content` |

- 返回格式约定写在模板内容里（`OUTPUT_FORMAT_SPECS.script`）：`## 场景 N` 组织，字段行 `- 属性名：内容`。**本环节结果不需要解析**（整段 Markdown 直接入库）。
- UI 关联提示：剧本页签顶部在分析为空时显示警示条（无分析仍可生成，变量/追加点会体现「尚未生成」）。
- 设置页推荐模板（`DEFAULT_SCRIPT_TEMPLATE`）已含全部变量。

### 3. 分镜生成（storyboard）

> 更新（现态）：中文变量为 `{{章节原文}}` / `{{原文分析}}` / `{{漫画剧本}}`（注册表与推荐模板同为此次序）；兜底追加机制已整体移除，模板里写了的变量才替换、没写的一律不出现。`{{章节原文}}` 用于补足剧本压缩掉的视觉细节，边界由推荐模板的【原文核对】一节约束（剧情与对白以剧本为准）；剧本缺失时原文已作为剧本底稿填入「漫画剧本」，故不再重复渲染。

| 变量 | 含义 | 来源 |
|------|------|------|
| `{{章节原文}}` | 当前章节原文（视觉细节的核对来源；剧本兜底时不再重复注入） | `chapter.content` |
| `{{原文分析}}` | 原文分析（辅助核对，可选） | `analysisDoc.content` |
| `{{漫画剧本}}` | 漫画剧本（**上一环节产物**；无剧本时以章节原文兜底 + toast「将以原文兜底」） | `scriptDoc.content`，兜底 `chapter.content` |

- ✅ 上述「`{{chapter_content}}` 不一致」问题已在早前修复；分镜变量表现已注册 `{{章节原文}}`（且刻意不设 `chapter_content` 别名，避免旧模板误用被迁移成整章原文）。
- 返回格式约定写在模板内容里（`OUTPUT_FORMAT_SPECS.storyboard`，v4）：`## 分镜 N · X格` + `【第X格】` + `「字段名」：内容`（景别/镜头必填，台词类四选一，其余按需）。**本环节结果需要解析**，设置页模板编辑器底部会提示「本环节结果按 Markdown 解析」。同一份格式文本也供「页面 AI 优化」提示词复用（`outputFormatSpec('storyboard')`），避免两处描述不一致。
- 产物解析 `parseStoryboardResponse` → panels；旧模板若输出「出场资产」行，按资产库做名称匹配回填绑定；重跑时按 order 迁移 panelArtworks（内容变了标记 stale）。

### 4. 资产提取（extract）

| 变量 | 含义 | 来源 |
|------|------|------|
| `{{章节原文}}` | 章节原文（视觉特征的唯一权威来源；无原文时以剧本兜底 + 说明头） | `chapter.content` |
| `{{原文分析}}` | 环节 1 产物（管线辅助） | `analysisDoc.content` |
| `{{漫画剧本}}` | 环节 2 产物（管线辅助） | `scriptDoc.content` |
| `{{分镜概要}}` | 环节 3 产物，每镜一行 `分镜N：画面｜人物：…`；**仅用于判断重要性/出现频率，不作外观依据** | 本章最近 `completed` 分镜 |
| `{{已有资产}}` | 项目资产库列表（名称+类型+已有视觉状态名）+ 状态名沿用规则 | 跨章节连续性 |

- 兜底追加机制已整体移除：以上 5 个变量**只有写进模板才生效**，推荐/内置默认模板已全部插入。
- 返回格式约定写在模板内容里（`OUTPUT_FORMAT_SPECS.extract`）：`# 人物 / # 场景 / # 道具` → `## 资产名` → `### 视觉状态：状态名`，块内字段：视觉描述、状态标签（**已不含「绘画提示词」**——绘画提示词下移到「资产绘画提示词」环节生成）。**本环节结果需要解析**，设置页模板编辑器底部会提示「本环节结果按 Markdown 解析」。
- 解析：先按中文 Markdown 报告解析，失败兜底旧 JSON 协议；与已有资产做名称匹配（决策 merge/create）、状态名多级模糊匹配。解析器仍保留「绘画提示词」分支作向后兼容（旧结果 / 手动导入）。
- 解析：先按中文 Markdown 报告解析，失败兜底旧 JSON 协议；与已有资产做名称匹配（决策 merge/create）、状态名多级模糊匹配。
- 确定性统计：候选名在本章分镜文本中出现次数（不调模型），供审核页参考。
- ⚠️ **发现（残留字段）**：`PromptTemplate.assetExtractionConfig`（注释称"仅资产提取模板使用"）在运行时仅被**原样存入** run.extractionConfig（LongProjectStoryboardTab.vue:596），**拼装提示词时完全未消费**——属于已废弃/未接线的字段。

### 5. 资产绘画提示词（asset-prompt）— 两种拼装路径

**A. 批量·一次性发送 `buildAssetPromptPrompt`**（一份清单一次请求，结果需程序解析回填）

| 变量 | 含义 | 来源 |
|------|------|------|
| `{{assets}}` | 待生成状态清单（每状态一行：序号｜资产名｜状态名｜资产描述｜视觉描述｜固定特征｜attributes 前 8 项） | 资产库中选中范围的目标状态（仅缺提示词 / 全部） |
| `{{style}}` | 风格上下文（整体画风 + 共用提示词块 description 按 sortOrder） | `buildStyleContext(imageGenConfig.sharedBlocks, comicConfig.paintingStyle)` |
| `{{target_model}}` | 目标生图模型名 | 资产生图配置里的 imageModelId → name |

**B. 批量·逐条发送 / 单条重写 `buildSingleAssetPrompt`**（每状态一次请求，结果直接取全文回填）

| 变量 | 含义 | 说明 |
|------|------|------|
| `{{assets}}` | 单个状态的信息块（资产/类型/描述/固定特征/状态名/视觉描述） | 与 A 的清单格式不同 |
| `{{style}}` | 风格上下文 | 同 A |

- 路径 B **不支持** `{{目标生图模型}}`；不走模板时会用内置默认指令（自带【返回格式】段）。
- **返回格式 = 写在模板内容里，运行时不追加任何协议段**（2026-09 改版）：
  - 各类型的结构说明集中在 `OUTPUT_FORMAT_SPECS`（promptTemplateRegistry.ts），由 `withFormatSpec()` **内联进推荐模板内容末尾的【返回格式】段**；`renderPromptTemplate` 只做变量替换，不再附加任何内容。
  - 因此设置页**删除了「输出协议」编辑框**（以及更早的「解析方式」下拉 / `PromptOutputParser` / `ASSET_PROMPT_PARSER_SPECS` / 模板 `outputProtocol` 字段）：模板内容就是唯一的提示词来源。
  - ⚠️ **代价**：用户改模板内容就可能改掉格式约定、进而改坏解析。所以设置页对**结果会被解析**的环节在模板编辑器底部提示一行（「本环节结果按 Markdown 解析，请保留上面的输出结构」；`story` 为 JSON）。
  - 存量模板里存过的 `outputProtocol` 值被忽略（字段已从类型里删除），**不做数据迁移**。
- **哪些环节需要解析**（决定了哪些模板必须带格式约定）：

  | 环节 | 模板类型 | 解析器 | 返回结构 |
  |------|----------|--------|----------|
  | 分镜生成 | `storyboard` | `parseStoryboardResponse` | `## 分镜 N · X格` + `【第X格】` + `「字段」：内容` |
  | 分镜页面 AI 优化 | `storyboard`（同格式） | `parsePanelBlock` | 同上 |
  | 资产提取 | `extract` | `parseAssetExtractionResponse` | `# 类型` + `## 资产名` + `### 视觉状态：X` + `- 字段：值`（兼容旧 JSON） |
  | 资产绘画提示词（批量·一次性） | `asset-prompt` | `parseAssetPromptResponse` | `【资产名｜状态名】` + 正文 |
  | 旧版漫画项目编辑器 | `story` | `extractJsonFromLlmResponse` | **JSON**（独立旧流程） |

  **不需要解析**：原文分析（`analysis`）、漫画剧本（`script`）、分镜画面描述（`panel-prompt`，逐镜单次调用返回纯文本，所谓「批量」是循环单次）、风格（`style`，不调模型）。**注意：「批量生成分镜绘画提示词」并不需要解析。**
- **批量与逐条同一返回格式**：批量一次性发送（`buildAssetPromptPrompt`）与逐条发送 / 单条重写（`buildSingleAssetPrompt`）都按同一份模板内容要求模型返回「【资产名｜状态名】+ 提示词正文」。逐条场景由 `extractSinglePromptText()` **只解析一条**——命中头部取那一行正文；完全没命中（模型只给裸文本、或加了客套前言）时剥掉包装后退化为整段正文，不让格式细节把结果丢掉。
- **回填解析：单管线多层容错**（`assetPromptParser.ts`）：
  ① 归一化预处理——剥代码块围栏、去零宽字符与 emoji、去整行分隔线、压缩多余空行；
  ② 多形态头识别——`【】`/`[]`/`**加粗**`/`###`/行内「名：值」/`（）`/`【单词】`/序号前缀叠加；**候选头必须先能 resolve 成功才算头**（否则正文里的「- 视觉描述：… - 略显疲惫」会被误判并切碎段落）；
  ③ 模糊名字匹配——精确键 → 紧凑键（去空白与全部分隔符）→ 资产×状态候选交集唯一 → 状态名全库唯一 → 资产名下单状态 → 包含匹配；非精确命中统一标记 `match: 'fuzzy'`，弹窗内提示核对；
  ④ 其它形态容错——模型自作主张给 JSON 数组、或写成「状态N：」序号行，也能收；
  ⑤ 顺序兜底——段落数**严格等于**目标数时按清单顺序 1:1 回填（模型只给裸提示词也能救回）；条数不等时**不猜**，宁可报错；
  ⑥ 诊断——解析全失败抛出 `AssetPromptParseError`，携带 `diagnostics`（停在哪一步 `stage` / 期望与命中条数 / missing / 模型原始返回），弹窗可直接查看原始返回并复制。
- ⚠️ **本节此前的两条结论已过期**（2026-09 重构后）：
  1. 「`{{style}}` 重复注入」不存在了——路径 A 已统一走 `buildAssetPromptPrompt` → `renderPromptTemplate`，没有函数末尾的无条件追加；
  2. 变量名已统一为中文 `{{状态清单}}` / `{{风格上下文}}` / `{{目标生图模型}}`，上文表格里的 `{{assets}}` / `{{style}}` / `{{target_model}}` 是重构前记录，本文档其余章节的变量表**需整体复核**。

### 6. 分镜画面描述（panel-prompt）

| 变量 | 含义 | 来源 |
|------|------|------|
| `{{panel_content}}` | 当前分镜画面内容 | panel.content |
| `{{shot}}` | 镜头类型 | panel.shot |
| `{{prev_panels}}` | **前文滑动窗口**：前 2 镜（DEFAULT_PREV_PANEL_WINDOW）的分镜 + 已推导画面描述；批量推导时随进度动态刷新 | panels + panelArtworks.imagePrompt |
| `{{chapter_outline}}` | 本章分镜概要（每镜一行） | `buildChapterOutline(panels)` |
| `{{assets}}` | 本分镜绑定资产的视觉设定（状态名/视觉描述/**已生成的资产绘画提示词**/固定特征） | 环节 5 产物，经 assetBindings 解析 |
| `{{style}}` | 风格上下文 | 同环节 5 |
| `{{target_model}}` | 目标生图模型名 | 生图配置 imageModelId → name |

- 批量推导按分镜顺序串行执行（每镜一次调用），前文窗口自动带上前面新推导的描述——**这是提示词前后关联最强的环节**。
- 返回格式约定写在模板内容里（`OUTPUT_FORMAT_SPECS['panel-prompt']`）。**本环节结果不需要解析**：逐镜单次调用、返回纯文本直接回填 artwork.imagePrompt，没有「一份清单解析回填」的问题（所谓「批量推导」是循环单次调用，见 LongProjectStoryboardTab 的批量推导循环）。
- ⚠️ **发现（{{style}} 重复注入，同环节 5 路径 A）**：panelPromptService.ts:89 变量替换 + :104 无条件追加。
- 模板未选/未命中 → **不再兜底内置模板**（`DEFAULT_PANEL_PROMPT_TEMPLATE` 已删除）：`resolvePanelTemplate` 返回 null，弹窗不允许开始推导，并提示/一键新建。
- **共用属性不进推导提示词**（v2）：只由 `composeFinalPrompt` 在生图时拼到描述前后；喂给模型的图号清单用 `buildRefManifestText(manifest, { assetsOnly: true })`，只列资产图但保持生图真实序号。

---

## 三、提示词前后关联链（管线依赖图）

```
章节原文
   │
   ▼ [1 analysis: {{chapter_content}}]
原文分析文档 ──────────────┐
   │                      │
   ▼ [2 script: {{chapter_content}} + {{analysis}}]
漫画剧本文档 ──────────────┤
   │                      │
   ▼ [3 storyboard: {{script_content}}(无剧本→原文兜底) + {{analysis}}]
分镜 panels ───────────────┤ (分镜概要)
   │                      │
   ▼ [4 extract: {{chapter_content}} + 追加:分析/剧本/分镜概要/已有资产]
资产候选 → 人工审核入库 ────┤
   │                      │
   ▼ [5 asset-prompt: {{assets}}(状态清单/单状态) + {{style}} + {{target_model}}]
资产视觉状态 imagePrompt ──┤ (进入环节6的 {{assets}} 与生图参考图)
   │                      │
   ▼ [6 panel-prompt: {{panel_content}} + {{shot}} + {{prev_panels}}(前2镜)
              + {{chapter_outline}} + {{assets}}(绑定资产含环节5提示词)
              + {{style}} + {{target_model}}]
分镜画面描述 → 批量生图（共用块参考图也在此参与）
```

横向贯穿项：
- **风格上下文 `{{style}}`**：由「样式模板/共用提示词块 + 项目画风」实时构建，环节 5、6 共用同一口径（`buildStyleContext`）。
- **目标生图模型 `{{target_model}}`**：环节 5（仅批量一次性）、环节 6 支持，让提示词面向具体生图模型优化。

---

## 四、"没插入变量"的处理方式汇总（逐函数对比）

> **⚠️ 本节是改造前的基线快照，已过时（2026-09-11 起）**：兜底追加机制已整体废弃，
> 现在统一为「模板里写了 `{{变量}}` 才替换，没写的一律不出现」。下表仅作历史对照。

| 拼装函数 | 变量缺失时的处理 |
|---------|-----------------|
| `buildAnalysisPrompt` / `buildScriptPrompt`（共用 `applyVariables`） | 变量在模板中→原地替换；不在→内容非空则以 `【标签】` 追加到模板末尾。`{{analysis}}` 存在但值为空→替换为占位文字「（本章尚未生成原文分析）」；`{{analysis}}` 不存在且值为空→完全不追加 |
| `buildStoryboardPrompt` | `{{script_content}}` 不在→追加【漫画剧本】（始终追加，剧本是主输入）；`{{analysis}}` 不在→仅非空时追加【原文分析（辅助上下文）】；`{{analysis}}` 在但为空→替换为「（本章尚未生成原文分析）」 |
| `buildAssetExtractionPrompt` | `{{chapter_content}}` 不在→追加【章节原文】；分析/剧本/分镜概要/已有资产四个上下文**不走变量**，非空即追加 |
| `buildAssetPromptPrompt`（批量一次性） | 三个变量**任一存在**→视为"有变量"，只做替换不追加清单；**全都不存在**→追加【待生成状态清单】。风格上下文**非空时无条件再追加**【风格上下文】→ 与 `{{style}}` 替换重复 ⚠️ |
| `buildSingleAssetPrompt`（逐条/单条） | `{{assets}}` 不在→追加【待生成状态】；`{{style}}` 不在且风格非空→追加【风格上下文】（判断正确，不重复） |
| `buildPanelPromptPrompt` | 七个变量**任一存在**→只替换；全不存在→追加【当前分镜】信息块。风格上下文**非空时无条件再追加**【风格上下文】→ 与 `{{style}}` 替换重复 ⚠️ |

变量**有占位但值缺失**时的填充词（发给模型的兜底文案）：
- `{{shot}}` / `{{target_model}}` → 「未指定」
- `{{style}}` → 「无特殊风格要求」
- `{{prev_panels}}` 空窗口 → 「当前是本章第一个分镜，无前文画面。」
- `{{assets}}`（分镜无绑定）→ 「本分镜无绑定资产。」
- `{{analysis}}` → 「（本章尚未生成原文分析）」

---

## 五、问题清单（本次只记录，不修）

| # | 级别 | 问题 | 位置 |
|---|------|------|------|
| 1 | 中 | 推荐分镜模板使用 `{{chapter_content}}`，但 `buildStoryboardPrompt` 只识别 `{{script_content}}`/`{{analysis}}` → 占位符原样发给模型，且原文注入位置失效（内容仍会以【漫画剧本】追加，但模板里的【章节原文】段落是空占位） | TemplateEditorForm.vue:186-187 vs storyboardService.ts:9-18 |
| 2 | 低 | `{{style}}` 双重注入：批量资产提示词与分镜画面描述两个拼装函数中，变量替换后又无条件追加【风格上下文】，风格文本出现两遍 | assetPromptService.ts:158+168、panelPromptService.ts:89+104 |
| 3 | 低 | `assetExtractionConfig` 字段（注释"仅资产提取模板使用"）运行时只被存入 run，从未参与提示词拼装——疑似旧管线残留 | types/index.ts:431、LongProjectStoryboardTab.vue:596 |
| 4 | 提示 | 逐条资产提示词路径（buildSingleAssetPrompt）不支持 `{{target_model}}`，与批量一次性路径能力不对齐 | assetPromptService.ts:214-250 |
| 5 | 提示 | 长篇故事 6 个环节的模板在设置页是同一个模板池按 type 过滤；各环节默认取该类型第一个模板，没有"项目级"或"章节级"模板记忆（模型/模板选择仅通过各环节 config 部分持久化） | useChapterDocRun / useStoryboardRun / 各弹窗 |

---

*分析基线：当前工作区代码（src/modules/comic + src/features/settings），未做任何修改。*
