# 长篇故事：漫画剧本 / 分镜 格式定稿（v5）

> **v5 修订一句话（2026-09-19）**：**全链路统一 Markdown**——分镜放弃 v4 专用符号（`【第X格】` / `「字段名」：`），
> 改用纯 Markdown 语法：格用 `### 第X格` 标题，字段用 `- 字段名：内容` 列表行；页头 `## 分镜 N · X格` 不变。
> 同批统一：资产绘画提示词逐条改为 `## 资产名｜状态名` 标题 + 正文；画面描述整章分段标记改为 `## 分镜 N`。
> 解析器向下兼容全部历史形态（v4 符号 / v3 页块 / v2 行内分格），旧数据与旧导入文本不受影响。
>
> **v4 修订一句话**：剧本不变；**分镜改用「符号规则」**——分镜格用 `【第X格】`，内容标题用 `「XXX」`，冒号后写具体内容。
> 一格不再只有「镜头 + 画面 + 一句台词」，而是**逐字段展开**：景别 / 镜头 / 画面 / 人物 / 动作 / 表情 / 台词 / 心声 / 画外 / 旁白 / 音效 / 光效 / 备注。
> 概念仍是 **一个分镜 = 一张竖屏漫画图 = 一页**，一页内 1~4 格（格 = 图内的分格）。
> 本文档只管「生成什么样的剧本、什么样的分镜」，不含代码改动。
>
> **v4 增量（2026-09-11）**：句型从 v3 的「`①【镜头】画面` + `说话人：【台词】`」换成
> 「`【第X格】` + `「字段名」：内容`」。理由：v3 只有三个槽位（景别、画面、台词），
> 运镜、人物、动作、表情、音效、光效无处安放，只能塞进「画面」里挤成一坨；
> 逐字段展开后既能写清楚，也便于解析器把每个字段单独取出来喂给下游。
> **剧本层不变**（剧本仍是 `## 场景 N` + `- 属性名：内容`）。

## 0. 一句话结论

| 层 | 单位 | 格式 |
|----|------|------|
| 剧本 | 场景 | `## 场景 N` + 8 字段（地点/人物/剧情/动作/情绪/对白/本场目的/预估页数）——**不变** |
| **分镜** | **一张漫画图 = 一页**（1~4 格） | **`## 分镜 N · X格` 起一块；格用 `### 第X格` 标题起头，逐行写 `- 字段名：内容`（13 字段）** |
| 画面描述 | 一格 / 一整页 | 自包含生图提示词（分格结构、分割线、人物占比、浅景深、光线、气泡位置…） |

三层职责边界：

- **剧本**决定"这一章每一场戏怎么演"；
- **分镜**决定"这一页里有几格、每格是什么镜头、画什么、说什么"——**内容与节奏**；
- **画面描述**决定"这一格具体怎么画"——**视觉参数**（6 种分格结构 + 13 种排版变体 + 人物占比铁律 + 浅景深铁律 + R13/R14 视角一致性 + 气泡规范）。

---

## 1. 漫画剧本（格式定稿，沿用 v1/v2，仅补强说话人规则）

### 1.1 结构

```markdown
## 场景 N 地点·时间
- 地点：这个场景在哪
- 人物：本场出场人物
- 剧情：本场发生什么（3~5 句，按发生顺序）
- 动作：关键动作 / 走位 / 画面重点（可多条）
- 情绪：情绪基调与转折（谁：怎么变）
- 对白：最终台词，一人一行，「说话人：台词」
- 旁白：无人称叙述（可省略；没有就不写这一行）
- 本场目的：铺垫 / 推进 / 转折 / 收束 + 一句话
- 预估页数：4
```

### 1.2 完整示例（示例故事 · 场景 1）

```markdown
## 场景 1 室内·傍晚
- 地点：家中室内
- 人物：角色B、角色A、关键道具「道具名」（关键道具级角色）
- 剧情：角色B提出关键道具已损坏、无法复原，角色A当场冷脸维护它；角色B平静地提出处理掉，
  角色A先反对、再转而哀求把它留在封闭容器里；角色B沉默一分钟后勉强让步。
- 动作：角色A把关键道具护到身后、指节收紧、咬牙攥拳、伸手想拉她又缩回；角色B垂眼、别开视线、捏衣角
- 情绪：角色A 焦躁维护 → 急切恳求；角色B 隐忍 → 平静 → 勉强让步
- 对白：
  角色A：角色B，你怎么能那么残忍？破坏了它的关键部分，跟要了它的命有什么区别？
  角色A：你让它以后怎么办？你怎么能说出这种话？
  角色B：我也觉得很残忍。那还是处理掉吧。
  角色B：总不能为了留着它，把它关起来吧。
  角色A：对！我们可以找个容器把它养起来。
  角色B：开放的容器还是不安全。万一盖子没盖紧呢？角色C会去碰的。
  角色A：那就买个封闭的！求求你了，咱们留下它吧？你放心，我绝对不会让它伤害到你们的！
  角色B：行吧。不过容器我来选。
- 本场目的：推进——暴露角色A对关键道具的异常执念，为后续冲突埋线
- 预估页数：22
```

### 1.3 剧本写作规则

1. 只改编原文已明确的内容，不新增事件、不补写设定；
2. 对白写成**最终上台词的版本**，**每一行都必须带说话人**（`说话人：台词`）——分镜会原样搬运，一字不改；
3. 大段心理描写外化为动作 + 表情 + 旁白，不保留成坨的内心戏；角色心理活动若必须保留，写成 `说话人（心声）：文字`；
4. 场景 = 同一时间 + 同一地点；闪回、时间跳跃单独成场景并在标题标注；
5. 一章建议 10~25 页（"页"= 一张图）；预估页数是锚点，分镜可浮动 ±20%；
6. 剧本不写"分格"——分格属于分镜与画面描述层。

> 注：「预估页数」里的"页" = 一张漫画图 = 一个分镜，不是"一格"。

---

## 2. 分镜（格式定稿 v4）——符号规则：格用【第X格】，字段用「」

### 2.1 为什么放弃"一行一图"

v2 把整页压成一行：`镜头｜画面｜台词 ‖ 镜头｜画面｜台词`。单格页还行，**一到多格就塌**：

| 维度 | v2 一行一图 | **v4 页块格式（采用）** |
|------|------------|------------------------|
| 一行是什么 | 一整页（含 1~4 格） | 一个元素（页头 / 格标题 / 一个字段） |
| 多格页可读性 | 差：格被 `‖` 挤在一行，肉眼难辨 | 好：格逐行排列、字段逐行列出，结构一眼看清 |
| 信息承载力 | 3 个槽位（景别 / 画面 / 台词），其余全挤进"画面" | **13 个字段**各占一行，运镜/人物/动作/表情/音效/光效各有归处 |
| 手改成本 | 高：要数 `‖` 在哪儿 | 低：改哪个字段就编辑哪行 |
| 模型输出稳定性 | 长行易错位、漏 `‖`、漏格 | 分行清晰，漏格/漏字段一眼可查 |
| 解析复杂度 | 需按 `‖` 二次切分，容错难 | 逐行状态机，规则极简 |
| 与生图对接 | 需要再拆 | 一个字段一天然对应下游的一个输入（景别 → 占比/景深、光效 → 光线） |
| 专业对照 | 无对应格式 | 接近日式条漫分镜表（面板 × 字段） |

### 2.2 页块格式规范（v5 Markdown）

```
## 分镜 1 · 双格
### 第1格
- 景别：近景转特写
- 镜头：从角色A侧脸下摇至小臂
- 画面：角色A的手臂占前景，指节收紧，青筋凸起。关键道具的尾端从画外缠上小臂，表面压进皮肤。
- 人物：角色A
- 出场资产：A1 角色A（战斗服）
- 动作：指节收紧，手臂绷紧，关键道具缠上小臂。
- 表情：侧脸冷硬，眼神压着怒意，下颌绷紧。
- 台词：角色A：“破坏了它的关键部分，跟要了它的命有什么区别？”
- 音效：沙沙——表面摩擦。
- 光效：暖黄顶灯，背景压暗，手臂局部高光。

### 第2格
- 景别：中景
- 镜头：平视，角色B站在室内靠背前，前景可带角色A手臂虚影。
- 画面：角色B一手扶住靠背，身体微僵，视线看向画外角色A方向。背景顶灯突然熄灭，画面下半部迅速压黑。
- 人物：角色B
- 出场资产：B1 角色B（便装）
- 动作：手扶靠背，身体微僵，视线投向画外。
- 表情：眉头拧起，嘴唇微张，眼神警惕。
- 心声：角色B：“他到底想干什么……”
- 旁白：夜色压下来，屋里的灯忽然灭了。
- 音效：啪——嗡——
- 光效：暖灯骤灭，冷蓝夜光。
- 备注：灯灭必须画出来：灯泡“啪”地熄灭，画面压黑，不要只靠旁白交代。
```

**四条语法（也是解析规则）：**

1. `## 分镜 N · X格` —— 起一页（一块）。`N` 与 `X格` 只供阅读，**解析以出现顺序为准**，系统按顺位自动纠正；`X格` 会显示在左栏列表上；
2. `### 第X格` —— 起一格（Markdown 标题行）。`X` 从 1 递增，可以同行续写内容，但**默认独立成行**；
3. `- 字段名：内容` —— 写这一格的某个字段（Markdown 列表行），一行一个字段，属于**它上方最近的 `### 第X格`**；
4. **没有内容的字段整行省略**，不要写空字段。

> 空行只用来分隔页块（也可不空）。字段行必须以 `- ` 起头——列表行是格字段的唯一形态。
> 解析时字段值会按字段名分流入库（台词类会把「说话人：」与引号拆开），序列化时再拼回，所见即所得。
>
> **出场资产 的编号语法**：写成 `- 出场资产：A1 角色A（战斗服）、B1 角色B（便装）`。
> `A1` 这类编号由系统按本章资产清单现算（资产按清单次序取字母、状态按资产内次序取数字），
> 模型照抄即可；解析时编号直接查表得到资产与状态 id，不再依赖名字字符串比对。机制见 2.3.1。
>
> **v4 历史示例兼容说明**：本文档 2.7 等处的示例页仍以 v4 符号（`【第X格】` / `「字段」：`）书写，
> 解析器照常识别；新产出一律按 v5 Markdown 写。两种符号可按 `【第X格】→### 第X格`、`「字段」：→- 字段：` 机械换算。

### 2.3 字段规范

| 顺序 | 字段 | 内容 | 取值 / 字数 |
|------|------|------|------------|
| 1 | **景别** | 这一格的景别 | 7 选 1：`远景` `中景` `近景` `特写` `POV` `过肩` `仰拍`（**必填**，可写 `近景转特写` 这种运镜式景别） |
| 2 | **镜头** | 机位与运镜 | 如 `平视` / `俯拍` / `从侧脸下摇至小臂` / `前景带另一角色虚影`（可省略） |
| 3 | **画面** | 这一格画面上能看到什么 | 构图 + 人物位置 + 动作 + 环境 + 关键细节，30~80 字 |
| 4 | **人物** | 本格画面内出现的角色 | 多个用 `、` 分隔；**不在画面内的说话人不写在这里** |
| 5 | **出场资产** | 本格画面内出现、且在本章资产清单里列出的资产与视觉状态 | 写成 `编号 资产名（状态名）`，多个用 `、` 分隔（如 `A1 林小雨（礼服）`）；机制见 2.3.1；画面内没有清单资产时整行省略 |
| 6 | **动作** | 本格的肢体动作 | 10~25 字 |
| 7 | **表情** | 本格的神态与情绪 | 10~25 字 |
| 8~11 | **台词 / 心声 / 画外 / 旁白** | 本格的说话内容 | 四选一，同一格**最多出现一个**；写法见 2.4 |
| 12 | **音效** | 本格的声音 | 如 `沙沙——表面摩擦`；没有就不写 |
| 13 | **光效** | 本格的光线与明暗关系 | 如 `暖黄顶灯，背景压暗，手臂局部高光`；没有就不写 |
| 14 | **备注** | 画面上必须画出来的关键点 | 如 `灯灭必须画出来`；没有就不写 |

`景别` 段的意义：它是画面描述层的**输入锚点**——人物占比（远景 <20% / 中景 40-60% / 近景 60-80% / 特写 >80%）、景别、景深等级、焦段都由它决定。写在分镜层，画面描述层就不必猜。

> 字段顺序固定为：景别 → 镜头 → 画面 → 人物 → 出场资产 → 动作 → 表情 → 台词/心声/画外/旁白 → 音效 → 光效 → 备注（共 14 个）。解析器按字段名识别、不依赖顺序，但**输出时按此顺序**，便于人读。

---

### 2.3.1 出场资产的编号机制（2026-09-21）

**要解决的问题**：分镜里的「资产名（状态名）」是模型另写一遍的字符串，而资产库里的状态名是提取环节另一次生成的。两边只要措辞差一点（「少年」vs「少年期」），绑定就断，表现为「这一格明明出现了该资产，却没绑上视觉状态」。

**做法**：本章资产清单由系统带上编号后再注入提示词。

```
【本章资产】
- 林小雨（人物）：A1 便装（全章默认）｜A2 礼服（换装戏后）
- 训练场（场景）：B1 露天训练场（全章）
```

- **编号规则**：资产按清单次序取字母（A、B、C…，超过 26 个续 AA、AB），状态按该资产内次序取数字（1、2、3…）。无视觉状态的资产不占字母位。
- **模型只负责照抄**：每项写成 `编号 资产名（状态名）`；编号由系统算，模型不得自行编号、不得省略。
- **解析时编号直接查表**：命中编号即得到资产 id 与状态 id，不经过任何字符串比对。

**编号是「第二证据」，不是名字的替代**——判定规则：

| 情况 | 采用 |
|------|------|
| 名字命中且与编号一致 | 该结果（最高置信） |
| 名字没命中、编号命中 | **编号救场**（原先会漏绑的主要场景） |
| 名字与编号指向不同状态 | **以名字为准**（名字来自资产库、跨章稳定；采用编号反而会引入新错绑） |
| 编号缺失 / 编号悬空 / 两者都没命中 | 回落名字三级匹配（精确 → 双向包含）→ 章节范围默认 |

由此，编号表可以随本章资产变化而变（只要每次渲染清单与解析用的是同一份），**不需要持久化到任何数据里**：落库的始终是资产 id 与状态 id。

> **存量影响**：没写编号的旧模板、旧分镜文本、编辑器里的手动编辑，全部照旧走名字通道，行为与改造前完全一致。要启用编号，需在设置页对该模板重新「填入推荐模板」。

> **编辑器往返必须无损（2026-09-21）**：右栏「分镜内容」输入框走 `parsePanelBlock` 解析，它**不带资产上下文**
> （空资产库 / 空章节），因此所有「出场资产」声明在这一步都是未命中态。据此固定一条解析口径：
> **资产未命中时保留文本原样的状态名**（资产命中时仍只保存实际匹配到的状态名，保证「显示状态」与「发送图片」一致）。
> 有了这条，`序列化 → 解析` 往返不会把 `资产名（状态名）` 退化成 `资产名`：输入框里写的什么，
> 保存后还是什么，重解析按「资产名 + 状态名」精确还原同一个状态 id，不落到 `defaultVariant()` 猜测
> ——同章多状态资产（如「测验魔石碑」的三种碑面显示）没有唯一默认，猜不出来就会丢状态，
> 表现为「资产还绑着却取不到图、中栏资产消失」。
> 另：编号不参与持久化，编辑器保存后 `A1` 前缀会被剥掉，属预期行为（落库的始终是资产 id + 状态 id）。

---

### 2.4 台词归属规则（一次讲清）

> **能不能不写说话人？不能。** 说话人决定三件事：① 气泡**挂在谁头上**；② 谁要**张嘴 / 给表情**；③ 绑定**哪个角色资产**。不写，竖屏成图时气泡就会挂到错误角色的脸上。

1. **台词类字段写在它所属那一格里**——上面最近的 `【第X格】` 就是它的格；
2. **每句台词必须带说话人**：`「台词」：说话人：“台词”`。台词文字就是剧本里的原句，**直接搬运、连标点都不改**；引号用中文引号 `“”`；
3. **同一格最多一句台词**（`台词` / `心声` / `画外` / `旁白` 只出现其中一个）；两个角色说话就拆成两格；
4. 说话人**不在该格画面里**时（听者反应镜、画外音），用 `「画外」`；
5. 角色**心里想的**，用 `「心声」`——心声不要求说话人出现在画面里；
6. **没有人称的旁白**用 `「旁白」`，**不带说话人**：`「旁白」：夜色压下来，屋里的灯忽然灭了。`；
7. 说话人名字与**资产库角色名一致**，方便自动绑定；剧本里怎么称呼，分镜里就怎么称呼；
8. `「人物」` 字段与台词说话人是两回事：`「人物」`写**画面里出现的人**，`「画外」`的说话人**恰恰不在画面里**。

**归属判定示例：**

```
## 分镜 3 · 双格
【第1格】
「景别」：过肩
「画面」：角色A俯身逼近。
「人物」：角色A
「台词」：角色A：“你让它以后怎么办？”        ← 说话人就在本格画面里
【第2格】
「景别」：近景
「画面」：角色B别开视线，嘴唇抿紧。
「人物」：角色B

## 分镜 4 · 单格
【第1格】
「景别」：特写
「画面」：角色B睫毛低垂，张了张嘴又把话咽回去。
「人物」：角色B
「画外」：角色A：“你怎么能说出这种话？”      ← 角色B的反应镜，说话人不在画面里
```

### 2.5 分格规则（对齐分格结构库）

| 规则 | 内容 |
|------|------|
| 一页格数 | 1~4 格（3:4 竖屏承载力）；5 格以上只留给"不规则拼贴"极端页 |
| **一格 = 一个信息单元** | 一个镜头类型 + 一个情绪焦点 + 一个动作/反应；**不要在一格里塞两个叙事节点**（结构库铁律 3） |
| 一格一个说话人 | 一格里只放一个角色的台词，且**台词行必须写明说话人**；两个角色对话 → 拆两格（结构库"一格最多 2 气泡"的简化执行） |
| 台词字数 | **每格 ≤ 20 字**；单格满版页 ≤ 30 字；旁白同限 |
| 主视觉格 | 页内必须唯一主格（情绪焦点）；分镜不必指定，画面描述层按"主格最大"处理 |
| 格数多样性 | 全篇 20~30% 用**单格满版或 3~4 格**，避免通篇都是上下双格（对应结构库"排版多样性"要求） |
| 视角一致性 | 面对面时**不要在同一页里让两个人各占一格且都露正脸**（R13/R14 由画面描述层校验，分镜层只需保证内容不矛盾） |

### 2.6 什么算"一格"（分格判定）

| 算一格 | 不算一格（并进同格或删） |
|--------|------------------------|
| 一次动作 / 一次反应 | 同一动作的重复描述 |
| 一句关键台词（≤20 字） | 语气词、无信息量的应答 |
| 一次表情 / 情绪转折 | 纯环境描写（并进有人的格，或作单格满版呼吸页） |
| 关键道具 / 信息首次出现（可作小格特写） | 无法画面化的心理独白（改写为心声或旁白） |
| POV 内容（手机屏、窗外、书页） | 把"角色看屏幕"和"屏幕内容"塞进同一格（必须拆格，R13） |

### 2.7 完整示例（同场景 1 → 22 页）

```
## 分镜 1 · 单格
【第1格】
「景别」：中景
「画面」：角色A把关键道具护到身后，冷脸盯着角色B。
「台词」：角色A：“角色B，你怎么能那么残忍？”

## 分镜 2 · 双格
【第1格】
「景别」：近景
「画面」：角色A指节收紧，关键道具缠上小臂。
「台词」：角色A：“破坏了它的关键部分，跟要了它的命有什么区别？”
【第2格】
「景别」：中景
「画面」：角色B站在沙发边，眉头拧起。

## 分镜 3 · 双格
【第1格】
「景别」：过肩
「画面」：角色A俯身逼近。
「台词」：角色A：“你让它以后怎么办？”
【第2格】
「景别」：近景
「画面」：角色B别开视线，嘴唇抿紧。

## 分镜 4 · 单格
【第1格】
「景别」：特写
「画面」：角色B睫毛低垂，张了张嘴又把话咽回去。
「画外」：角色A：“你怎么能说出这种话？”

## 分镜 5 · 单格
【第1格】
「景别」：特写
「画面」：关键道具昂起一端，表面弯成月牙，一脸得意。
「心声」：关键道具：“果然，角色A最爱的是我！”

## 分镜 6 · 双格
【第1格】
「景别」：特写
「画面」：关键道具表面在灯下泛冷光。
「心声」：关键道具：“为了我，他居然凶那个人！”
【第2格】
「景别」：近景
「画面」：角色A低头看它，神色柔和。

## 分镜 7 · 双格
【第1格】
「景别」：近景
「画面」：关键道具缠上他的手腕。
【第2格】
「景别」：特写
「画面」：它斜睨向沙发方向。
「心声」：关键道具：“你除了是个女人，你拿什么和我比？”

## 分镜 8 · 单格
【第1格】
「景别」：中景
「画面」：角色B坐在沙发上捏着衣角，脸色发白。
「心声」：关键道具：“这么多年过去，身形都走了样，不像我，永远不变。”

## 分镜 9 · 单格
【第1格】
「景别」：近景
「画面」：关键道具整条盘上他的手臂，得意地昂起一端。
「心声」：关键道具：“角色A最喜欢摸我了，他说我滑溜溜的。”

## 分镜 10 · 双格
【第1格】
「景别」：特写
「画面」：关键道具勾住他的指尖。
「心声」：关键道具：“好，今晚我要奖励你。”
【第2格】
「景别」：特写
「画面」：角色A眼神发直，喉结滚动。

## 分镜 11 · 单格
【第1格】
「景别」：近景
「画面」：角色A咽了口口水，手无意识抚过它。

## 分镜 12 · 单格
【第1格】
「景别」：远景
「画面」：关键道具扭得更欢，像一条带子收紧、松开。

## 分镜 13 · 单格
【第1格】
「景别」：中景
「画面」：角色B垂着眼把视线移开，声音很淡。
「台词」：角色B：“我也觉得很残忍。那还是处理掉吧。”

## 分镜 14 · 单格
【第1格】
「景别」：近景
「画面」：她抬眼看向他，语气平静没有起伏。
「台词」：角色B：“总不能为了养它，把它关起来吧。”

## 分镜 15 · 单格
【第1格】
「景别」：中景
「画面」：角色A猛地看向她，像抓住救命稻草。
「台词」：角色A：“对！我们可以买个容器把关键道具养起来。”

## 分镜 16 · 双格
【第1格】
「景别」：近景
「画面」：角色B摇头。
【第2格】
「景别」：特写
「画面」：她的指尖指向隔壁房门。
「台词」：角色B：“开放容器还是不安全。万一盖子没盖紧呢？”

## 分镜 17 · 双格
【第1格】
「景别」：近景
「画面」：角色A咬牙攥拳，额头青筋冒起。
【第2格】
「景别」：特写
「画面」：紧闭的邻室房门。
「画外」：角色B：“角色C会去碰的。”

## 分镜 18 · 单格
【第1格】
「景别」：近景
「画面」：他凑近一步，伸手想拉她的胳膊又缩了回去。
「台词」：角色A：“那就买个封闭的！”

## 分镜 19 · 单格
【第1格】
「景别」：中景
「画面」：他低着头，肩膀微微塌下，声音发软。
「台词」：角色A：“求求你了角色B，咱们养关键道具吧？”

## 分镜 20 · 双格
【第1格】
「景别」：近景
「画面」：角色A盯着她，眼底都是恳求。
「台词」：角色A：“你放心，我绝对不会让关键道具伤害到你们的！”
【第2格】
「景别」：中景
「画面」：角色B垂下眼，室内安静下来。

## 分镜 21 · 单格
【第1格】
「景别」：特写
「画面」：角色B抬起头，脸上写满勉为其难。
「台词」：角色B：“行吧。不过容器我来选。”

## 分镜 22 · 单格
【第1格】
「景别」：中景
「画面」：角色A愣了一秒，随即用力点头，笑开了。
「台词」：角色A：“好，谢谢你，你最好了。”
```

说明：第 11、12 页是**纯画面页**（无台词，推进节奏）；第 4、17 页是**画外音页**；第 5~10 页是**关键道具的心声页**；第 2、3、6、7、10、16、17、20 页是**双格页**。呼吸页不要因为"没台词"就删掉。

### 2.8 分镜写作规则

1. 一页一块（`## 分镜 N · X格`），一格一段（`【第X格】` + 若干 `「字段名」：内容`）；页块之间空一行；
2. 一页 1~4 格，一格一个信息单元，不要塞两个叙事节点；
3. 每格必写 `「景别」`，取 7 种标准景别之一；`「画面」` 30~80 字；台词 ≤20 字（单格满版 ≤30 字）；
4. **每句台词都带说话人**（`说话人：“台词”`），从剧本原样搬运、不改写；剧本与原文冲突时以剧本为准；
5. 说话人不在画面内用 `「画外」`；角色内心活动用 `「心声」`；无人称叙述用 `「旁白」`，同一格只出现一个；
6. 关键动作/情绪转折可给独立页或独立格；重要道具/信息首次出现可给小格特写；
7. 保持人物空间关系连续（上一格在左，下一格不要跳到右）；
8. 全篇 20~30% 用非"上下双格"的格数组合（单格满版 / 3~4 格），避免通篇一个节奏；
9. **没有内容的字段整行不写**，不要留空字段、不要写"无"。

---

## 3. 剧本 → 分镜 的对应关系

| 剧本字段 | 落到分镜哪里 |
|---------|-------------|
| 地点 / 情绪 | 画面段的场景与神态（不明写，体现在描述里） |
| 剧情 | 页的数量与顺序（一句剧情 → 1~4 页） |
| 动作 | 画面段的动作主体，优先独立成页/格 |
| **对白** | `「台词」` 字段，**连同说话人一起原样搬运**（`说话人：“台词”`）；超 20 字拆成多格或多页，拆开后每句仍带说话人 |
| 本场目的 | 不进入分镜，用于筛页（对目的无贡献的页删掉） |
| 预估页数 | 页数锚点（±20%） |

**格数与剧情密度对照**：

| 剧情情况 | 建议格数 |
|---------|---------|
| 一句台词 / 一个动作 | 单格满版 |
| 对话来回（问 → 答） | 上下双格 / 左右双格 |
| 对峙、对比、双视角 | 左右双格 |
| 铺垫 → 发展 → 爆点 | 三格递进 |
| 情绪核心 + 细节补充（表情/手部/道具） | 不规则主从格 |
| 爆点、反转、结尾 | 单格满版（人物占比拉高） |

---

## 4. 可直接粘贴的模板（系统设置 → 提示词模板）

### 4.1 剧本 · 推荐模板

```
你是一名资深的条漫编剧。请把当前章节改编成漫画剧本，回答"这一章每一场戏怎么演"。

【改编原则】
- 只改编原文已明确的内容，不新增事件、不补写原文没有的设定。
- 按场景组织：场景 = 同一时间 + 同一地点的连续剧情；闪回、时间跳跃单独成场景并在标题标注。
- 对白写成最终上台词的版本：口语化、贴合人物身份，每行都必须带说话人（说话人：台词）。
  分镜阶段会原样搬运，不再改写，所以说话人一个字都不能省。
- 角色内心活动写成「说话人（心声）：文字」；无法上台词的叙述写成「旁白：文字」。
- 删掉无法画面化的纯心理描写与重复叙述，改用动作、表情、对白、旁白外化情绪。
- 关键转折、关键信息必须保留；不要写成分章小说。

【篇幅节奏】
- 按"页"（= 一张漫画图）估算每场信息量，写入「预估页数」；一章建议 10~25 页。
- 一个动作 / 一句关键台词 / 一次情绪变化 ≈ 一页。
- 不要写分格——分格由分镜与画面描述环节负责。

【输出内容】
每个场景写明：地点、人物、剧情、动作、情绪、对白、本场目的、预估页数。

【章节原文】
{{章节原文}}

【原文分析】
{{原文分析}}
```

### 4.2 剧本 · 输出协议

```
只输出中文 Markdown，不要解释、代码块或 JSON。
用 ## 场景 N 地点·时间 组织每个场景，场景内用「- 属性名：内容」逐行写明：
地点、人物、剧情、动作、情绪、对白、本场目的、预估页数。
对白一人一行，写成「说话人：台词」，必须是最终上台台词的版本；说话人不能省略。
角色内心独白写「说话人（心声）：文字」，多人对话按先后顺序排列。
无人称的叙述另起一行写「旁白：文字」。
「预估页数」写成整数，不带单位。
```

### 4.3 分镜 · 推荐模板（v4 历史快照）

> 本节是 v4 时期的形态，保留供对照。**当前推荐模板以设置页「填入推荐模板」拉取到的为准**（v5，含 `{{本章资产}}` 变量与状态编号要求），其输出协议见 4.4、编号机制见 2.3.1。

```
你是一名条漫分镜师。请把下面的漫画剧本拆成一页页漫画分镜。
一个分镜 = 一张竖屏漫画图 = 一页；一页内可以有 1~4 格。

【分页原则】
- 先把剧情切成"页"（一页承载一个完整节拍），再决定这一页内分几格。
- 一页 1~4 格；同一页内的若干格属于同一节拍的不同瞬间（对话来回、动作连拍、主画面+细节）。
- 全篇约 20%~30% 的页使用单格满版或 3~4 格，避免通篇都是上下双格。
- 保留呼吸页：纯画面页（无台词）或短旁白页，每章 3~6 页，用来控制节奏。

【分格原则】
- 一格 = 一个信息单元 = 一个镜头类型 + 一个情绪焦点 + 一个动作/反应；不要在一格里塞两个叙事节点。
- 一格里只放一个角色的台词；两个角色对话就拆成两格。
- 角色"看手机/电视/书/窗外，同时读者也要看到内容"时，必须拆成两格（一格角色反应 + 一格主观 POV）。
- 面对面交谈时，不要在同一页里让两个人各占一格且都露正脸。

【每一页怎么写】
- 页面标题：## 分镜 N · 单格 / 双格 / 三格 / 四格（N 从 1 递增，格数与页内实际格数一致）。
- 每一格先单独一行写【第X格】（X 从 1 递增），再从下一行开始逐行写这一格的字段。
- 字段行格式：「字段名」：内容。字段名用「」括起，冒号后写一句具体内容，一行一个字段。

【每格字段】（按此顺序写，没有内容的字段整行省略）
- 「景别」：从 远景 / 中景 / 近景 / 特写 / POV / 过肩 / 仰拍 中选一个（每格必填）。
- 「镜头」：机位与运镜，如 平视／俯拍／从角色侧脸下摇至小臂／前景带另一角色虚影。
- 「画面」：这一格画面上能看到的内容——构图、人物位置、动作、环境、关键细节，30~80 字。
- 「人物」：这一格画面内出现的角色名，多个用、分隔；不在画面内的说话人不写在这里。
- 「动作」：这一格的肢体动作，10~25 字。
- 「表情」：这一格的神态与情绪，10~25 字。
- 台词类字段四选一，同一格最多出现一个：

【台词怎么写】（说话人不能省略，必须与剧本逐字一致）
- 对白：「台词」：说话人：“台词”
- 内心独白：「心声」：说话人：“心里想的话”（可不出现在画面里）
- 说话人不在本格画面内：「画外」：说话人：“台词”
- 无人称叙述：「旁白」：文字

【其余字段】
- 「音效」：本格的声音，如 沙沙——衣料摩擦。没有就不写。
- 「光效」：本格的光线与明暗关系，如 暖黄顶灯，背景压暗，手臂局部高光。没有就不写。
- 「备注」：画面上必须画出来的关键点，没有就不写。
- 不写绘画风格、颜色、人物占比、景深、分格结构、气泡位置——这些由画面描述环节按规则库处理。

【原文核对】
- 原文分析仅作辅助核对：与剧本冲突时以剧本为准，不要机械按章节推断回忆、倒叙、变身等特殊状态。

【漫画剧本】
{{漫画剧本}}

【原文分析】
{{原文分析}}
```

### 4.4 分镜 · 输出协议（v5 Markdown）

```
只输出中文 Markdown，不要解释、代码块或 JSON。
结构规则：每一页写成一个 ## 分镜 N · 单格 / 双格 / 三格 / 四格 小节（N 从 1 递增，格数与页内实际格数一致）；页内每一格用一个 ### 第X格 标题行开始（X 从 1 递增），格字段写为 - 字段名：内容 的列表行，一行一个字段。
每格的字段按此顺序写，没有内容的字段整行省略：景别 / 镜头 / 画面 / 人物 / 出场资产 / 动作 / 表情 / 台词 / 心声 / 画外 / 旁白 / 音效 / 光效 / 备注。
景别 取 远景 / 中景 / 近景 / 特写 / POV / 过肩 / 仰拍 之一，每格必填；镜头 写机位与运镜（如 平视、从侧脸下摇至小臂）。
画面 写这一格画面上能看到的内容——构图、人物位置、动作、环境、关键细节，30~80 字。
人物 写这一格画面内出现的角色名，多个用、分隔；动作、表情 分别写这一格的肢体动作与神态，各 10~25 字。
出场资产 写这一格画面内出现、且在【本章资产】清单中列出的资产，格式为 编号 资产名（状态名）（编号是清单每项状态前面的字母加数字，如 A1），多个用、分隔；编号、资产名与状态名都必须照抄清单、一字不差，不得自行编号或省略编号；状态按剧情锚点判断该格所处阶段，同一资产同一格只声明一个状态；同一资产在同页不同格状态不同时各格分别声明；画面内没有清单资产时整行省略，不得编造清单外的编号、资产或状态名。
台词类字段四选一，同一格最多出现一个：对白写 台词、内心独白写 心声、说话人不在本格画面内写 画外、无人称叙述写 旁白；没有就不写。
台词类字段的写法为 说话人：“台词”——说话人不能省略，必须与剧本逐字一致，引号用中文引号；旁白不带说话人。
音效、光效 按需写；备注 只写画面上必须画出来的关键点。
每句台词不超过 20 字，单格满版页不超过 30 字，超出必须拆格或拆页。
一个分镜 = 一张竖屏漫画图 = 一页，一页 1~4 格，一格只讲一个信息单元。
```

---

## 5. 生成质量自检清单

**剧本**

- [ ] 每个场景 = 同一时间同一地点，标题带地点·时间
- [ ] 对白是最终台词，一人一行，**每行都带说话人**
- [ ] 没有成坨的心理描写，情绪都外化为动作/表情/对白/旁白
- [ ] 每场都有「本场目的」，删掉会塌的才留
- [ ] 预估页数合计 10~25

**分镜**

- [ ] 每一页都以 `## 分镜 N · X格` 开头，N 连续、X格与页内实际格数一致
- [ ] 每一格都以 `【第X格】` 开头，X 从 1 连续递增
- [ ] 每个字段行都是 `「字段名」：内容` 的形式，字段名在 `「」` 里
- [ ] 字段顺序为 景别 → 镜头 → 画面 → 人物 → 动作 → 表情 → 台词/心声/画外/旁白 → 音效 → 光效 → 备注
- [ ] 每格都有 `「景别」`，取 7 种标准景别之一
- [ ] **每一句台词都写了说话人（`说话人：“台词”`），且与剧本逐字一致**
- [ ] 同一格内 `台词 / 心声 / 画外 / 旁白` **最多出现一个**，没有就不写
- [ ] 说话人不在画面内的用 `「画外」`，内心独白用 `「心声」`，无人称的用 `「旁白」`（不带说话人）
- [ ] `「人物」` 写的是画面里出现的人，与 `「画外」` 的说话人不矛盾
- [ ] 没有内容的字段整行省略，没有空字段、没有写"无"
- [ ] 每页格数在 1~4 之间，与页头「X格」一致
- [ ] 每格台词 ≤20 字（单格满版页 ≤30 字），超长的已拆格/拆页
- [ ] 一格只讲一个信息单元，没有"一格塞两件事"
- [ ] 全篇有 20~30% 的单格满版或 3~4 格，不是清一色上下双格
- [ ] 有 3~6 页呼吸页（纯画面或短旁白）
- [ ] 没有"看手机/看屏幕"塞在同一格的物理矛盾
- [ ] `「画面」` 没写风格/人物占比/景深/分格结构

---

## 6. 已确认（全部落定）

1. **符号规则（v4）** —— 分镜格用 `【第X格】`，内容标题用 `「XXX」`，冒号后写具体内容；字段行一行一个。
2. **14 个字段固定顺序** —— 景别 / 镜头 / 画面 / 人物 / 出场资产 / 动作 / 表情 / 台词 / 心声 / 画外 / 旁白 / 音效 / 光效 / 备注；没有内容的字段整行省略（出场资产的编号机制见 2.3.1）。
3. **「景别」每格必填** —— 它是人物占比 / 景深 / 焦段的推导锚点；「镜头」写运镜，可省略。
4. **台词类字段四选一** —— `台词` / `心声` / `画外` / `旁白` 同一格最多出现一个。
5. **台词必须写明说话人** —— `「台词」：说话人：“台词”`，与剧本原句逐字一致，分镜直接搬运（见 2.4）；引号用中文引号，解析时入库去引号、序列化补回。
6. **页头固定用 `## 分镜 N · X格`** —— `X格` 既是模型输出，也是左栏列表显示的分格标签（`单格 / 双格 / 三格 / 四格`）。
7. **`【】` 与 `「」` 分工** —— `【】` 只用于 `【第X格】`；`「」` 只用于字段名。二者不混用。

### 已落地（代码）

**2026-09-10（v3 / v3.1）**

- `types/index.ts`：新增 `LongProjectStoryboardCell`；`LongProjectStoryboardPanel` 增加可选 `cells[]`（旧数据缺省 = 单格页，零迁移）。
- `storyboardService.ts`：解析器改为逐行状态机，支持页块格式 + 说话人（含心声 / 画外 / 无人称旁白），并兼容 v2 行内分格与旧 `- 字段：` 格式。
- 页级 `content / shot / dialogue / narration` 仍由分格汇总回填，生图推导、概览、资产计数等既有链路无需改动。

**2026-09-11（v4）**

- `types/index.ts`：`LongProjectStoryboardCell` 增加 `camera / cast / action / expression / sfx / lighting / note`；`LongProjectStoryboardPanel` 增加 `cellLabel`（页头声明的格数标签）。
- `storyboardService.ts`：新增 `CELL_TITLE_RE` / `LABEL_FIELD_RE` / `splitSpeech`，解析 v4 的 `【第X格】` + `「字段名」：内容`；`serializePanelBlock` 输出 v4；新增 `cellCountLabel` / `resolvePanelCellLabel` / `formatCellsForPrompt`。旧 v3（`①【镜头】画面` + `说话人：【台词】`）、v2 行内分格、旧 `- 字段：` 格式全部照旧解析，打开即升级为 v4。
- `promptTemplateRegistry.ts`：storyboard 默认输出协议与推荐模板改写为 v4（符号规则 + 13 字段）。
- `panelPromptService.ts`：画面描述环节新增「分格详情」——把每格的 景别/镜头/画面/人物/动作/表情/音效/光效 一并交给模型（台词类不进，避免与"不写对白旁白"冲突）。
- `PanelListSidebar.vue`：左栏第二行由「内容预览」改为**格数标签**（解析页头「· 双格」），内容预览降为 hover tooltip。
- 新增 / 改写解析用例（v4 逐字段 / v4 往返 / v3 升级 v4 / 空白页 / 格数标签 / 分格详情）；全量 79 个单测通过，`vue-tsc --noEmit` 无报错，`vite build` 655 模块通过。

**2026-09-21（状态绑定编号通道）**

- `storyboardService.ts`：新增 `buildVariantCodeMap()`（本章资产状态 → 编号 `A1/A2/B1`，纯函数，渲染与解析同源）与 `VariantCodeEntry`；`renderChapterAssetsText()` 清单每项改为 `编号 状态名（锚点）`；`bindingsFromValue()` / `parseStoryboardResponse()` 增加可选 `variantCodes` 参数，判定口径为**名字优先 → 编号救场 → 冲突以名字为准**；新增 `splitVariantCode()` 无条件剥离编号前缀（编辑器解析路径没有资产上下文，不剥离会让编号污染资产名）。
- `promptTemplateRegistry.ts`：`OUTPUT_FORMAT_SPECS.storyboard` 与推荐模板的「出场资产」两处协议同步为 `编号 资产名（状态名）`，并补示例。
- `useStoryboardRun.ts` / `LongProjectStoryboardTab.vue`：分镜生成、手动导入、导入预览三处补传编号表（`chapterAssets` 均在手）。
- 兼容：无编号的旧模板、旧文本、编辑器手动编辑一律走原名字通道，行为不变。启用编号需在设置页对该模板重新「填入推荐模板」。
- 新增 10 条用例（编号规则 / 清单渲染 / 编号救场 × 2 / 冲突以名字为准 / 编号悬空回落 / 只写编号 / 无编号表剥离 / 不误伤字母数字资产名 / 端到端）；全量 300 个单测通过，`vue-tsc --noEmit` 无报错。

**2026-09-21（编辑器往返丢状态修复）**

- **现象**：分镜导入后中栏资产正常，但只要在右栏「分镜内容」输入框里编辑一次并失焦保存，输入框里的「出场资产」就会被改写（`测验魔石碑（萧炎测验·三段显示）` → `测验魔石碑`），同章多状态资产随之丢状态，取图清单 / 中栏里该资产直接消失——看起来就像「导入没绑定成功」。
- **根因**：`parsePanelBlock` 不带资产上下文（空 assets / 空 chapterId）→ `bindingsFromValue` 未命中资产时把 `visualVersionName` 置空 → `serializeBindings` 只能写回资产名 → 保存重解析只能靠 `defaultVariant()`，而它在**同章多状态且无唯一默认**时返回 `undefined`。
- **修复**：`bindingsFromValue` 在**资产整体未命中时保留文本原样的状态名**；资产命中时维持原口径（只保存实际匹配到的状态名，保证「显示状态」与「发送图片」一致）。
- 新增 `tests/modules/panelEditRoundTrip.test.ts`（7 条：往返留状态名 / 文本不悄悄改写 / 重解析还原同一 id / 未命中保留原文 / 命中但状态写错仍回落 / 只写资产名不凭空造状态 / 编号剥离不污染资产名）；全量 313 个单测通过，`vue-tsc --noEmit` 无报错。

### 尚未落地（后续阶段）
- 右栏「每格一卡」编辑器（景别下拉 + 逐字段编辑，替代单一文本框）
- 右栏内容区的分格可视化（编辑框内分格导航条 / 分格卡片）
- 结构操作：拆分弹窗改「格拆分」
- 生成完成 toast（报「解析到 N 页 · 共 M 格」）
