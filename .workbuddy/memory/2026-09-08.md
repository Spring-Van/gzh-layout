# 2026-09-08

## 长篇项目收缩侧栏章节浮层（方案已确认并实现）
- 需求：长篇故事侧栏收起（w-14 窄条）后原来只显示 `chapters.slice(0, 8)` 图标，数据多时不可用。
- 方案（用户确认）：hover flyout 浮层模式。新增 `src/modules/comic/components/LongProjectChapterFlyout.vue`：
  - 顶层章节为无标题默认组，一级文件夹分组，第 3 级以下路径压入行尾 sub 前缀；
  - 固定 max-height: min(70vh, 560px)，超出滚动，打开时 scrollIntoView 定位当前章节；
  - hover 120ms 防误触延迟弹出、移出 260ms 收起；Esc / 遮罩点击 / 选中章节后关闭；
  - 内置搜索过滤、文件夹组折叠、底部统计栏；只做浏览选择，不做拖拽/右键管理。
- `LongProject.vue` 改动：aside 加 `relative`；收缩态模板替换为「章节入口图标（ListTree+章数徽标）+ 当前章节图标」两个触发按钮（hover/click 均触发，经 flyoutRef defineExpose 方法转发时机）；删除 slice(0,8) 逻辑。
- 展开态 LongProjectTree 未动。vue-tsc 通过。

## 浮层修复 + 资产库浮层（第二轮）
- 闪动根因：全屏遮罩（fixed inset-0）盖住触发按钮 → hover 被夺走 → 260ms 收起 → 遮罩消失 hover 恢复 → 再弹出，循环闪动。修复：遮罩改为 `fixed inset-y-0 left-14 right-0`（不覆盖侧栏触发区）。
- 抽出共用组合式函数 `src/modules/comic/composables/useFlyoutVisibility.ts`（120ms 弹出 / 260ms 收起 / 点击切换），章节与资产浮层共用。
- 新增 `LongProjectAssetFlyout.vue`：资产库浮层（人物/场景/道具 + 项目级计数，口径同 assetsForSelectedCategory：scope !== 'chapter'）。
- LongProject.vue 收缩态：资产按钮改为浮层触发；hover 一个浮层时立即 close 另一个（两浮层同位叠放会闪烁）；侧栏容器 mouseleave 时两浮层都进入延迟收起。vue-tsc 通过。

## 浮层锚定触发按钮（第三轮）
- 问题：两浮层都固定 top-[64px]，资产浮层未对齐自己的入口按钮。
- 修复：useFlyoutVisibility 增加 `anchorTop`（默认 64），triggerEnter/triggerClick 接受可选 top；LongProject.vue 触发时传 `event.currentTarget.offsetTop`（aside 为 relative，offsetTop 即按钮在侧栏内的垂直位置）；浮层面板 `:style="{ top: anchorTop+'px', maxHeight: min(70vh, 560px, calc(100vh - top - 16px)) }"`，去掉固定 top 类。章节浮层的两个触发按钮各自锚定，面板跟随当前 hover 的按钮。vue-tsc 通过。

## 系统设置页改造方案定稿（未实施）
- 多轮迭代后确定布局：顶部小圆角(4px)分段分类条 → 顶部横向条目切换胶囊条（带图标+末端＋）→ 左栏资源库（跨分类搜索、分组列表、行内悬浮复制/删除、拖拽排序、LLM连接状态点）→ 右栏编辑器（内嵌表单，底部左侧测试连接+右侧重置/保存）
- 存储与上传分类：隐藏左栏与切换条，右栏整宽
- 废除全部编辑弹窗（ModelEditorModal 等），表单抽为 ModelEditorForm/TemplateEditorForm/WechatEditorForm
- 路由扩展为 ?tab=xxx&id=yyy，向后兼容
- 完整方案文档：docs/系统设置工作台改造方案.md（含组件结构、状态设计、7步实施顺序、风险评估、验收清单）
- 已确认外部仅 router 引用 SettingsView，弹窗组件无外部依赖，可安全重构

## 系统设置工作台改造【已实施完成】
- 新增组件：SettingsCategoryBar（分段条，flex-1 均分+calc 定位指示块滑动动画）、SettingsItemChips（条目胶囊条，隐藏滚动条）、SettingsResourceList（左栏资源库：搜索跨分组过滤、行内悬浮 Copy/Trash2、HTML5 拖拽排序 emit 给父级落库、statusMap 状态点）
- 新增表单：ModelEditorForm/TemplateEditorForm/WechatEditorForm（均为 watch(props.entity) 初始化 + JSON 快照 dirty 检测 + emit dirty-change/save；wechat 表单内置鉴权、切换当前账号、默认同步按钮）
- SettingsView.vue 重写为状态中枢：resourceGroups 统一数据源、applySelection/confirmLeave（dirty 拦截：切tab/切条目/新增/删除/复制/排序都走）、路由 syncQuery（tab+id+create）、测试结果 testResults → llmStatusMap → 左栏状态点
- 删除 6 个旧文件：ModelEditorModal/ModelSettings/PromptTemplateSettings/WechatAccountSettings/WechatAccountList/SettingsIconButton
- vue-tsc --noEmit 通过；边界保护：删除当前条目后自动选中同组第一个；初始加载校验 query id 有效性
- 用户节奏：先方案确认，代码变更需等明确指令后再动

## 设置页表单细节迭代（第二轮）
- 左栏资源库改为只显示当前分类（单组传参），搜索仅限当前组；模板分类下新增类型筛选下拉（全部类型+8类各带计数），快速预览不同分类
- ModelEditorForm：API格式/API来源改为下拉框；模型名称+Model一行各半；API格式、Base URL、API Key各独占一行；图片模型扩展参数区不变
- TemplateEditorForm：模板名称+模板类型一行各半；类型改下拉框，下方显示当前类型用途说明提示条
- WechatEditorForm：AppID+AppSecret一行各半
- StorageSettingsPanel：结构对齐编辑器表单（统一header+扁平分区+field-control样式），SettingsView外层去掉p-5
- reorder 事件签名简化为 {sourceId,targetId}（单组后不再传tab，handler内取activeTab）
- vue-tsc 通过

## 模板表单微调
- TemplateEditorForm 删除「模板描述」输入框（form.description 保留内部状态，编辑/推荐模板时原描述不丢）
- 提示词内容 textarea 去掉 rows 固定高度，改 flex-1 占满剩余高度（min-h 120px 兜底）；表单体改为 flex-col 布局
- vue-tsc 通过

## 长篇故事提示词模板全景梳理（仅分析未改码）
- 产出文档：docs/长篇故事提示词模板梳理.md
- 管线 6 个模板环节：analysis → script → storyboard → extract → asset-prompt → panel-prompt；各环节变量、无变量兜底策略（替换/追加【标签】/占位文案）、前后关联链已逐一梳理成表。
- 发现 4 个问题（未修）：①推荐分镜模板用 {{chapter_content}} 但 buildStoryboardPrompt 只认 {{script_content}}，占位符原样发给模型；②{{style}} 在 buildAssetPromptPrompt（批量一次性）与 buildPanelPromptPrompt 中双重注入（变量替换+末尾无条件追加）；③assetExtractionConfig 字段运行时只存入 run 从未参与拼装，疑似残留；④buildSingleAssetPrompt（逐条）不支持 {{target_model}}，与批量路径不对齐。

## 提示词模板体系改造方案（规划稿，未实施）
- 产出文档：docs/长篇故事提示词模板改造方案.md
- 三需求：①变量全中文化（{{章节原文}}等，含 legacy 英文别名兼容+一次性迁移）；②模板表单加"变量状态"tag 区（已用高亮/未知红色警告/点击插入光标）；③输出协议字段化到全部 6 类型+「填入推荐协议」按钮（优先级：自定义>类型默认，统一标签【输出要求】）。
- 核心架构：新建 services/promptTemplateRegistry.ts 单一事实来源（变量注册表/默认协议/推荐模板/renderTemplate 统一渲染引擎），4 个 service builder 只准备变量值。
- 统一兜底策略：变量在=只替换（空值有 emptyText）；不在=always 追加（关键输入）/if-nonempty（辅助上下文）/drop（增强信息）；模板零变量→类型级关键信息块；未注册变量构建时清理。
- 一并修复：分镜推荐模板变量错误+删出场资产旧指引、{{风格上下文}}双注入、assetExtractionConfig 三处删除、逐条路径补 {{目标生图模型}}；资产提取四上下文（分析/剧本/分镜概要/已有资产）变量化。
- 3 个待确认决策点已列在方案第十节（兜底策略、tag 点击插入、推荐分镜模板重写）。

## 提示词模板体系改造【已实施完成】
- 用户确认三个决策点：兜底策略用推荐分档（always/if-nonempty/drop）、tag 点击插入、推荐分镜模板同步重写。
- 新建 src/modules/comic/services/promptTemplateRegistry.ts：变量注册表（20 个中文变量含 legacy 英文别名/兜底策略/emptyText）、OUTPUT_PROTOCOL_DEFAULTS（asset-prompt 分 batch-once/per-item 两套默认）、RECOMMENDED_TEMPLATES（6 类型，分镜推荐模板已重写为 {{漫画剧本}}+{{原文分析}}）、renderPromptTemplate 统一渲染引擎（归一化→替换/追加→清理未注册占位符→协议；函数式替换防 $& 误解析）、migrateTemplateContent。
- 五个 service 全部切换引擎：chapterDoc（DEFAULT_*_TEMPLATE 迁出删除）、storyboard、assetExtraction（四上下文变量化）、assetPrompt（删 applyOutputProtocol/DEFAULT_ASSET_PARSE_PROTOCOL/requireParseable；buildSingleAssetPrompt 补 targetImageModel）、panelPrompt。
- 删除残留：AssetExtractionConfig 接口、PromptTemplate.assetExtractionConfig、LongProjectAssetExtractionRun.extractionConfig、StoryboardTab 赋值、SettingsView 透传。
- TemplateEditorForm 重写：变量状态 tag 区（已用青色高亮/未用灰/未知红警告+tooltip）、点击插入光标处（textarea selectionStart）、输出协议字段扩展到 6 长篇类型+填入推荐协议按钮、单一「填入推荐模板」按钮接注册表；placeholder 用绑定字符串防 Vue3 属性插值报错。
- SettingsView：loadTemplates 增加存量模板一次性变量迁移（归一化后回写库）；saveTemplate 的 outputProtocol 条件改为 LONG_STORY_TEMPLATE_TYPES。
- 行为变化：panel-prompt/逐条资产路径现在默认附加输出协议（原先无）；未注册 {{}} 占位符构建时清理。
- 测试：新增 tests/features/promptTemplateRegistry.test.ts（16 例）+ promptBuilders.test.ts（6 例）；全量 vitest 60 例通过；vue-tsc --noEmit 通过。
- 遗留：dist/ 是旧构建产物（仍含旧代码），下次 build 自动更新。

## 模板表单编译报错修复
- vite:vue 报 Unterminated string constant：插值表达式内写字面 `{{ '{{' + name + '}}' }}`，字符串里的 `}}` 被 Vue 编译器当作插值结束符，表达式被截断。
- 修复：script 内加 `variableTag(name)` 函数生成 tag 文本，模板插值只引用函数结果；title 绑定里的 `{{${name}}}` 同步改为函数调用。vue-tsc + vite build 均通过。
- 样式/布局微调：变量 tag 改小圆角（rounded）+ 亮色主题加深配色（已用 cyan-700、未知 red-600、未用 text-secondary）；「提示词内容」label 的 min-h-0 是输出协议重叠根因（flex 压缩到内容高度以下导致 textarea 溢出盖住下方区域），改 min-h-[176px] 让外层滚动容器正常滚动。

## 设置页输入框无法聚焦（未解决，已挂起）
- 用户 16:32 报告系统设置输入框点击无光标。静态排查 SettingsView/CategoryBar/ItemChips/ResourceList/Toast 均无遮挡元素；纯 Chromium + vite URL 无法复现（缺 Electron preload，electronAPI.comic 未注入页面直接报错）。chromium 已装好（ms-playwright），诊断脚本在 test-results/diagnose-settings.cjs。需真实 Electron 环境排查（如 --remote-debugging-port + CDP），等用户确认 bug 是否仍存在。

## 长篇故事手动导入结果方案（规划稿，未实施）
- 产出文档：docs/长篇故事手动导入结果方案.md
- 需求：4 个 LLM 环节支持外部 AI 代跑——应用内复制组装好的提示词 → 外部生成 → 粘贴结果回应用，走相同解析/审核/存储链路。
- 核心设计：①PromptPreviewDialog+PromptRunBar 加复制按钮；②通用 ManualResultImportDialog（两步式：粘贴→解析预览→确认导入，formatHint=系统默认输出协议）；③run/doc 加 source?: 'llm'|'manual' 字段+徽标。
- 分析/剧本=importDoc 直接写 completed doc；分镜=importStoryboard（parseStoryboardResponse+migratePanelArtworks）；资产=importExtraction（parseAssetExtractionResponse+既有审核确认链路，不绕过人工确认）。
- 3 个待确认决策点：PromptRunBar 常驻复制按钮、双入口位置、徽标展示。

## 长篇故事手动导入结果【已实施完成】
- 用户确认三个决策点全采纳（常驻复制按钮、双入口、徽标展示）。
- 复制出口：PromptPreviewDialog footer 加「复制提示词」按钮（copyContent 用 props.content，复制后 toast 不关窗）；PromptRunBar 主按钮左侧加常驻复制按钮（copyPrompt 直接 buildPrompt()，自建 inline tailwind 样式，因该组件无 icon-button/secondary-button 类）。
- 新增 ManualResultImportDialog（common）：两步式（粘贴→解析预览→确认导入），parse 回调返回 {title,items}，失败红字不落库，formatHint 展示系统默认输出协议（details 折叠）。
- types 三类型加 source?: 'llm'|'manual'（LongProjectChapterDoc/StoryboardRun/AssetExtractionRun，缺省 llm 零迁移）。
- 分析/剧本：useChapterDocRun.importDoc（upsert completed doc + stage 只升不降）；LongProject.vue 顶栏 PromptRunBar 旁「手动写入」按钮 + SourceTab/ScriptTab 空态按钮（emit import-analysis/import-script）。
- 分镜：useStoryboardRun.importStoryboard（parseStoryboardResponse + migratePanelArtworks 对位迁移 + stage）；PanelContentEditor 加「手动导入」按钮 emit('import')，StoryboardTab 接事件 + 弹窗。
- 资产：StoryboardTab.importExtraction（parseAssetExtractionResponse + countCandidatesAppearances + completed run source manual）；顶栏资产提取操作区 + PanelGenAssetTab 空态加入口（emit import-extraction）；后续复用 AssetExtractionReview 审核确认。
- 徽标：SourceTab/ScriptTab 头部加「手动导入」紫色徽标（source==='manual'）。
- 测试：新增 tests/features/manualImport.test.ts（5 例，覆盖分镜/资产解析复用+出现次数统计）。发现 parseAssetExtractionResponse 对无资产文本是 throw 而非返回空数组（legacy JSON 回退抛错），测试据此断言。
- 全量 vitest 65 例通过，vue-tsc 零错误。dist/ 仍旧产物。

## 手动导入功能微调（用户反馈）
- 移除 PromptRunBar 常驻复制小按钮（Copy icon + copyPrompt + useToast），复制出口只保留 PromptPreviewDialog 弹窗内的「复制提示词」按钮。
- ManualResultImportDialog 删除「期望格式」折叠提示块及 formatHint prop；同步清理 LongProject.vue（importDialogConfig、defaultOutputProtocol import）与 LongProjectStoryboardTab.vue（两处 format-hint 绑定、defaultOutputProtocol import）。
- vue-tsc 通过。注意：本轮 Edit 曾出现"报告成功但内容未持久化"的现象，重读文件后重做才生效。
