# 2026-09-19

## 批量弹窗收敛：只留发送方式 + 资产提示词支持外部 AI 导入

需求：批量生成绘画提示词、批量推导画面描述两个弹窗都只保留「全部重新生成」（删除仅补缺失/范围选择），发送方式统一叫「一次性发送 / 逐条发送」；资产批量弹窗的一次性发送支持「复制提示词 → 外部 AI 生成 → 导入解析」。

改动：
- `PanelPromptGenerateModal.vue`：删 scope/missingCount/isOverwrite 与推导范围 radio；「生成方式」改名「发送方式」，选项改「逐条发送（per-panel）/ 一次性发送（chapter）」；`buildPrompt` prop 去掉 scope 参数；confirm payload 去掉 scope。内部 PromptSource 值不变。
- `LongProjectStoryboardTab.vue`：generalRunPrompts 去掉 scope 过滤（始终全部分镜）；buildBatchPromptPreview 固定取首镜；删除 needsInfer/missingInferCount（批量不再用，单镜 stale 逻辑仍在 reevaluatePromptStatus）。
- `AssetPromptGenerateModal.vue`：删 allowScope/missingCount/totalCount props 与生成范围 radio；title 判定改用 allowSendMode；一次性发送提示词视图头部新增「复制提示词」「导入外部 AI 结果」按钮（emit `import-request`）；`applyProgress` 的 done-with-items 分支在 targets 快照为空时用 buildItems 补齐（导入未经 confirm 时缺失检测仍可用）；RetryPayload.scope（failed/all）保留——那是逐条模式的「仅重跑失败」，不是初始范围。
- `LongProjectAssetWorkbench.vue`：删 promptTargets/promptTargetCount（统一用 allPromptTargets）；buildPromptPreview/buildPromptItems 去 scope；runBatchPrompts 始终全部；新增 ManualResultImportDialog（z-[140]，高于 z-[130] 提示词弹窗）+ parseImportedAssetPrompts/confirmPromptImport——复用 buildTargetList + parseAssetPromptResponse，结果按 'batch-once' done 回执写入弹窗结果视图，等「填充到资产」；导入时 batchRuns.once 为空则用项目默认模型/模板补记上下文，让「重新生成」可用。
- `assetPromptService.ts`：`buildTargetList` 加 export（导入解析与内置批量同构的前提）。

校验：vue-tsc ✅ / vitest 262 ✅ / vite build ✅。

注意：ManualResultImportDialog 本身已 Teleport to body，zIndexClass 全局生效，不会被困在抽屉层叠上下文里。

## 手动导入入口统一收进「复制提示词」弹窗

需求：原文/剧本/资产/分镜/绘图各环节的可手动导入解析结果入口，统一收进「复制提示词」弹窗（PromptPreviewDialog），布局仿照资产批量提示词弹窗的「复制提示词 + 导入外部 AI 结果」。

改动：
- `PromptPreviewDialog.vue`：footer 新增「导入外部 AI 结果」按钮，emit `import`（弹窗保持打开）。
- `PromptRunBar.vue`：转发 PromptPreviewDialog 的 `import` 事件。
- `LongProject.vue`：分析/剧本两个 PromptRunBar `@import="openManualImport(...)"`；删除顶栏两个「手动写入」按钮与 SourceTab/ScriptTab 的 import-analysis/import-script 事件链；导入弹窗 z-[140]。
- `LongProjectSourceTab.vue` / `LongProjectScriptTab.vue`：删空态「手动写入分析/剧本结果」按钮、emit 定义与 ClipboardPaste。
- `LongProjectAssetTab.vue`：提取 PromptRunBar `@import`；删「手动导入」按钮与 PanelGenAssetTab 的 import-extraction 链；导入弹窗 z-[130]→z-[140]。
- `PanelGenAssetTab.vue`：删空态「手动导入资产」按钮与 emit。
- `LongProjectStoryboardTab.vue`：分镜 PromptRunBar `@import="storyboardImportVisible = true"`；删顶栏「手动导入」「导入描述」按钮；两个导入弹窗 z-[140]。
- `PanelPromptGenerateModal.vue`：批量+一次性发送（isChapter）时复制按钮旁新增「导入外部 AI 结果」（emit `import` → promptImportVisible）。

口径：导入弹窗层级统一 z-[140]（压过 PromptPreviewDialog z-[130]）；绘图导入仅一次性发送（整章【分镜N】对位），与资产弹窗「逐条发送意义不大」同一判断。

校验：vue-tsc ✅ / vitest 262 ✅ / vite build ✅。

## 分镜中栏「无参考图」根因 + 图N角标

根因（`PanelAssetTabs.vue`）：「无参考图」是**视觉状态级**判定 —— 绑定解析到的那个状态 `referenceImageIds` 为空才显示，与资产整体无关。资产有图仍提示的三种典型：
1. 工作台生成后未点「采纳」：图在 `generatedImageIds` 暂存，采纳后才进 `referenceImageIds`，分镜/生图只认后者；
2. 绑定命中了另一个状态：状态解析 = 延续上一镜 → 章节范围默认（`defaultVariant` 按 chapterRange.startChapterId 取离本章最近者），命中态 ≠ 生成图的态；
3. 重新提取（覆盖）后新建的空状态被延续/兜底命中，旧图随旧状态删除。

改动：
- `PanelAssetTabs.vue`：新增 `refManifest` prop（`buildPanelRefManifest` 产物，唯一图号来源）；选中缩略图加「图N」角标（`manifestIndexOf` 按 variantId 唯一定位清单条目），说明文字带「生图传入为图N」；无图按钮分三情形：`有 N 张待采纳` / `该状态无图 · 其他状态有 N 张`（提示点状态名切换）/ `无参考图`，title 对症指路（`otherVariantImageCount` / `pendingImageCount`）。
- `LongProjectStoryboardTab.vue`：新增 `currentRefManifest` computed 传入。
- 默认第一张 + 多图切换本就有（`resolvePanelRefImage`：selectedImageIds 为空/失效回落第一张；缩略图点选切换；点第一张=恢复默认），本次通过角标与文案把口径显性化。

校验：vue-tsc ✅ / vitest 262 ✅ / vite build ✅。

## 分镜中栏简化：只做「选图 + 图N」

用户反馈（对上一轮三情形提示的修正）：
1. 不显示「有 N 张待采纳」；2. 不是「添加图片」——中栏不做图片管理动作；3. 默认选中该视觉状态的一张图，多图可切换，图片上显示图N（编号 = buildPanelRefManifest 清单顺序，即共用属性图 + 各资产图最终传给生图的顺序）。

改动（`PanelAssetTabs.vue` + `LongProjectStoryboardTab.vue`）：
- 删掉无图按钮（三情形文案 + 跳工作台）→ 无图时只显示灰字「无参考图」；删 `pendingImageCount` / `otherVariantImageCount` / `noImageLabel` / `noImageTip` / `focusTargetOf` 与 `go-asset-workbench` emit。
- 删掉「添加/更换图片」按钮与 `AssetImagePickerModal` 引用、`update-variant-images` emit；父组件删 `updateVariantImages` 与两个事件绑定（换图去资产工作台做）。
- 缩略图保留：默认第一张（selectedImageIds 空）、点选切换、点第一张=恢复默认；选中图角标「图N」（manifestIndexOf 按 variantId 查清单）；说明文字精简为「共 N 张 · 生图传入为图N」。
- 坑（再次踩到）：**同文件多个 Edit 放同一并行批次会互相覆盖**（后执行的基于旧内容写回），本轮模板/emits/函数三处编辑被回滚后逐个补齐。规则：同一文件的多次编辑必须串行。

校验：vue-tsc ✅ / vitest 262 ✅ / vite build ✅。

## 「有图却显示无参考图」真修复：生成图回落为参考图

用户明确口径：**资产生成的图就是参考图**，多张默认第一张；不该要求先「采纳」才算数。

改动：
- `panelPromptService.ts` 新增 `effectiveVariantRefImages(variant)`：`referenceImageIds` 非空用它，为空回落 `generatedImageIds`（工作台已生成未采纳的暂存图）。`resolvePanelRefImage` 改走有效列表（Pick 类型加 `generatedImageIds`）。
- 生效面自动覆盖（都经 resolvePanelRefImage / manifest）：buildPanelRefManifest（生图取图 + 图号 + 提示词清单）、PanelAssetTabs 缩略图（refImagesOf 也改走 effectiveVariantRefImages）、assetUsageService 角标、AssetHoverCard。
- 采纳图优先于生成图：一旦点过采纳，生成图不再参与（避免口径混乱）；单选/切换/默认第一张语义不变。
- `tests/modules/panelRefImage.test.ts` 补 4 例锁死：回落生成图第一张、手动选生成图生效、采纳图优先、两者皆无 → undefined。

校验：vue-tsc ✅ / vitest 266 ✅ / vite build ✅。

## 中栏卡片改版：只显示一张图 + 弹窗切换（修卡顿）

用户需求：底部每个资产卡片只显示当前选中的一张图；切视觉状态、切参考图都唤出弹窗完成；切换图片此前有卡顿。

改动（整体重写 `PanelAssetTabs.vue`）：
- 卡片内不再铺缩略图行：只显示当前选中的一张图（h-14 w-14，带「图N」角标）+ 一行说明（「点击图片切换（共 N 张）· 生图传入为图N」）。
- 切视觉状态：tag 内状态名点击 → 居中弹窗（原锚定 dropdown 整套锚点/翻转/滚动监听代码删除）；点选即时生效。
- 切参考图：点图 → 居中弹窗网格候选，**本地预选 + 「确定」才 emit 写库**（此前每次点击缩略图都立即 emit → 父组件 mutateLongProjectData 写盘 = 卡顿根因）。选第一张 = 恢复默认（写空数组）；无变化点确定不写库直接关。
- 弹窗 Teleport 到 body、z-[60]、Escape 关闭；样式统一 4px 圆角。
- Maximize2 图标移除（tag 点击预览保留，走 inspectEntry → preview emit）。

校验：vue-tsc ✅ / vitest 266 ✅ / vite build ✅。

## 中栏改版 v2：缩略图 + 左侧参考图抽屉

用户需求：底部只显示缩略图，其余都在弹窗里且按人物/场景/道具分 tab；或改为左侧抽屉，样式参考绘图配置抽屉（ImageConfigDrawer）。

改动（再次整体重写 `PanelAssetTabs.vue`）：
- 底部卡片按 人物→场景→道具 排序，每卡 = AssetBindingTag（名+状态只读）+ 当前选中缩略图（图N 角标）或「无参考图」；顶部三 tab 移除。
- 点击缩略图 → **左侧抽屉**（对齐 ImageConfigDrawer：遮罩 z-[100] bg-black/60 backdrop-blur + 面板 z-[101] rounded-xl + 头部/Tab 行/内容区/底部操作 + slide-left 过渡 + Esc 关闭）。
- 抽屉内：人物/场景/道具 tab（带计数）；每个绑定资产一张卡：tag + 格级状态行 + 视觉状态 chip 行（本地预选）+ 候选图网格（本地预选，选中打勾 + 图N 角标；选第一张=恢复默认）。
- **保存模式**：抽屉内所有切换只改本地 pending（pendingVariants/pendingImages），点「保存」一次性 emit（先 set-binding-variant 后 set-binding-images）；取消/遮罩/Esc 丢弃预选。卡顿根因（逐次点击写盘）彻底消除。
- 细节：换状态预选时清掉该资产的图片预选；面板标题「本镜参考图 · 第 N 镜」用 panel.order（1 起，与「分镜N」口径一致）。
- 样式 token：chip 用 var(--border-subtle)/var(--accent)，4px 小圆角。

校验：vue-tsc ✅ / vitest 266 ✅ / vite build ✅。

## 中栏底部加回三 tab

用户需求：中栏底部也要 人物/场景/道具 三个 tab（此前 v2 改版把 tab 移进了抽屉、底部改为平铺）。

改动（`PanelAssetTabs.vue`）：
- 底部卡片列表前加 tab 行（样式与抽屉内 tab 一致：底部 2px 边框高亮 + 分类计数）。
- 新增 `bottomTab` ref + `bottomEntries`（按 bottomTab 过滤 orderedEntries）；切分镜时自动定位到第一个有绑定的分类。
- 当前 tab 无绑定时显示空态「本分镜未绑定XX资产」。

校验：vue-tsc ✅ / vitest 266 ✅ / vite build ✅。

## 中栏底部简化为纯缩略图墙

用户需求：底部每个 tab 只显示缩略图，不显示其他（资产名 tag、状态明细行都去掉），缩略图从左往右排列自动换行。

改动（`PanelAssetTabs.vue`）：底部改为 flex-wrap 缩略图墙（h-16 w-16，图N 角标保留），点击打开抽屉（悬浮 title 显示「资产名 · 状态」作识别）；无图显示虚线「无图」占位（同样可点开抽屉）。AssetBindingTag / cellVariantRows 在底部不再渲染（抽屉内保留）。

坑：上轮补空态段落时插错了层级，产生 Invalid end tag（vite build 抓出），已修正——Vue 模板改动后必须跑一次 build 而不只是 tsc。

校验：vue-tsc ✅ / vitest 266 ✅ / vite build ✅。

## 底部交互再调整：缩略图点击=放大预览，抽屉入口移到 tab 行按钮

用户需求：tab 行右侧加「打开抽屉」按钮；缩略图点击改为放大预览。

改动（`PanelAssetTabs.vue`）：
- tab 行右侧加「参考图抽屉」按钮（SlidersHorizontal 图标，ml-auto）；openDrawer 改收可选 type（缺省用底部当前 tab）。
- 有图缩略图点击 → inspectEntry（emit preview，定位到当前选中图）；「无图」占位仍点击开抽屉（定位当前 tab）。

## 底部 tab 行右侧入口改为「更多 >」

用户需求：右侧不要按钮，改成一个文字「更多」+ ">" 图标。

改动（`PanelAssetTabs.vue`）：tab 行右侧入口从描边按钮（SlidersHorizontal + 「参考图抽屉」）改为纯文字链接「更多」+ ChevronRight 图标（hover 变青色），点击仍打开抽屉。

## 画面描述模板下线「绑定资产 / 全章资产设定」变量与共用属性输出协议

用户需求：① 推导画面描述的模板不再有 {{绑定资产}} / {{全章资产设定}}（资产外观交给参考图清单承载，模型照图号引用即可）；② 输出协议里两段「不要写全局画风、共用属性（前置/后置）…」说明删除——AI 不用感知共用属性的存在。

改动：
- `promptTemplateRegistry.ts`：VARIABLE_REGISTRY 删 '绑定资产' / '全章资产设定' 两个变量条目（legacy 'assets' 映射随之失效）；OUTPUT_FORMAT_SPECS 两个 panel-prompt 类型删末尾共用属性声明句；推荐模板删【本分镜绑定资产视觉设定】/【本章各镜绑定资产视觉设定】段、「出场资产外观必须与资产视觉设定一致」写作要求、以及「不写全局画风与共用属性」要求行（章节版 intro 文案同步）。
- `panelPromptService.ts`：buildPanelPromptPrompt / buildChapterPanelPromptPrompt 删对应 values 与 `assets` 参数；删除 `buildPanelAssetsContext` 与 `typeLabel`（唯一消费方就是该变量）。resolvePanelAssetStates 保留（panelRefManifest / assetUsageService 仍消费）。
- `LongProjectStoryboardTab.vue`：两个调用方删 `assets` 实参。
- 测试：promptBuilders（删 assets 实参）、panelRefManifest（模板串与断言更新 + 新增「注册表已无绑定资产/全章资产设定」断言）、panelAssetStates（buildPanelAssetsContext describe 改为仅锁 resolvePanelAssetStates 展开）、storyboardAssetStateFlow（删绑定变量用例）。

口径更新：画面描述模型只感知 分镜信息 + 参考图清单（图号）+ 前文上下文；资产外观/画风全部经参考图与生图时拼接承载。已有用户模板里的这两个变量渲染为空——需要用户自行更新模板。

校验：vue-tsc ✅ / vitest 264 ✅ / vite build ✅。

## 整章画面描述手动导入卡顿优化（20:20）
- 根因：`confirmPromptImport` / `runChapterPrompts` 循环逐条 `await upsertArtwork()`，每条都是一次「DB 全量读 → 两次全量深拷贝 → 全量写回」的串行队列任务，34 镜 = 34 次全量 IO。
- 修复：LongProjectStoryboardTab 新增 `upsertArtworksBatch(items)`，合并为一次 read-modify-write（跳过已删分镜）；导入与整章推导两处循环均改用它。
- 反馈：ManualResultImportDialog 新增 `busy` prop，确认按钮转圈禁用（"导入中…"）。
- 新规则：**批量写 panelArtworks 一律用 upsertArtworksBatch，禁止循环 await upsertArtwork**。
- 验证：vue-tsc 0 错误 / vitest 264 / vite build ✅。

## 画面描述输出格式改版（20:40，方案 B 定稿）
- 目标：每镜 imagePrompt 输出三段结构：「资产参考图：」小节 → 「请根据以上参考图生成一页N格漫画。」→ 逐格「第X格：」小节（景别镜头/画面内容/使用图M声明/构图衔接）。
- 用户拍板：保留视觉状态名、场景/道具写格号范围、选方案 B（清单版式=输出版式，模型照抄）。
- panelRefManifest.ts：assetsOnly 分支行版式改为输出版式（新增 ASSET_TYPE_REF_NOUN / ASSET_TYPE_USAGE_CORE；人物无格号、场景/道具带「仅用于第N格的」）；完整清单版式不变。
- promptTemplateRegistry.ts：panel-prompt / panel-prompt-chapter 的 OUTPUT_FORMAT_SPECS 重写为三段协议；推荐模板写作要求同步。
- LongProjectStoryboardTab.vue：COPY_FORMAT_NOTE 补一句「每段按返回格式三部分输出」。
- 测试：panelRefManifest.test.ts 4 处断言更新为新行版式。验证全绿（264 tests / vue-tsc / build）。

## 成图交互改版：去悬停卡 + 去「采纳」环节（22:00）
- **悬停卡全删**：AssetHoverCard.vue 已删除（文件级），useAssetHighlight 只剩高亮着色 + 滚动同步 + 点击取词（onPick 仅提示词框接）。分镜内容/提示词两输入框的悬停卡、坐标反查命中（hitAssetAt/rAF）、悬停卡状态机全部移除。识别资产 chips 改纯点击（AssetBindingTag inspect）。
- **成图口径定稿：显示哪张就用哪张**（导出即 selectedImageId）。PanelPreview 重写：无候选图暂存区/「点击采纳」；生成成功即 selectedImageId = 新图；多张时悬停主图出左右箭头（循环切换，立即落库）+ 右上角删除 + 「N/M」角标；点击图打开全量大图预览。
- **删除一律弹窗确认**：PanelPreview 内置 ConfirmDialog（z-50）；大图预览内删除走 storyboard tab 新增 ConfirmDialog（z-[210] 压预览 z-[200]）；资产工作台生成图删除（卡片叉 + 预览删除）走 workbench 内 ConfirmDialog（z-[210]）。
- **删除无残留**：PanelGenAssetTab.updateAssetVariant 检测 patch 差集（被删图 id），同步清扫 storyboardRuns 全部页级/格级绑定 selectedImageIds（resolvePanelRefImage 本就自愈，此为数据级兜底）。
- 删空成图：selectedImageId=undefined + genStatus='none'（可再被批量生图命中）。
- 顺手修：useStoryboardOps 删除页确认文案计数重复（selected 曾与候选重复计数）；StoryboardMergeDialog「N 张候选」→「N 张生成图」。
- 验证：vue-tsc 0 / vitest 264 / build ✅。

## 细节调整（22:10）
- PanelPreview 主图悬停删除按钮从右上角移到**右下角**（bottom-2 right-2），注释同步。vue-tsc/build 通过。

## 模板默认选中第一个（22:15）
- PanelPromptGenerateModal：打开弹窗时若记忆模板不在当前生成方式的列表里，回退选中**第一个**模板（此前留空需手选）；新增 activeTemplates watch，切换发送方式（逐镜/整章换模板列表）导致模板失效时也自动回退第一个。
- 其余带模板选择的弹窗（AssetPromptGenerateModal、LongProjectAssetTab 资产提取、useStoryboardRun/useChapterDocRun initDefaults）本就默认第一个，无需改。

## 全链路统一 Markdown（23:15，v5 定稿）
- 用户拍板：分镜 v4 一并转纯 Markdown；「统一语法、解析专用」（不抽重型统一解析算法）。
- storyboardService：MD_CELL_TITLE_RE（### 第X格）主分支 + STRUCTURAL_RE 加 #；FIELD_RE 分支改为格内/页级双语义（格内 镜头=运镜、台词 pushSpeech 拆说话人、出场资产 pushCellAssets 进格）；serializePanelBlock / formatCellsForPrompt 输出 markdown；buildPanelPolishPrompt 措辞同步；v4/v3/v2 兼容保留。
- panelPromptService：buildChapterPanelPromptPrompt 输入标记 → `## 分镜 N`；**修复 parseChapterPanelPrompts 存量 bug**：旧正则把 `第N格` 当分镜标记（会截断三段结构的格小节正文），现只认 `分镜N`/`第N镜`。
- promptTemplateRegistry：ASSET_PROMPT_FORMAT → `## 资产名｜状态名`；storyboard / panel-prompt-chapter 输出协议与推荐模板全面 markdown 化。
- UI 文案：LongProjectStoryboardTab（COPY_FORMAT_NOTE、导入 placeholder/预览、toast）、PanelPromptGenerateModal、LongProjectAssetWorkbench、PanelContentEditor placeholder、assetPromptParser 错误文案。
- 测试：manualImport（v5 往返 + v4 兼容升级 + 多页 markdown 解析）、panelRefManifest（md 标记 + 第N格不截断回归用例）、promptTemplateRegistry、panelAssetStates、promptBuilders 同步；267 tests / vue-tsc / build 全绿。
- docs/长篇故事剧本与分镜格式定稿.md → v5（标题、0 表格、2.2 规范+示例、4.4 协议；v4 历史示例保留并注明换算规则）。
