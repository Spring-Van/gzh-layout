# 2026-09-11 工作日志

## 分镜格式升级 v4：符号规则 + 13 字段 + 左栏格数标签

用户明确否掉了上一轮的「台词用 `【】` 包裹」方案（v3.1），改为 **v4 符号规则**：

```
## 分镜 1 · 双格
【第1格】
「景别」：近景转特写
「镜头」：从角色A侧脸下摇至小臂
「画面」：角色A的手臂占前景，指节收紧，青筋凸起。
「人物」：角色A
「动作」：指节收紧，手臂绷紧。
「表情」：侧脸冷硬，眼神压着怒意。
「台词」：角色A：“破坏了它的关键部分，跟要了它的命有什么区别？”
「音效」：沙沙——表面摩擦。
「光效」：暖黄顶灯，背景压暗，手臂局部高光。
```

要点：
- **符号规则**：分镜格用 `【第X格】`、内容标题用 `「XXX」`、冒号后写具体内容。`【】` 从此只用于 `【第X格】`；`「」` 只用于字段名，二者不混用。
- **13 字段固定顺序**：景别 / 镜头 / 画面 / 人物 / 动作 / 表情 / 台词 / 心声 / 画外 / 旁白 / 音效 / 光效 / 备注；没有内容的字段**整行省略**。
- **台词类四选一**：`台词` / `心声` / `画外` / `旁白` 同一格最多出现一个。写法 `「台词」：说话人：“台词”`（引号中文，入库去引号、出库补回）。`「旁白」` 不带说话人。
- 语义注意：v3 的 `【镜头】` = 景别；v4 拆成 `「景别」`（→ `cell.shot`）与 `「镜头」`（运镜 → `cell.camera`）。解析器按行首符号分流，两套语法互不干扰。

### 代码改动

| 文件 | 改动 |
|---|---|
| `types/index.ts` | `LongProjectStoryboardCell` 增 `camera/cast/action/expression/sfx/lighting/note`；`LongProjectStoryboardPanel` 增 `cellLabel` |
| `services/storyboardService.ts` | 新增 `CELL_TITLE_RE` / `LABEL_FIELD_RE` / `splitSpeech`；解析分支顺序 **v4格标题 → 旧`-字段：` → v3格行 → v4字段行 → v2行内分格 → 方括号行 → 旁白行 → 说话人行 → 续行**；续行按 `lastCellKey` 并回；`serializePanelBlock` 输出 v4（空字段省略、整页空白返回 `''`）；`summarizeCells` 保持裸文本；新增 `cellCountLabel` / `resolvePanelCellLabel` / `formatCellsForPrompt`；`buildPanelPolishPrompt` 改 v4 口径 |
| `services/promptTemplateRegistry.ts` | storyboard 输出协议 + 推荐模板四节全部改 v4 |
| `services/panelPromptService.ts` | 「当前分镜」块新增**分格详情**（景别/镜头/画面/人物/动作/表情/音效/光效；台词类不进），无 cells 时行为不变 |
| `components/panel-gen/PanelListSidebar.vue` | 第二行由内容预览改为**格数标签**（`resolvePanelCellLabel`），内容预览降为 hover title |
| `components/panel-gen/PanelContentEditor.vue` | 占位示例改 v4 |
| `composables/useStoryboardOps.ts` | **合并**时把各页 cells 顺序接起来并重算 `cellLabel`；**拆分**时清空 cells/cellLabel（拆的是页级画面文本，分格结构作废，避免左栏显示过期格数） |
| `components/LongProjectStoryboardTab.vue` | `savePanelEdit` 按实际格数重算 `cellLabel` |

### 设计判断
- **左栏第二行**：用户要「不显示内容、改显示页头解析出的『双格』」。实现为 `cellLabel`（解析页头 `· X格`，旧数据按 `cells.length` 推导），并在**所有会改变格数的路径**（编辑保存 / 合并 / 拆分）同步刷新，避免标签过期。内容预览保留为 hover tooltip（不占空间、不丢信息）。
- **兼容**：v3 / v2 / 旧 `- 字段：` 全部照旧解析；旧文本进编辑器时序列化为 v4（打开即升级，且幂等）。

### 文档
- 《长篇故事剧本与分镜格式定稿.md》升 **v4**：头注 / §0 / §2.1~§2.4 / §2.7（22 页示例用脚本机械转换）/ §2.8 / §4.3 / §4.4 / §5 / §6 全部改口径。
- 《长篇故事剧本分镜设计方案.md》§2.2 / §3.2（新增 C″ 行）/ §3.3 / §3.4 / §3.5 / §5.6 / §7 / §8.5（新增本轮改动记录）。
- 《长篇故事分镜分格显示改造方案.md》顶部加**§1 作废声明**（【】方案已被 v4 取代），§0 状态列、§5、§6 更新为「左栏已落地 / 右栏待拍板」。

### 校验
`vue-tsc --noEmit` 无报错；79 个单测通过（新增 v4 逐字段解析 / v4 往返 / v3 升级 v4 / 空白页 / 格数标签 / 分格详情）；`vite build` 通过。

### 待拍板（界面呈现，不影响生成）
① 右栏内容区的分格可视化：C1（单框 + 格间空行 + 顶部只读格导航）还是 C3（分格卡片）；② 生成完成 toast 报「N 页 · 共 M 格」；③ 手动导入解析预览摘要带分格构成；④ 拆分弹窗改「格拆分」（现在拆的是页级画面文本，拆完分格结构会丢）。

---

## 修复：资产全屏抽屉内子弹窗被遮（z-index）

### 现象 / 根因
分镜页的**资产全屏抽屉**（`LongProjectStoryboardTab.vue`，`fixed inset-0 z-[101]`）内再打开的子弹窗多为 `z-50`（少数 `z-[100]/[101]`），层级低于 101 → 被抽屉盖住不可见。

### 修复
统一把"会在资产抽屉内打开"的浮层提到 **`z-[130]`（遮罩）/ `z-[131]`（面板）**——高于抽屉 101、低于大图预览 200：
- `AssetVariantCard.vue`（编辑视觉状态）z-50 → z-[130]
- `AssetPromptGenerateModal.vue`（生成/重写提示词）z-50 → z-[130]
- `AssetImagePickerModal.vue`（选参考图）z-50 → z-[130]（也用于分镜页 PanelAssetTabs，层级更低，不受影响）
- `AssetImageGenDrawer.vue`（生图配置抽屉）z-[100]/[101] → z-[130]/[131]
- `common/PromptPreviewDialog.vue`（发送前确认预览）z-50 → z-[130]
- `common/PromptRunBar.vue`（窄形态模型/模板配置浮层）z-[60]/[70] → z-[130]/[131]
- `common/ManualResultImportDialog.vue`：新增 `zIndexClass` prop（默认 `z-50`，保持通用组件的原有行为）；资产页 `LongProjectAssetTab.vue` 传 `z-[130]`。**不能硬提**——它在 LongProject.vue 里与 `ConfirmDialog z-[120]` 配对，硬提会反过来遮住覆盖确认弹窗。

### 校验
`vue-tsc` 无报错；79 单测通过；`vite build` 654 模块通过；产物 CSS 中 `z-[130]`/`z-[131]` 均已生成。

---

## 审查：资产提取链路 —— 绘画提示词该由谁生产（规划，未改码）

### 现状（三块）
1. **提取默认提示词**两处：`promptTemplateRegistry.ts` 的 `extract` 推荐模板正文（角色：资产分析师；要求「有明确外观信息时写视觉描述和绘画提示词」「信息不足时留空，不要猜测」）+ `OUTPUT_PROTOCOL_DEFAULTS.extract`（`### 视觉状态：名称`，块内字段：视觉描述 / 状态标签 / **绘画提示词**）。变量表 `VARIABLE_REGISTRY.extract` 只有 章节原文/原文分析/漫画剧本/分镜概要/已有资产 —— **没有「风格上下文」**。
2. **解析**：`assetExtractionService.ts` → `parseChineseAssetReport`（`# 人物/场景/道具` + `## 资产名` 切片）→ `parseCandidateContent`（逐行 `- 字段：值`；`### 视觉状态：X` 起块；字段「视觉描述/状态标签/绘画提示词」按 currentState 或 pendingStateFields 回填）→ `states[]`。另有 `parseLegacyJson` 兼容旧 JSON（`visualVersion.imagePrompt`）。序列化 `serializeCandidateContent` 反向输出。
3. **落库与消费**：`assetExtractionConfirm.ts` 把 `state.imagePrompt` 写进 `variant.imagePrompt`。消费方三处：资产生图工作台（`LongProjectAssetWorkbench.vue` 直接用 `variant.imagePrompt` 生图）、分镜画面描述（`panelPromptService.buildPanelAssetsContext` 注入「绘画提示词：」）、文本自动绑定扫描（`promptAssetService.panelScanText` 含 imagePrompt）。

### 诊断（4 个问题）
1. **角色错位**：提取模板是「分析师 + 不编造 + 信息不足留空」，与绘画提示词需要「补全为可直接生图」相冲突。
2. **缺风格上下文**：extract 模板无风格变量 → 提取阶段产出的 imagePrompt 天然无画风一致性。
3. **「仅补缺失」把专门环节跳过**：`LongProjectAssetWorkbench` 的 `promptTargets` = `variants.filter(v => !v.imagePrompt?.trim())`；提取既已写入 → 不再走带风格上下文的 `asset-prompt` 环节。
4. **解析脆弱 + 语义冗余**：`- 绘画提示词：xxx` 是单行匹配，长提示词若换行会被截断；且 `description`（视觉描述）与 `imagePrompt` 语义重叠。

### 建议方案（待拍板）
**分层**：提取阶段只产「视觉状态 + 视觉描述（客观特征）」；绘画提示词生产权下移到 `asset-prompt` 环节（它有风格上下文 / 目标生图模型）。`variant.imagePrompt` 字段**保留不动**（三处消费方接口不变），只是不再由提取阶段写入。
- 备选 A（维持现状）不推荐；备选 B（提取可选「顺带出初稿」开关）增加复杂度，暂不采用。
- 涉及改动：`extract` 模板正文与输出协议、`parseCandidateContent`/`serializeCandidateContent`（保留「绘画提示词」解析分支作向后兼容）、`assetExtractionConfirm` 落库时不再写 imagePrompt、审核页右列状态标签（如需展示特征）。
- 风险：存量 run 的 candidates.states 已带 imagePrompt → 保留即可，不破坏；旧输出仍可解析。

### 方案 C 已落地（用户拍板：采用 C / 不新增字段复用视觉描述 / 审核页维持现状）
- `promptTemplateRegistry.ts`：
  - `OUTPUT_PROTOCOL_DEFAULTS.extract`：视觉状态块内字段由「视觉描述、状态标签、绘画提示词」改为「视觉描述、状态标签」；新增「视觉描述只写客观外观特征（体型/发型发色/面部/服饰材质/配色/标志物/受损痕迹）…不要输出绘画提示词」。
  - `extract` 推荐模板【信息要求】：`写"视觉描述"和"绘画提示词"` → 只写"视觉描述"（客观特征），并加一句「这里只沉淀可复用的外观特征，绘画提示词由后续『资产绘画提示词』环节生成」。
  - `asset-prompt` 推荐模板【写作要求】：新增「清单中的『视觉描述』是客观特征基准，必须完整体现」；末条由「严格基于清单给定信息撰写」改为「不得编造与清单信息冲突的细节」。
- `assetExtractionService.ts`：`parseCandidateContent` 的「绘画提示词」分支加注释，标明**向后兼容**（旧协议结果 / 手动导入仍解析）；`serializeCandidateContent` 保留输出。**落库逻辑未改**——提取不再产该字段即天然为空，但手动导入带此字段仍会保留。
- 校验：79 单测通过；`vue-tsc` 无报错；`vite build` 渲染层 2155 模块 + 主进程 654 模块通过。

## 审查：资产提取提示词的「变量拼接」机制（诊断，未改码）

### 渲染规则（`promptTemplateRegistry.renderPromptTemplate`，全链路唯一）
对每个注册变量：**在模板中 → 原地替换**（值为空用 `emptyText`）；**不在模板中 → 按 `fallback` 兜底**：`always` / `if-nonempty` 且有值且有 `blockLabel` → 追加 `【blockLabel】\n值` 到末尾；`drop` → 跳过。最后清理未注册 `{{...}}` + 附加输出协议。
→ **结论：没写进模板的变量会被自动补充**，这是"防丢上下文"设计，但用户无法用"只写 {{章节原文}}"来精简。

### extract 的 5 个变量兜底
| 变量 | fallback | 未插入且非空时 |
|---|---|---|
| 章节原文 | always | 追加【章节原文】 |
| 原文分析 | if-nonempty | 追加【原文分析】 |
| 漫画剧本 | if-nonempty | 追加【漫画剧本】 |
| 分镜概要 | if-nonempty | 追加【本章分镜概要（已确定会被绘制的画面）】 |
| 已有资产 | if-nonempty | 追加【项目已有资产】 |

### 分镜概要的数据源与口径
- 数据源：`LongProjectAssetTab.vue` → `latestStoryboardRun`（本章 `status==='completed'`，按 **updatedAt** 倒序取首个）；分镜页 `currentRun` **同口径**，两页一致。
- 生成：`panelsOutline = panels.map(p => '分镜'+order+'：'+p.content)` —— **只取页级 `content`（各格「画面」拼接），不含格级「人物」/台词**。v4 分格里 `cast`（人物）才是角色出现的关键证据。
- 相关缺口：`countCandidatesAppearances`（出现次数统计，用于审核页参考重要性）扫描文本 = `content + dialogue + narration + imagePrompt`，**不含 `cells[].cast`**；`promptAssetService.panelScanText`（自动绑定扫描）同样不含 cast。
- "不是最新"的排查方向：① 排序用 `updatedAt` 而非 `createdAt`，编辑旧 run 会把它顶成"最新"；② 只取 `completed`，最新 run 若为 running/failed 会退回上一版。

### 建议优化项（待用户确认）
1. 分镜概要带格级要点（人物 + 画面），或复用 `formatCellsForPrompt` 精简版 —— 直接提升角色/道具召回。
2. `countCandidatesAppearances` 与 `panelScanText` 纳入 `cells[].cast`。
3. 兜底策略透明化：设置页变量列表标注「未插入时是否自动追加」，让用户可预期（不建议新增禁用语法）。
4. 可选：重新评估「原文分析 / 漫画剧本」与「章节原文」的信息重叠，考虑降为不自动追加，减少噪声与 token。

---

## 落地：废弃变量兜底 —— 变量插入完全由模板作者控制

用户拍板：**去掉兜底自动追加**，改「模板里写了才替换」，并要求**默认/推荐模板必须自带全部变量**。

### 代码改动
| 文件 | 改动 |
|---|---|
| `services/promptTemplateRegistry.ts` | 删除 `VariableFallback` 类型与 `PromptVariableSpec.fallback` / `.blockLabel`；渲染引擎删掉 `appends` 追加逻辑，只做「命中才替换（空值落 `emptyText`）+ 清理未注册占位符」；新增 `defaultTemplateContent(type)`（= 该类型推荐模板内容，内置默认模板同源） |
| 同上 · 推荐模板 | `extract` 正文补齐 `{{原文分析}} / {{漫画剧本}} / {{分镜概要}} / {{已有资产}}`，并加「分镜概要仅用于判断重要性/出现频率，不作外观依据」；`panel-prompt` 正文补 `{{镜头}}`；`extract` 默认输出协议里「参考分镜概要」改为「在章节原文中反复出现」 |
| `services/chapterDocService.ts` / `storyboardService.ts` / `assetExtractionService.ts` / `assetPromptService.ts` | 模板内容为空时回落到 `defaultTemplateContent(type)`（否则零模板 = 空提示词） |
| `services/panelPromptService.ts` | `DEFAULT_PANEL_PROMPT_TEMPLATE` 改为 `defaultTemplateContent('panel-prompt')`（原内置模板没有变量，去兜底后会变空） |
| `services/assetExtractionService.ts` | 新增 `cellCastNames` / `panelCastNames` / **`buildPanelsOutline`**：每镜一行 `分镜N：{画面}｜人物：{各格 cast 去重}`；`countCandidatesAppearances` 扫描文本纳入 `panelCastNames(panel)` |
| `components/LongProjectAssetTab.vue` | 改用 `buildPanelsOutline(panels.value)`（与 service 同源，消掉重复公式） |
| `features/settings/components/TemplateEditorForm.vue` | 变量区副标题补「未插入的变量不会进入提示词」 |
| `docs/长篇故事提示词模板改造方案.md` / `长篇故事提示词模板梳理.md` | 顶部/§4 加作废声明，标注兜底策略已被最简方案取代 |

### 关键判断
- **分镜概要**：`if-nonempty` 自动追加废弃后，它变成"作者可选"。用户明确要求推荐模板必须含全部变量 → 已插入 `{{分镜概要}}`。
- **取值形态**：保持「每镜一行」，但由「仅画面」升级为「画面 + 人物」。不带全字段（13 字段全文）——台词/音效/光效/备注是绘制指令不是资产特征，且会带来下游演绎噪声与 token 膨胀。
- **测试守卫**：新增「六个类型的推荐模板都显式插入了该类型全部变量」用例，防止模板再漏变量。

### 校验
`vue-tsc --noEmit` 无报错；**80 个单测通过**（改写 3 个旧兜底断言 + 新增 2 个）；`vite build` 渲染层 2155 模块 + 主进程 654 模块通过。

---

## 分镜模板补注册「章节原文」+ 两个模板的变量顺序调整

### 背景
- 分镜（`storyboard`）之前只声明 `漫画剧本` / `原文分析` 两个变量，**「章节原文」从未注册**，用户在设置页想手插也插不了 —— 与「变量插入完全由作者控制」原则冲突。

### 改动
| 文件 | 改动 |
|---|---|
| `services/promptTemplateRegistry.ts` | `VARIABLE_REGISTRY.storyboard` 新增 `章节原文`（**刻意不带 `legacy` 别名**，避免旧模板误用的 `{{chapter_content}}` 被自动迁移成整章原文）；推荐模板【原文核对】补一条「章节原文用于补足剧本没写全的视觉细节，不要把剧本已删减的剧情加回」；description 更新 |
| `services/storyboardService.ts` | `buildStoryboardPrompt` 新增第 4 参 `chapterContent`，注入 `{{章节原文}}`；**剧本兜底时（scriptContent === chapterContent）不再重复渲染章节原文**（原文只出现一次） |
| `composables/useStoryboardRun.ts` / `components/LongProjectStoryboardTab.vue` | 两个调用点把章节原文传进去 |
| `docs/长篇故事提示词模板梳理.md` | §3 分镜变量表补 `章节原文` |

### 变量顺序调整（用户指定）
- **分镜**：`{{章节原文}}` → `{{原文分析}}` → `{{漫画剧本}}`
- **资产提取**：`{{章节原文}}` → `{{原文分析}}` → `{{漫画剧本}}` → `{{分镜概要}}` → `{{已有资产}}`
- 两处均把「章节原文」从原来的**末尾**提到**最前**。（注意：这与「指令在前、素材在后」的旧惯例相反 —— 用户明确要求，照做。）
- **补充（同一轮）**：设置页「变量状态」tag 顺序**取自 `VARIABLE_REGISTRY[type]` 的顺序**（`TemplateEditorForm.vue` 的 `variableSpecs = getTemplateVariables(type)`），**不是**模板正文顺序 —— 只改模板正文，表单 tag 不动。故同步把 `VARIABLE_REGISTRY.storyboard` 也调成 `[章节原文, 原文分析, 漫画剧本]`。`extract` 注册表顺序本来就是目标顺序，无需改。

### 校验
`vue-tsc --noEmit` 无报错；**82 个单测通过**；`vite build` 渲染层 2155 模块（dist/，9.35s）+ 主进程 654 模块（dist-electron/main.js）+ preload 通过。

### 环境坑（重要）
`pnpm build:renderer` 在 chunk 渲染阶段被静默中断、日志停在「2155 modules transformed」且退出码可能假报 0 —— 两种成因，按序排查：
1. **孤儿进程**：上一轮被中断的构建残留 node/esbuild 进程（锁文件、占内存）。`Get-Process node,esbuild` 找出「非 11:xx 起的」残留 PID → `Stop-Process -Force`。
2. **Node 堆不足**（本机内存紧张）：本机 16GB/默认堆 ≈4014MB，实测 `--max-old-space-size=3072` 反而调小、**无效**；真因是可用物理内存被其他程序吃光。事后在 `vite.config.ts` 顶部加了自举（用 NODE_OPTIONS 更高堆重拉一次），**但实测仍未解决**（vite 仍停在同一点），待后续排查。
**注意：不要误杀 11:29 起的 vite dev / electron（用户开发环境）。** 排查时先跑 `vue-tsc --noEmit && vite build` 里的 vue-tsc 段——它能过说明类型检查 OK，崩的是 vite 打包段。

---

## 长篇项目：切 tab 回来恢复「上次选中的章节/资产分类」（2026-09-14）

### 背景
用户反馈：切到别的菜单 tab 再切回长篇项目 tab，左侧章节树没有选中当时那章、右栏回空白。

### 根因分析
- tab 体系：`App.vue` 的 keep-alive 缓存的是**每个 tab 的包装组件**（`getTabComponent` → `Tab_<matchKey>`，name 稳定），漫画子页面（`long-project` / `project-assets` / …）共享 tab（`dk='comic-project'` → `matchKey=comic:p:{projectId}`）。
- `ComicLayout.vue` **内部是裸 `<router-view />`，无 keep-alive**；`LongProject.vue` 内所有状态（`selectedChapterId` / `activeTab` / `storyboardOpened` 等）都只是组件局部 ref。
- 全库确认：`selectedChapterId` 只在 `selectChapter` / `selectAssetCategory` / 新建 / 删除时被写入，**没有任何地方在 onActivated 时重置它** → 说明实例在某些路径下会被重建，局部 ref 随之丢失。

### 方案（用户拍板：持久化选中章节）
不依赖 keep-alive 是否命中，直接**按项目持久化选中态**到 localStorage，激活/挂载时读回。

### 代码改动（`src/modules/comic/views/LongProject.vue`）
| 位置 | 改动 |
|---|---|
| 常量区 | 新增 `LAST_SELECTION_PREFIX = 'comic-long-last-selection:'` + `type LastSelection = { chapterId?; assetCategory? }` |
| 新增 | `readLastSelection()` / `persistLastSelection(patch)`：按 `projectId` 隔离读写，try/catch 静默降级 |
| `selectChapter` | 写入 `{ chapterId, assetCategory: null }` |
| `selectAssetCategory` | 写入 `{ chapterId: null, assetCategory }` |
| `handleNodeDialogSubmit` | 新建章节后写入 `{ chapterId: newNode.id, assetCategory: null }` |
| `confirmDelete` | 删除选中章节时清 `chapterId`（防悬空 id） |
| 新增 `restoreLastSelection()` | 校验 id 在 `chapters` 中真实存在才恢复（防已删/换项目悬空）；同步 `draftContent`；页签落到该章节首个可用页签（`startMode==='script'` → script） |
| `onMounted` | `recoverInterrupted()` 之后调用 `restoreLastSelection()`（关闭页签重开 / 重启应用生效） |
| `onActivated` | `loadProject()` 之后，**仅当当前无选中章节且无选中分类时**才恢复（避免覆盖 keep-alive 已保住的状态） |

### 校验
`vue-tsc --noEmit` 无报错；82 单测通过。

### 遗留
`vite.config.ts` 顶部的堆自举**实测无效**（见上方「环境坑」第 2 条），本机内存紧张时构建仍会在 chunk 渲染阶段中断，待后续排查（可能是 spawn 后 NODE_OPTIONS 未传递给 esbuild worker / 或进程被系统 OOM killer 直接杀掉，不受 V8 堆限制）。

---

## 改造：批量生成绘画提示词弹窗 —— 单弹窗内完成配置 / 逐条查看修改 / 进度

### 用户拍板的约束
1. 不去掉弹窗；2. 保留「生成范围」「发送方式」两项配置；3. **不弹第二个进度窗口**，进度显示在弹窗内「最终发送的提示词」文本框**下面**；4. 逐条发送时可在文本框上方**切换查看每一条**，下方文本框可修改。

### 中途返工（重要教训）
第一版做成了 **弹窗两阶段（config → progress）** + 独立进度视图（左侧条目清单 + 右侧编辑器）。用户否掉：**不需要切换视图/第二个弹窗**，配置项和提示词框都应始终可见，进度只是提示词框下方的一个区域。→ **教训：用户说「弹窗内」时，优先理解为「同一弹窗同一视图内加内容」，而不是「同一弹窗换阶段」。**

### 最终实现（单视图，无 stage 切换）
- 上方固定：LLM 模型 / 提示词模板 / 生成范围 / 发送方式（执行中全部 disabled）。
- 中间提示词区按发送方式分支：
  - **一次性发送**：一个「最终发送的提示词」textarea（发送前可整体改）。
  - **逐条发送**：textarea **上方**一条横向**条目切换条**（`overflow-x-auto`，每条含状态图标 + 「资产 · 状态」+ dirty 黄点），点谁切谁；下方 textarea 即该条提示词。**未开始时 `text` = 按模板拼装的预览（可改，改后按用户内容发送）**。
- 下方进度区：`v-if="started"`，进度条 + `已完成 N/M` + 失败重试入口。`started` 由点「开始生成」置 true（`applyProgress` 也会兜底置 true）。
- 底部按钮随状态切换：未开始 → 取消 / 开始生成；已开始 → 关闭 / 保存结果。

### 接口设计（关键）
| 接口 | 用途 |
|---|---|
| `buildPrompt(template, scope, sendMode)` | 一次性发送的整份清单文本（保持不变） |
| **`buildItems(template, scope)`** | **新增**。逐条模式：返回 `AssetPromptRunItem & { prompt }`，`prompt` = 该条按模板拼装的初始文本。父组件 `buildPromptItems()` 实现 |
| `confirm` 载荷 **`perItemPrompts`** | **新增**。逐条发送时把用户改过的每条文本带回父组件，执行时**原样发送**（不再重新拼装） |
| `retry` / `save` | 保留。`retry` 时父组件用弹窗内当前的 `item.text` 作为重试文本 |
| `defineExpose({ applyProgress, items, started })` | 父组件回传进度 |

### 代码改动
| 文件 | 改动 |
|---|---|
| `components/AssetPromptGenerateModal.vue` | 重写为**单视图**。删除 `stage` / 独立进度视图 / `beforeRun`；新增 props `buildItems`；新增 `confirm.perItemPrompts`；新增 `refreshItems()`（切模板/范围时重建条目，已生成/已改的保留）；`markActiveDirty` **不再改 status**（原实现会把未开始阶段编辑过的条目误标 done，污染进度计数——已修） |
| `components/LongProjectAssetWorkbench.vue` | 删 `planBatchPrompts`，新增 `buildPromptItems`（含每条拼装文本）；`runPerItemPrompts` 新增 `userPrompts: Map<variantId, 文本>` 参数，**优先用用户改过的文本**；`runBatchPrompts` 在函数内自行解析 `batchScopeTargets`（原由 `planBatchPrompts` 设置）并在无目标时 toast；`retryFailedPrompts` 也用弹窗当前文本 |

### 校验
`vue-tsc --noEmit` 无报错；82 单测通过。**`vite build` 未验证**（已知 OOM 问题，见上）。

### 未处理的边界（如需可继续）
- 用户改了文本后直接点「关闭」而非「保存结果」→ 修改丢弃（已落库的取生成原文）。未加二次确认弹窗。
- 一次性发送若模型返回后**解析只命中部分状态**，未命中的不出现（静默缺失），只在 toast 报 `已生成 N 条`。

### 第二轮迭代（用户四项要求，已完成）

| # | 要求 | 实现 |
|---|---|---|
| 1 | **换模板不作废** | `refreshItems()` 重写：由「重算所有 pending 条目」改为 **保留全部已存在条目（文本/状态/dirty 原样）** + 只追加新条目（`added`）+ 移除不在目标范围内的条目（`kept`）。`watch([builtPrompt, isPerItem])` 删掉「一次性模式下自动重置 `prompt.value`」的行——换模板一律保留用户改动，想重算点「重置」 |
| 2 | **输入框上方右侧重置按钮** | 一次性模式：标题行右侧 `重置`（`resetPrompt`，恢复 `builtPrompt`）；逐条模式：切换行右侧 `重置本条`。`resetActiveItem()` 改为**主动向 `props.buildItems` 重新索取「当前模板」的拼装文本**（不能只回退 `originalText`——换模板后旧原文已过期），并同步 `originalText`、清 `dirty` |
| 3 | **下拉框 + 快速切换** | 横向标签条（`overflow-x-auto`，条目多时占位严重）→ 换成 `<select :value="activeIndex" @change="selectIndex(...)">`（选项前缀 `statusMark(item)` 字符表示状态 `◐/✓/✕/○`，dirty 加「（已改）」）+ `ChevronLeft/ChevronRight` 前后切换按钮（各自在首/尾 disabled）+ `N / M` 计数。新增 `selectIndex(index)`（clamp + `userPickedIndex = true`）、`statusMark(item)`；图标导入改为 `ChevronLeft, ChevronRight, CircleCheck, LoaderCircle, X`（移出 `Circle`/`CircleX`） |
| 4 | **输入框到底 + 开始后压缩** | 内容容器由 `overflow-y-auto` 改 **`overflow-hidden`**（`flex min-h-0 flex-1 flex-col`）；两个 textarea 由固定高度（240/320/200/280px）改为 **`:class="started ? 'h-[160px] shrink-0' : 'flex-1'"`**，基础 class 加 `min-h-[120px]` → 未开始撑满剩余高度，开始后固定 160px 把空间让给下方进度区 |

**附带修复**：新增 `userPickedIndex` 标记（用户是否手动选过条目），`applyProgress` 的 running 分支**仅在该标记为 false 时**才自动跟随 `activeIndex` —— 否则生成进度会把用户正在查看的条目抢走。`handleConfirm` / `selectIndex` 分别重置 / 置位该标记。

### 校验
`vue-tsc --noEmit` 无报错；**82 单测通过**（11 个测试文件）。`vite build` 未验证（已知 OOM）。

**补充教训**：「换模板不作废」的正确实现是**保留旧条目文本**，而「重置」才需要重新拼装 —— 两者职责必须分开写（`refreshItems` 保留 vs `resetActiveItem` 重算），不能共用一个重算函数。
