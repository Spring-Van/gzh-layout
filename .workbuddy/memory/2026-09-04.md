# 2026-09-04

- 通读长篇漫画管线的核心代码（说明文档.md + src/modules/comic）：LongProject.vue（原文/剧本双tab）、LongProjectPanelGen.vue（分镜/资产生图工作台）、useChapterDocRun/useStoryboardRun/useStoryboardOps/useLongProjectPersistence、chapterDocService/storyboardService/assetExtractionService(+Confirm)。管线：原文→分析→剧本→分镜→资产→提示词推导→生图。持久化为串行队列+队列内 read-modify-write，双页 keep-alive。

- 重构：长篇主页面扩为「原文｜剧本｜分镜」三 tab，分镜生图工作台（含资产）由独立路由 /comic/panel-gen 并入主页面分镜 tab。新增 components/LongProjectStoryboardTab.vue（props 化共享持久化实例，Teleport 注入 tab 行右侧操作按钮，含新增「资产」视图切换按钮）；删除 views/LongProjectPanelGen.vue 与路由；原文/剧本 tab 改 v-show、分镜 tab 首次进入挂载后常驻（批量任务切页签不中断）；删除 legacy 返回参数逻辑。vue-tsc + vitest(38) 全量通过。

- 修复：分镜 tab 组件为多根节点（Teleport+div）导致父页面 v-show 失效、页签切换后分镜内容覆盖其他页签 → 模板改为单根节点（Teleport 移入根 div 内）；删除顶栏重复的「N 已描述 · N 已成图」文案及 describedCount 计算属性。vue-tsc 通过。
