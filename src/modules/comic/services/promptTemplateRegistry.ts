import type { TemplateType } from '@comic/types'

/**
 * 提示词模板注册表（单一事实来源）：
 * 长篇故事全部环节的变量定义、默认输出协议、推荐模板与统一渲染引擎。
 * 设置页（TemplateEditorForm）与运行时拼装（chapterDoc/storyboard/assetExtraction/assetPrompt/panelPrompt 各 service）共用，
 * 保证「表单所见 tag = 模板实际可插变量 = 空值兜底文案 = 默认协议」全链路一致。
 */

/** 长篇故事使用的模板类型（其余 style/story 属于其它工作流，无变量与协议）。 */
export const LONG_STORY_TEMPLATE_TYPES: TemplateType[] = ['analysis', 'script', 'storyboard', 'extract', 'asset-prompt', 'panel-prompt', 'panel-prompt-chapter']

/**
 * **画面描述环节（panel-prompt / panel-prompt-chapter）的变量边界**：
 * 只给分镜内容与上下文，不给共用属性、资产参考图清单或图号。
 * `composeFinalPrompt` 会在生图时按当前图片顺序动态拼接这些内容，因此改共用属性、
 * 换视觉状态或参考图都不需要重跑画面内容推导。
 */

/**
 * 模板变量定义：变量是否进入提示词完全由模板作者决定，不做任何自动追加。
 * 模板里没写 {{变量名}}，该变量就不会出现在最终提示词里。
 */
export interface PromptVariableSpec {
  /** 变量名（中文），模板中写作 {{变量名}} */
  name: string
  /** 说明：设置页变量 tag 的 tooltip */
  desc: string
  /** 旧英文别名：兼容存量模板，渲染与迁移时归一化为中文名 */
  legacy?: string[]
  /** 变量已插入模板但值为空时的替换文案（仅模板内占位，不是自动追加） */
  emptyText?: string
}

/** 变量注册表：按模板类型定义全部可用变量。 */
export const VARIABLE_REGISTRY: Partial<Record<TemplateType, PromptVariableSpec[]>> = {
  analysis: [
    {
      name: '章节原文',
      desc: '当前章节的原文内容（编辑草稿的实时值）',
      legacy: ['chapter_content'],
    },
  ],
  script: [
    {
      name: '章节原文',
      desc: '当前章节的原文内容（编辑草稿的实时值）',
      legacy: ['chapter_content'],
    },
    {
      name: '原文分析',
      desc: '上一环节「原文分析」的产物；未生成时替换为占位提示',
      legacy: ['analysis'],
      emptyText: '（本章尚未生成原文分析）',
    },
  ],
  storyboard: [
    {
      // 故意不设 legacy：旧分镜模板里的 {{chapter_content}} 属误用，
      // 不迁移，避免存量模板被灌进整章原文。
      name: '章节原文',
      desc: '当前章节原文，用于补足剧本没写全的视觉细节；剧情推进、场景取舍与对白以漫画剧本为准。无剧本时原文已作为剧本底稿，此处不再重复',
      emptyText: '（本章无独立原文）',
    },
    {
      name: '原文分析',
      desc: '原文分析产物，仅作分镜的辅助核对',
      legacy: ['analysis'],
      emptyText: '（本章尚未生成原文分析）',
    },
    {
      name: '漫画剧本',
      desc: '上一环节「漫画剧本」的产物；本章无剧本时系统自动以章节原文兜底并提示',
      legacy: ['script_content'],
    },
    {
      name: '本章资产',
      desc: '管线环节③「资产提取」确认后的本章资产清单（每行：资产名（类型）｜各视觉状态名 + 剧情锚点一句话）；分镜需逐格声明「出场资产」并严格沿用清单中的状态名',
      emptyText: '（本章尚未确认资产，模板含出场资产要求时可留空）',
    },
  ],
  extract: [
    {
      name: '章节原文',
      desc: '当前章节的原文；本章从剧本开始（无原文）时系统自动以漫画剧本兜底并加说明头',
      legacy: ['chapter_content'],
      emptyText: '（本章尚未录入原文与剧本）',
    },
    {
      name: '原文分析',
      desc: '管线环节①「原文分析」的产物',
      emptyText: '（本章尚未生成原文分析）',
    },
    {
      name: '漫画剧本',
      desc: '管线环节②「漫画剧本」的产物；新管线中资产提取紧随其后（候选出现数按剧本文本统计）',
      emptyText: '（本章尚未生成剧本）',
    },
    {
      name: '已有资产',
      desc: '项目已有资产清单（含各资产已有视觉状态名与锚点/描述一句话），保证跨章节连续性；状态名沿用规则见输出协议',
      emptyText: '（项目暂无资产）',
    },
  ],
  'asset-prompt': [
    {
      name: '状态清单',
      desc: '批量·一次性发送 = 全部待生成状态的清单；逐条发送 = 当前状态的信息',
      legacy: ['assets'],
    },
    {
      name: '目标生图模型',
      desc: '资产生图配置选用的生图模型名，用于让提示词面向具体模型优化',
      legacy: ['target_model'],
      emptyText: '未指定',
    },
  ],
  'panel-prompt': [
    {
      name: '当前分镜',
      desc: '当前分镜完整信息（序号、镜头、画面内容、分格详情、分镜参考描述）',
      legacy: ['panel_content'],
    },
    {
      name: '镜头',
      desc: '当前分镜的镜头类型（如全景、特写）',
      legacy: ['shot'],
      emptyText: '未指定',
    },
    {
      name: '前文分镜',
      desc: '前 2 个分镜的画面内容与已推导描述（滑动窗口，批量推导时随进度刷新）',
      legacy: ['prev_panels'],
      emptyText: '当前是本章第一个分镜，无前文画面。',
    },
    {
      name: '本章分镜概要',
      desc: '本章全部分镜概要，每镜一行',
      legacy: ['chapter_outline'],
    },
    {
      name: '目标生图模型',
      desc: '生图配置选用的生图模型名',
      legacy: ['target_model'],
      emptyText: '未指定',
    },
  ],
  'panel-prompt-chapter': [
    {
      name: '全章分镜',
      desc: '本章全部分镜的完整信息（序号、镜头、画面内容、分格详情、分镜参考描述），一次交给模型，替换逐镜模式的「当前分镜」',
      legacy: ['chapter_panels'],
    },
    {
      name: '目标生图模型',
      desc: '生图配置选用的生图模型名',
      legacy: ['target_model'],
      emptyText: '未指定',
    },
  ],
}

/** 获取指定类型的变量定义列表。 */
export function getTemplateVariables(type: TemplateType): PromptVariableSpec[] {
  return VARIABLE_REGISTRY[type] ?? []
}

// ========== 返回格式说明（内联进模板内容，不再由系统注入） ==========

/**
 * **全链路统一返回格式：Markdown**（不要 JSON、不要代码块）。
 *
 * 格式说明**直接写在各类型模板的默认内容里**（见下方 RECOMMENDED_TEMPLATES），
 * 渲染时不再另外追加协议段 —— **模板内容就是唯一的提示词来源**。
 * 代价是用户若改掉内容里的格式约定就可能改坏解析，因此设置页对「结果会被解析」的环节
 * 在底部给一行提示：这些环节的结果按 Markdown 结构解析。
 *
 * 本常量保留两个用途：
 * 1. 内联进推荐模板内容（withFormatSpec）；
 * 2. 分镜「页面 AI 优化」等系统自查提示词复用同一份格式文本，避免两处描述不一致。
 */

/** 资产绘画提示词的返回格式说明：Markdown 逐条「## 资产名｜状态名」+ 提示词正文。 */
export const ASSET_PROMPT_FORMAT = `只输出中文 Markdown，不要解释、不要代码块、不要 JSON。
清单里的每个状态各输出一条，格式固定为：
## 资产名｜状态名
提示词正文

「资产名｜状态名」必须与清单中的完全一致、一字不差；不要遗漏任何状态、不要新增、不要打乱顺序。`

/** 各类型的结果结构说明（Markdown）：程序按它解析，因此默认模板内容必须带上。 */
export const OUTPUT_FORMAT_SPECS: Partial<Record<TemplateType, string>> = {
  analysis: `只输出中文 Markdown，不要解释、代码块或 JSON。用 ## 小节标题组织（如 ## 人物、## 场景、## 道具、## 事件与时间线、## 人物关系、## 对白、## 情绪、## 重要视觉信息），每条信息写为「- 名称：描述」列表项。`,
  script: `只输出中文 Markdown，不要解释、代码块或 JSON。用 ## 场景 N 组织每个剧本场景，场景内用「- 属性名：内容」列表项写明场景、剧情、人物、动作、情绪、对白与剧情目的。`,
  storyboard: `只输出中文文本，不要解释、代码块或 JSON。仅页级标题使用 Markdown：每一页使用一个 ## 分镜 N · 单格 / 双格 / 三格 / 四格 标题（N 从 1 递增，格数与页内实际格数一致）。页内内容不要使用 Markdown 标题或列表：每格单独写「第X格」，下面逐行写「字段名：内容」。
每格的字段按此顺序写，没有内容的字段整行省略：景别 / 镜头 / 画面 / 人物 / 出场资产 / 动作 / 表情 / 台词 / 心声 / 画外 / 旁白 / 音效 / 光效 / 备注。
景别 取 远景 / 中景 / 近景 / 特写 / POV / 过肩 / 仰拍 之一，每格必填；镜头 写机位与运镜（如 平视、从侧脸下摇至小臂）。
画面 写这一格画面上能看到的内容——构图、人物位置、动作、环境、关键细节，30~80 字。
人物 写这一格画面内出现的角色名，多个用、分隔；动作、表情 分别写这一格的肢体动作与神态，各 10~25 字。
出场资产 写这一格画面内出现、且在【本章资产】清单中列出的资产，格式为 编号 资产名（状态名）（编号是清单每项状态前面的字母加数字，如 A1），多个用、分隔；编号、资产名与状态名都必须照抄清单、一字不差，不得自行编号或省略编号；状态按剧情锚点判断该格所处阶段（如 换装后的林小雨），同一资产同一格只声明一个状态；同一资产在同页不同格状态不同时，各格按所在格的实际状态分别声明（如第1格便装、第2格换战斗服），不得统一成一个状态；画面内没有清单资产时整行省略，不得编造清单外的编号、资产或状态名。
台词类字段四选一，同一格最多出现一个：对白写 台词、内心独白写 心声、说话人不在本格画面内写 画外、无人称叙述写 旁白；没有就不写。
台词类字段的写法为 说话人：“台词”——说话人不能省略，必须与剧本逐字一致，引号用中文引号；旁白不带说话人。
音效、光效 按需写；备注 只写画面上必须画出来的关键点。
每句台词不超过 20 字，单格满版页不超过 30 字，超出必须拆格或拆页。
一个分镜 = 一张竖屏漫画图 = 一页，一页 1~4 格，一格只讲一个信息单元。`,
  extract: `只输出中文 Markdown，不要解释、代码块或 JSON。
一级标题只能是 # 人物、# 场景、# 道具；没有该类资产则不输出。
每项资产必须以 ## 资产名称 开始；资产字段每行写为 - 属性名：属性内容，每个字段必须在同一行写完。
资产字段使用：别名、重要性（主要/次要）、描述、原文依据；有依据时可补充人物的身份/阵营/职业/年龄阶段、场景的地点类型/时代、道具的持有者/用途。
每个视觉状态必须以 ### 视觉状态：状态名 单独成块；同一资产可输出多个视觉状态。
状态块内只能使用：状态标签、视觉描述、剧情锚点。
状态标签用顿号分隔，至少包含状态类型和 3 个具体视觉记忆点。
视觉描述必须完整、自包含、可独立用于生图，末尾写“记忆点：……”和“与其他状态差异：……”，不得使用“同上”“沿用”等指代。
剧情锚点写明从哪个事件开始生效、到哪个事件结束；初始状态写“全章默认”，回忆/闪回/梦境/未来片段写清时间线边界。
视觉状态名必须能直接供分镜绑定，不要把状态写成新的资产标题。`,
  'asset-prompt': ASSET_PROMPT_FORMAT,
  'panel-prompt': `只输出中文 Markdown，不要解释、代码块或 JSON。
只写这一镜的纯画面内容，不要输出资产参考图清单、图号、共用属性、全局画风或系统说明；这些内容会在生图前由系统根据当前图片顺序动态拼接。
逐格输出「第X格：」小节（X 从 1 递增）：先一行写景别与镜头，再一到三行写画面内容，包括人物位置、动作、表情、场景环境、光线氛围，最后可加一行构图或与前格的衔接说明。
不要写对白或旁白，不要添加模板之外的前言和结语。`,
  'panel-prompt-chapter': `只输出中文 Markdown，不要解释、代码块或 JSON。
为每一镜各输出一段，逐镜之间用 ## 分镜 N 标题行分段（N 必须与给定分镜的序号完全一致，按序号递增，不遗漏、不新增、不打乱顺序）。
每段只写该镜的纯画面内容，不要输出资产参考图清单、图号、共用属性、全局画风或系统说明；这些内容会在生图前由系统根据各镜当前图片顺序动态拼接。
每段内逐格输出「第X格：」小节（X 从 1 递增）：先一行写景别与镜头，再一到三行写画面内容，包括人物位置、动作、表情、场景环境、光线氛围，最后可加一行构图或与前格的衔接说明。
不要写对白或旁白，不要添加模板之外的前言和结语。`,
}

/** 取类型的结果结构说明（该环节不需要固定结构时返回空串）。 */
export function outputFormatSpec(type: TemplateType): string {
  return OUTPUT_FORMAT_SPECS[type] ?? ''
}

/** 把类型的结果结构说明拼到模板内容末尾 —— 推荐模板内容的唯一构建入口。 */
function withFormatSpec(content: string, type: TemplateType): string {
  const spec = outputFormatSpec(type)
  return spec ? `${content}\n\n【返回格式】\n${spec}` : content
}

// ========== 推荐模板（设置页「填入推荐模板」与文案同源） ==========

export interface RecommendedTemplate {
  /** 一键填入时的默认名称 */
  name: string
  /** 一键填入时的默认描述 */
  description: string
  content: string
}

const RECOMMENDED_TEMPLATE_BASE: Partial<Record<TemplateType, RecommendedTemplate>> = {
  analysis: {
    name: '长篇章节原文分析',
    description: '通读章节原文，结构化输出人物/场景/道具/事件/时间线/关系/对白/情绪/重要视觉信息。',
    content: `你是一名专业的小说解读者。请通读当前章节，输出一份结构化的原文分析，回答"这一章到底有什么"，为后续漫画剧本、分镜与资产提取提供依据。

【分析范围】
- 人物：有姓名或明确身份的全部角色，含身份、关系、当前状态。
- 场景：出现过的全部地点，含空间特征与氛围。
- 道具：推动剧情或反复出现的物品。
- 事件与时间线：按发生顺序列出本章事件。
- 人物关系：角色之间的称呼、立场、情感变化。
- 对白：关键台词与说话人。
- 情绪：主要角色的情绪变化轨迹。
- 重要视觉信息：原文明确写出的外貌、服装、环境、光线等可直接入画的信息。

【写作要求】
- 只归纳原文已经明确的信息，不要推测、补写。
- 每条信息标注出处要点（第几段/关键句），便于核对。
- 语言精炼，直接服务于后续漫画改编。

【章节原文】
{{章节原文}}`,
  },
  script: {
    name: '长篇章节漫画剧本',
    description: '基于章节原文与原文分析，按场景改编为漫画剧本。',
    content: `你是一名资深的漫画编剧。请基于章节原文与原文分析，把这一章改编成漫画剧本，回答"这一章改成漫画后要讲什么"。

【改编要求】
- 按场景组织：每个场景写明地点、出场人物、剧情推进。
- 每个场景包含：剧情概要、人物动作、情绪基调、关键对白、剧情目的（本场景为什么存在）。
- 删减不能画面化的纯心理描写与重复叙述；关键转折必须保留。
- 对白精炼口语化，符合角色身份。
- 剧情目的写清该场景在全章中的作用（铺垫/推进/转折/收束）。

【章节原文】
{{章节原文}}

【原文分析】
{{原文分析}}`,
  },
  storyboard: {
    name: '长篇章节分镜',
    description: '以漫画剧本为主输入，按「一页一张图、图内 1~4 格」拆解分镜，原文分析与章节原文作辅助核对。',
    content: `你是一名条漫分镜师。请把下面的漫画剧本拆成一页页漫画分镜。
一个分镜 = 一张竖屏漫画图 = 一页；一页内可以有 1~4 格。

【分页原则】
- 先把剧情切成"页"（一页承载一个完整节拍），再决定这一页内分几格。
- 一页 1~4 格；同一页内的若干格属于同一节拍的不同瞬间（对话来回、动作连拍、主画面+细节）。
- 全篇约 20%~30% 的页使用单格满版或 3~4 格，避免通篇都是上下双格。
- 保留呼吸页：纯画面页（无台词）或短旁白页，每章 3~6 页，用来控制节奏。

【分格原则】
- 一格 = 一个信息单元 = 一个镜头类型 + 一个情绪焦点 + 一个动作/反应；不要在一格里塞两个叙事节点。
- 一格里只放一个角色的台词；两个角色对话就拆成两格。
- 角色"看手机/电视/书/窗外，同时读者也要看到内容"时，必须拆成两格（一格角色反应 + 一格主观 POV）。
- 面对面交谈时，不要在同一页里让两个人各占一格且都露正脸。

【每一页怎么写】
- 页面标题：## 分镜 N · 单格 / 双格 / 三格 / 四格（N 从 1 递增，格数与页内实际格数一致）。
- 每一格先用一行普通文本 第X格 开始（X 从 1 递增），不要加 ###；再从下一行开始逐行写这一格的字段。
- 字段行格式：字段名：内容。一行一个字段，不要加 - 或其它列表符号。

【每格字段】（按此顺序写，没有内容的字段整行省略）
- 景别：从 远景 / 中景 / 近景 / 特写 / POV / 过肩 / 仰拍 中选一个（每格必填）。
- 镜头：机位与运镜，如 平视／俯拍／从角色侧脸下摇至小臂／前景带另一角色虚影。
- 画面：这一格画面上能看到的内容——构图、人物位置、动作、环境、关键细节，30~80 字。
- 人物：这一格画面内出现的角色名，多个用、分隔；不在画面内的说话人不写在这里。
- 出场资产：这一格画面内出现、且在【本章资产】清单中列出的资产，写为 编号 资产名（状态名），多个用、分隔。
- 动作：这一格的肢体动作，10~25 字。
- 表情：这一格的神态与情绪，10~25 字。
- 台词类字段四选一，同一格最多出现一个：

【出场资产怎么写】（系统按编号自动绑定参考图，务必严格遵守）
- 清单里每个状态前面都有一个编号（字母加数字，如 A1、B2）。每项必须写成 编号 资产名（状态名），编号照抄清单：不要自己编号、不要改写、不要漏写。
- 只能出现【本章资产】清单里有的编号、资产与状态名，一字不差；不得编造清单外的编号、资产或状态。
- 每项的状态按清单中的剧情锚点判断这一格处于哪个阶段（如 换装戏之后的格写「A2 林小雨（礼服）」），同一资产同一格只声明一个状态。
- 状态在剧情中切换后，后续所有格都跟随新状态，直到再次切换（锚点通常已写明起止）。
- 同一资产在同页不同格状态不同时（如换装页第1格便装、第2格战斗服），各格按所在格实际状态分别声明，不得统一成一个状态。
- 该格画面内没有清单资产（空镜、纯环境）时整行省略；背景板式出现但视觉特征不重要的资产也可省略。

示例（清单：- 林小雨（人物）：A1 便装（全章默认）｜A2 礼服（换装戏后））：
- 出场资产：A1 林小雨（便装）

【台词怎么写】（说话人不能省略，必须与剧本逐字一致）
- 对白行：- 台词：说话人：“台词”
- 内心独白行：- 心声：说话人：“心里想的话”（可不出现在画面里）
- 说话人不在本格画面内：- 画外：说话人：“台词”
- 无人称叙述：- 旁白：文字

【其余字段】
- 音效：本格的声音，如 沙沙——衣料摩擦。没有就不写。
- 光效：本格的光线与明暗关系，如 暖黄顶灯，背景压暗，手臂局部高光。没有就不写。
- 备注：画面上必须画出来的关键点，没有就不写。
- 不写绘画风格、颜色、人物占比、景深、分格结构、气泡位置——这些由画面描述环节按规则库处理。

【原文核对】
- 剧情推进、场景取舍、对白一律以漫画剧本为准；原文分析与章节原文只作核对，冲突时以剧本为准，不要机械按章节推断回忆、倒叙、变身等特殊状态。
- 章节原文用于补足剧本没写全的视觉细节（服饰、环境、光线、材质、道具外形等）；不要把剧本已经删减或压缩的剧情重新加回分镜。
- 资产的当前视觉状态以【本章资产】清单的锚点为准（清单先于分镜确认），剧本与清单冲突时以清单为准。

【本章资产】
{{本章资产}}

【章节原文】
{{章节原文}}

【原文分析】
{{原文分析}}

【漫画剧本】
{{漫画剧本}}`,
  },
  extract: {
    name: '长篇章节资产提取',
    description: '按人物、场景、道具建立可供生图复用的资产实体，并拆分年龄、回忆、服装、天气、损坏等视觉状态。',
    content: `你是一名负责漫画资产连续性与生图设定资料整理的资产设定师。请把本章中会影响后续画面一致性的对象整理为“一个资产实体 + 多个视觉状态”。你输出的是供后续资产绘画提示词环节使用的结构化视觉资料，不是最终绘画提示词。

【本环节边界】
- 本环节只做：人物、场景、道具的实体识别；跨状态不变特征整理；视觉状态拆分；客观视觉资料整理；剧情锚点整理。
- 本环节不做：情节概括、文学赏析、主题分析、最终绘画提示词、负面提示词、镜头语言、景别、构图、焦距、视点坐标、多视角提示词、九宫格提示词。
- 不要为了“生图级”而强行补齐原文没有的细节；原文未说明的视觉信息直接省略。只有可以由文本明确推出的空间关系才记录，不得虚构精确尺寸、坐标、材质、肤质、妆容或光源。
- 输出的“视觉描述”是客观资料，不能写成带画风、渲染器、画质和镜头指令的成品 prompt。

【输入阅读与使用规则】
- 必须完整阅读【章节原文】【原文分析】【漫画剧本】【已有资产】四部分后再提取，不得只依据其中一部分直接输出。
- 【章节原文】是一手事实来源：用于确认人物、场景、道具是否真实出现，以及外貌、服装、空间、光线、材质、损坏等明确视觉细节；原文没有的信息不得编造。
- 【漫画剧本】是本章实际漫画化范围：用于确认哪些原文内容被保留、删减或合并，以及资产在成片中的出场和状态切换；原文有但剧本明确删掉、且不再影响本章画面的对象，通常不提取。
- 【原文分析】只作为人物关系、别名、时间线、回忆/闪回和重要物件的索引与查漏依据；必须回到原文或剧本核实，不得用分析中的推测覆盖一手内容。
- 【已有资产】是连续性基准：先按名称、别名、称谓和身份判断是否为同一对象；同一对象沿用已有资产名，不重复建档；外观未变化时逐字沿用已有状态名，出现明确视觉变化时在原资产下新增状态。
- 信息冲突时按以下优先级处理：漫画是否采用及出场范围以【漫画剧本】为准；客观视觉细节以【章节原文】为准；实体归属和既有状态命名以【已有资产】为准；【原文分析】仅辅助，不覆盖前三者。
- 某部分显示为空或“尚未生成”时，使用其余有效输入继续提取，不得因缺少辅助输入而虚构内容。

【整体提取规则】
- 提取目标是“会在画面中被识别、会影响后续生图一致性、或承载关键剧情”的人物、场景、道具；只出现一次且没有辨识价值的普通路人、泛化地点和日常物件不提取。
- 一个真实对象只建立一个资产实体。姓名、昵称、称谓、代号、旧名、身份称呼和代词指向同一对象时合并，其他称呼写入“别名”；不同对象即使外观相似也必须分开。
- 资产实体记录跨状态相对稳定的视觉身份；视觉状态记录该实体在特定年龄、时间线、服装、身体、环境或形态下会影响生图的外观。
- 状态拆分的判断标准是“如果不单独提供该状态，生图是否可能把对象画错”。会改变轮廓、年龄感、脸部、发型、服装、配饰、颜色、材质、空间结构、天气光线或物体形态的变化必须拆分；只改变情绪、动作、台词、镜头或短暂姿态的变化不拆分。
- 状态名称要描述“时间/阶段 + 主要视觉差异”，例如“少年时期·回忆校服”“中年时期·军装”“雨夜·战后广场”“损坏·裂纹怀表”；不要用“第一个镜头”“萧炎失意”等只描述剧情的名称。
- 每个视觉状态必须能单独生成一张参考设定图：视觉描述自包含，不依赖“同上”“沿用前态”“见默认状态”；同时写清状态开始和结束的剧情锚点。

【人物提取规则】
- 资产描述提取跨年龄、跨服装仍能辨认人物的视觉基因：体型轮廓、脸型、眉眼、瞳色、肤色、发型发色与分缝、固定疤痕/痣/纹身、长期配饰、相对身高和稳定主色。
- 视觉状态提取会改变生图结果的人生阶段和外观：少年/青年/成年/中年/老年、回忆/闪回/未来时期、换装、制服/礼服/战斗服、身份伪装、面具、明显伤势、长期新增配饰、变身或特殊形态。
- 服装状态要写完整可见层次：外层、内层、下装、鞋袜、帽子/面具/披风和临时配饰；原文没有写明的部分省略。
- 短暂表情、握拳、弯腰、行走、流泪、看向某人、孤独站立、说话和情绪变化属于分镜表现，不单独建视觉状态。

【场景提取规则】
- 资产描述提取可跨时段复用的空间骨架：室内/室外/半开放、空间形状、纵深、出入口、固定大型物件、地面/墙面/主要材质和稳定地标。
- 视觉状态提取会改变整体画面识别的时间与环境：白天/夜晚/黄昏、季节、雨雪雾、节庆布置、战前/战后、正常/毁坏、过去/未来建筑、停电/火光等。
- 只记录文本明确或可直接确认的相对位置，例如“魔石碑位于广场中央”；不虚构房间尺寸、坐标、米数、建筑平面图或固定光源位置。
- 人群情绪、人物动作、议论声、镜头方向和构图属于分镜内容，不写进场景视觉状态；除非人群或布置本身是稳定且反复出现的场景组成部分。

【道具提取规则】
- 资产描述提取道具的稳定物理身份：轮廓、相对手掌/身体/环境的大小、材质、颜色、反光、固定纹样、刻字、缺口和通常持有者。
- 视觉状态提取会改变道具形态或生图结果的状态：闭合/展开、完好/损坏、未激活/激活、普通/发光、使用前/使用后、修复前/修复后、升级或变形阶段。
- 一次性显示的台词、数值、测验结果和短暂光效，默认属于分镜画面信息，不为每个文字结果单独建立道具状态；只有不同结果造成稳定且明确的外观差异时才拆分。
- 道具用途、持有者和剧情作用可以作为属性或原文依据，但不要把象征意义直接写成物理特征。

【视觉记忆点规则】
- 记忆点必须是能帮助模型稳定画出对象的具体视觉特征：轮廓、发型、脸部标记、服装色块、独特配饰、材质、纹样、缺口、空间地标等。
- 不要把“勇敢、失意、温柔、神秘、被众人嘲讽”等抽象性格、剧情评价或情绪词当作视觉记忆点。
- 有充分文本依据时，每个状态整理 3～5 个记忆点；依据不足时少写，禁止为了凑数量脑补。

【内部工作顺序，不要输出】
1. 按上述职责交叉阅读四部分输入，盘点人物、场景、道具。
2. 合并同一对象的姓名、称谓、代号、旧名和代词，只保留一个资产实体。
3. 扫描年龄/时间线、回忆闪回、服装、身份、身体、变身、天气、光照、损坏和使用形态变化。
4. 凡是会让生图中的轮廓、脸部、服装、颜色、材质、空间结构或整体光线明显不同的变化，都拆为独立视觉状态。

【提取范围】
- 人物：有姓名或明确身份、持续出现、推动剧情，或需要稳定画面形象的角色；忽略无辨识价值的路人和群众。
- 场景：承载关键事件、会重复使用，或有明确空间骨架/识别地标的地点；忽略“路上”“房间里”等泛化地点。
- 道具：推动剧情、反复出现、有独特轮廓/材质/标记，或会成为画面重点的物品；忽略普通无重点的日常物件。

【状态拆分规则】
- 一个对象只能有一个资产实体；年龄、时间线、服装、身份伪装、身体状态、变身不是新资产，而是视觉状态。
- 按上述人物、场景、道具规则拆分视觉状态；“回忆少年时期”必须成为独立状态，例如“少年时期·回忆校服”，不能只写在剧情锚点中。
- 正面/侧面/背面、景别、构图、短暂表情、普通动作、一次性文字变化不拆分。

【资产描述】
每项资产用一行“描述”，只写跨状态不变的视觉记忆点，原文没写明就省略：人物写体型、发型发色与分缝、脸型眉眼瞳色肤色、固定疤痕/痣/纹身及左右位置、长期配饰、最多 5 个具体主色；场景写空间骨架、出入口、固定大型物件、墙地材质；道具写轮廓、相对尺寸、材质反光、固定纹样/刻字/缺口和通常持有者。

【视觉资料的详细程度】
- 人物按原文可确认的维度整理：体型与体态、脸型与五官、发型与发色、肤色与皮肤可见特征、服装层次与材质、配饰与标志物、妆容或明显污损。没有依据的维度省略，不得为了填满维度而脑补。
- 场景整理：室内/室外/半开放、空间边界、出入口、固定大型物件、物体相对位置、地面/墙面/主要材质、明确的时段天气和光源方向。只写文本中明确或直接可确认的相对关系，不要求坐标、米数或十个视角。
- 道具整理：形状轮廓、材质、颜色、反光、相对大小、纹样/刻字/磨损/缺口、明确的开合/损坏/激活形态和用途。
- 抽象性格、象征意义、情绪氛围、台词内容和人物关系不写进视觉描述；有需要时放入身份、原文依据或状态锚点。

【视觉状态输出】
每个状态必须使用以下结构；状态块内只能使用“状态标签、视觉描述、剧情锚点”三个字段：
### 视觉状态：状态名
- 状态标签：状态类型、记忆点1、记忆点2、记忆点3
- 视觉描述：必须自包含、可直接供生图。人物写年龄感、发型、面部可见特征、外层到内层的完整服装、下装、鞋袜、临时配饰和身体变化；场景写固定结构、天气/时段/布置、光源方向、色温和强度；道具写当前形态、开合/损坏/激活状态、材质和独有细节。末尾必须写“记忆点：……”和“与其他状态差异：……”。
- 剧情锚点：从哪个事件开始生效，到哪个事件结束；初始状态写“全章默认”；回忆、闪回、梦境和未来片段写清时间线边界。

状态名建议使用“时间/年龄阶段·主要外观”，如“少年时期·回忆校服”“中年时期·黑色军装”“雨夜·战后候车厅”“损坏·裂纹表盘”。视觉描述不得使用“同上”“沿用前述”“见默认状态”；颜色写具体色名，位置写左右；不要写画风、镜头语言、构图或主观评价。

【资产复用与连续性规则】
- 已有资产且本章外观未变化：状态名逐字沿用已有状态名。
- 已有资产但有明确视觉变化：沿用资产名，新增状态，不新建同名资产。
- 不要把状态字段写成资产属性；只在原文有依据时补充人物身份/阵营/职业/年龄阶段、场景地点类型/时代、道具持有者/用途。

【自查】
- 是否只提取了影响画面一致性的实体，是否过滤了无辨识价值的路人、泛化地点和普通物件。
- 是否把同一对象的不同称呼合并，同时避免把不同对象错误合并。
- 是否把真正影响生图的年龄、服装、时间线、环境和形态变化拆成状态，并把短暂表情、动作、台词、数字显示留给分镜。
- 每个资产至少有一个状态；有足够原文依据时，为每个状态整理 3～5 个具体视觉记忆点；依据不足时宁可少写，不得脑补。
- 每个状态的视觉描述是否能独立用于后续提示词生成，剧情锚点是否明确状态起止。
- 只基于输入明确内容，不编造。

【章节原文】
{{章节原文}}

【原文分析】
{{原文分析}}

【漫画剧本】
{{漫画剧本}}

【已有资产】
{{已有资产}}`,
  },
  'asset-prompt': {
    name: '资产绘画提示词',
    description: '按状态清单为每个资产视觉状态生成设定图规格的绘画提示词，画风由生图时的共用属性拼接。',
    content: `你是一名资深的漫画绘画提示词工程师。请根据状态清单，为每一个视觉状态撰写一段可直接用于生图的中文绘画提示词。画风、渲染规格与画质由系统在生图时另行拼接，提示词里不要写。

【目标生图模型】
{{目标生图模型}}

【待生成状态清单】
{{状态清单}}

每条清单包含：资产名、类型、状态名、资产描述（跨状态不变的身份特征）、视觉描述（该状态的完整外观与差异点）、固定特征与补充属性。

【写作要求】
- 每个状态输出一段完整连贯的提示词，不要分点，不要解释。
- 以「资产描述」为身份基准、「视觉描述」为该状态的外观基准，两者都必须完整体现、不得改变或遗漏；清单没写的特征不要编造。
- 按资产类型组织：
  - 人物：标准九头身设定图。写明体型比例、发型发色与分缝、面部特征、妆面、本状态的完整着装（外层到内层、下装、鞋袜逐层，含材质、配色、边缘与装饰细节）、专属标记与长期配饰。要求角色完整全身出镜，精准体现发型与服装的全部细节。
  - 场景：完整空间展示。写明空间结构与纵深、出入口、固定大型物件及相对位置、地面材质，以及该状态的光线（光源方向、色温、强度）。
  - 道具：单件完整展示。写明轮廓、相对手掌或身高的尺寸参照、材质与反光、独有细节（磨损、刻字、缺口），以及该状态下的形态。
- 同一资产的不同状态：身份特征保持完全一致，只在该状态的差异点上变化。
- 不写画风、渲染器、画质、镜头语言与构图指令，这些由生图配置拼接。`,
  },
  'panel-prompt': {
    name: '分镜画面描述',
    description: '按分镜内容逐镜生成纯画面描述，参考图与共用属性在生图前动态拼接。',
    content: `你是一名专业的漫画分镜画面描述师。请根据下面的分镜信息，只写这一镜的纯画面内容。参考图、图号、共用属性与画风由系统在生图前动态拼接，你不要输出这些内容。

【目标生图模型】
{{目标生图模型}}

【本章分镜概要】
{{本章分镜概要}}

【前文分镜与画面】
{{前文分镜}}

【当前分镜】
{{当前分镜}}

【本镜镜头】
{{镜头}}

【写作要求】
- 每格先写一行景别与镜头，再写画面内容：人物位置与动作、表情、场景环境、光线氛围，按本镜的镜头类型组织画面；
- 与前文分镜保持画面连续：人物位置关系、服装状态、光线、场景细节；
- 严格基于给定信息撰写，不要补写原文没有的细节；不写对白与旁白，只描述画面本身；
- 不要输出参考图清单、图号、共用属性或全局画风。`,
  },
  'panel-prompt-chapter': {
    name: '分镜画面描述（整章一次生成）',
    description: '一次把本章全部分镜交给模型，按 ## 分镜 N 标题分段输出各镜画面描述。',
    content: `你是一名专业的漫画分镜画面描述师。下面给出本章的全部分镜，请为每一镜各写出一段纯画面内容。参考图、图号、共用属性与画风由系统在生图前动态拼接，你不要输出这些内容。

【目标生图模型】
{{目标生图模型}}

【本章全部分镜】
{{全章分镜}}

【写作要求】
- 逐镜输出：每一镜各一段，用 ## 分镜 N 标题行开头，N 必须与上面给定的分镜序号完全一致，按序号递增，不遗漏、不新增、不打乱顺序；
- 每格先写一行景别与镜头，再写画面内容：人物位置与动作、表情、场景环境、光线氛围，按该镜的镜头类型组织画面；
- 各镜之间保持画面连续：人物位置关系、服装状态、光线、场景细节；
- 严格基于给定信息撰写，不要补写原文没有的细节；不写对白与旁白，只描述画面本身；
- 不要输出参考图清单、图号、共用属性或全局画风。`,
  },
}

/**
 * 推荐模板 = 模板正文 + 该类型的结果结构说明（`withFormatSpec`）。
 *
 * 默认模板**自带返回格式约定**，所以运行时不需要再注入协议段；用户改模板正文时就顺便改了格式要求，
 * 设置页对「结果会被解析」的环节给一行提示，避免改坏解析后无从察觉。
 */
export const RECOMMENDED_TEMPLATES: Partial<Record<TemplateType, RecommendedTemplate>> = Object.fromEntries(
  (Object.entries(RECOMMENDED_TEMPLATE_BASE) as Array<[TemplateType, RecommendedTemplate]>).map(([type, template]) => [
    type,
    { ...template, content: withFormatSpec(template.content, type) },
  ]),
) as Partial<Record<TemplateType, RecommendedTemplate>>

/**
 * 无用户模板时的内置模板内容：与推荐模板同源。
 * 去掉了自动追加兜底之后，内置模板必须自己写全该类型的全部变量，否则等于空提示词。
 */
export function defaultTemplateContent(type: TemplateType): string {
  return RECOMMENDED_TEMPLATES[type]?.content ?? ''
}

// ========== 归一化与检测 ==========

function escapeRegExp(value: string): string {
  return value.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
}

function variablePattern(name: string, flags = ''): RegExp {
  return new RegExp(`\\{\\{\\s*${escapeRegExp(name)}\\s*\\}\\}`, flags)
}

/** 旧英文占位符归一化为中文占位符（兼容存量模板）。 */
export function normalizeTemplateVariables(content: string, type: TemplateType): string {
  let text = content
  for (const spec of getTemplateVariables(type)) {
    for (const alias of spec.legacy ?? []) {
      text = text.replace(variablePattern(alias, 'g'), `{{${spec.name}}}`)
    }
  }
  return text
}

/** 检测模板中不适配当前类型的占位符（渲染时会被清理；设置页红色警告）。入参应为归一化后的内容。 */
export function findUnknownVariables(content: string, type: TemplateType): string[] {
  const known = new Set(getTemplateVariables(type).map((spec) => spec.name))
  const found = new Set<string>()
  for (const match of content.matchAll(/\{\{\s*([^{}\s][^{}]*?)\s*\}\}/g)) {
    if (!known.has(match[1])) found.add(match[1])
  }
  return [...found]
}

/** 存量模板一次性迁移：返回归一化后的内容；无需迁移（无旧占位符）时返回 null。 */
export function migrateTemplateContent(content: string, type: TemplateType): string | null {
  const normalized = normalizeTemplateVariables(content, type)
  return normalized === content ? null : normalized
}

// ========== 统一渲染引擎 ==========

/**
 * 渲染最终提示词（全链路唯一规则）：
 * 1. 归一化：旧英文占位符 → 中文占位符；
 * 2. 只替换「模板里写了的」变量（值为空替换为 emptyText）；
 *    模板没写的变量一律不出现，绝不自动追加——插入哪些变量完全由模板作者决定；
 * 3. 清理未注册的 {{...}} 占位符（防误插变量污染提示词）。
 *
 * 不再附加任何协议段：返回格式等约定已写在模板内容里（见 OUTPUT_FORMAT_SPECS / RECOMMENDED_TEMPLATES）。
 */
export function renderPromptTemplate(options: {
  type: TemplateType
  content: string
  values: Record<string, string | undefined>
}): string {
  const specs = getTemplateVariables(options.type)
  let text = normalizeTemplateVariables(options.content, options.type)
  for (const spec of specs) {
    if (!variablePattern(spec.name).test(text)) continue
    const raw = options.values[spec.name] ?? ''
    const value = raw.trim() ? raw : (spec.emptyText ?? '')
    // 函数式替换：避免注入内容中的 $&、$1 等被当作 replace 特殊序列解析
    text = text.replace(variablePattern(spec.name, 'g'), () => value)
  }
  text = text.replace(/\{\{[^{}]*\}\}/g, '')
  return text
}
