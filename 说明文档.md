# 说明文档

> 项目全生命周期管理载体：项目规划 / 实施方案 / 进度记录。每次打开项目前先阅读本文档确认当前进度。

## 一、项目规划

### 1.1 项目定位

面向公众号排版与长篇漫画创作的桌面应用（Electron + Vue 3 + TypeScript + Pinia + Tailwind CSS）。

### 1.2 长篇漫画 AI 管线（本次改造目标）

将长篇故事创作管线改造为清晰的四环节 AI 管线（**2026-09-18 起：资产提取移到分镜之前**，分镜生成时注入本章资产清单）：

```text
原小说 → 【原文分析 AI】 → 【剧本生成 AI】 → 【资产提取 AI】 → 【分镜生成 AI】 → 绘画 Prompt → 生图
```

| 产物   | 主要输入                  | 解决的问题            |
| ---- | --------------------- | ---------------- |
| 原文分析 | 原小说 + 分析规则            | 小说里有什么           |
| 漫画剧本 | 原小说 + 原文分析 + 剧本规则     | 这一章怎么改编成漫画剧情     |
| 资产   | 原文分析 + 剧本 + 已有资产 + 提取规则 | 哪些人物/场景/道具需要固定一致、各有哪些视觉状态 |
| 分镜   | 剧本（主）+ 本章资产清单 + 原文分析（辅）+ 分镜规则 | 每一段剧情具体怎么拍、怎么画、每格出场哪些资产（含视觉状态） |

**资产 → 分镜的视觉状态绑定通道**：分镜模板 `{{本章资产}}` 变量每行 `资产名（人物/场景/道具）：A1 状态A（一句话）｜A2 状态B（…）`——**编号由系统按本章清单现算**（`buildVariantCodeMap`：资产按清单次序取字母、状态按资产内次序取数字）；模型逐格声明「出场资产：编号 资产名(状态名)」；解析器先按编号直接查表得到资产/状态 id，再与名字通道互校（**名字优先 → 编号救场 → 冲突以名字为准**），格级声明汇总到页级 `assetBindings`（matchSource: model）。auto-text 回填通道全保留（旧章兼容/资产后补/模型漏写双保险）。

### 1.3 页面结构

- **主页面** `/comic/long-project/:projectId`：四个顶级 tab「原文｜剧本｜资产｜分镜」（按钮 tab 样式，tab 行右侧为当前页签的操作按钮区）。原文 tab 左编辑右分析，剧本 tab 左原文只读右剧本编辑；资产 tab = LongProjectAssetTab（提取/审核/生图工作台，内部三子视图「信息｜图片｜生图工作台」，操作按钮注入子 tab 行右侧）；分镜 tab = 分镜生图工作台（操作按钮经 Teleport 注入 tab 行右侧 `#storyboard-actions` 容器，含「分镜/绘图」内容阶段下拉）；底部 PromptRunBar（模型/模板/发送前确认）。

- ~~生图工作台 `/comic/panel-gen/:projectId/:chapterId`~~：已并入主页面「分镜」tab（2026-09-04），路由与 `LongProjectPanelGen.vue` 已删除。

### 1.4 关键决策

1. 分镜生成以剧本为主输入 + 本章资产清单（`{{本章资产}}`）+ 原文分析辅助；无剧本时原文兜底 + toast 提示；清单每项状态带系统现算的**编号**（`A1`），模型逐格声明「出场资产」（`编号 资产名(状态名)`，照抄清单）；解析器按编号直接查表得 id，并与名字通道互校（名字优先 → 编号救场 → 冲突以名字为准），汇总格级声明到页级绑定（matchSource: model；名字兜底走 精确 → includes 模糊 → 章节默认 三级匹配）。
2. 资产提取输入 = 原文 + 分析 + 剧本 + 已有资产（状态名+视觉描述+剧情锚点）；候选出现次数统计——有分镜按分镜计数（旧顺序章节），无分镜（新管线）按剧本文本命中计数；确认后按文本自动回填分镜绑定（auto-text，双保险通道保留）。
3. 主页面移除分镜表格；合并/拆分等结构操作迁移到分镜 tab 左栏右键菜单（8 秒可撤销，成图归属随迁）。
4. 持久化统一走 useLongProjectPersistence（串行队列 + 队列内 read-modify-write）；分镜工作台并入主页面后共享同一持久化实例，跨页覆盖问题彻底消除。
5. 分镜/资产 tab 均为首次进入才挂载、之后常驻（v-if + v-show）：批量推导/生图/提取等长任务切换页签不中断；原文/剧本 tab 同理改 v-show 保持编辑状态。
6. 手动状态锚点（matchSource: manual）：分镜资产绑定卡可直接切换视觉状态；锚点之后 auto-text 来源的绑定自动跟随重算（延续链：锚点 → … → 遇到 model/manual/chapter-range 绑定则改以其为新起点）。
7. 兼容：LONG_CHAPTER_STAGE_ORDER 只升不降（assets-ready 移至 storyboard-ready 前，旧章 stage 不重排）；存量自定义分镜模板没写 `{{本章资产}}` 时自动退化为现状；extract 模板的「分镜概要」变量已删除（新管线资产先于分镜，panel-prompt 的「本章分镜概要」不受影响）。
8. 格级状态绑定精度（2026-09-18）：格级 `cell.assetBindings` 声明成为状态精度的补充事实源——生图参考图与画面描述按「页级 ∪ 格级」展开（resolvePanelAssetStates），同资产镜内多状态各成条目（各带一张参考图，单选口径不变）；页级绑定保持每资产一条 = 资产清单 + **镜末状态**（summarizeCellBindings 同资产多格取最后一格，延续链起点）；绑定卡显示各格状态差异明细；旧数据（无格级声明）完全退化为页级行为，零迁移。

## 二、实施方案（已落地文件清单）

### 2.1 新建（14）

| 层  | 文件                                            | 职责                               |
| -- | --------------------------------------------- | -------------------------------- |
| 服务 | `services/chapterDocService.ts`               | 原文分析/剧本提示词组装与执行 + 推荐模板           |
| 服务 | `services/assetExtractionConfirm.ts`          | 提取确认数据变换 + 分镜绑定回填（纯函数）           |
| 组合 | `composables/useLongProjectPersistence.ts`    | 串行队列 + read-modify-write 持久化     |
| 组合 | `composables/useChapterDocRun.ts`             | 分析/剧本生成执行与持久化 + stage 推进         |
| 组合 | `composables/useStoryboardRun.ts`             | 分镜生成（剧本主输入）+ 异常恢复                |
| 组合 | `composables/useStoryboardOps.ts`             | 合并/拆分/复制/撤销 + panelArtworks 归属迁移 |
| 工具 | `utils/markdown.ts`                           | Markdown 渲染纯函数                   |
| 组件 | `components/common/MarkdownView.vue`          | 只读 Markdown 渲染                   |
| 组件 | `components/common/PromptPreviewDialog.vue`   | 发送前确认弹窗（可编辑最终提示词）                |
| 组件 | `components/common/PromptRunBar.vue`          | AI 任务执行底栏（模型/模板/确认/执行）           |
| 组件 | `components/LongProjectSourceTab.vue`         | 原文 tab（左编辑右分析）                   |
| 组件 | `components/LongProjectScriptTab.vue`         | 剧本 tab（左原文右剧本 + 进入分镜入口）        |
| 组件 | `components/LongProjectStoryboardTab.vue`     | 分镜 tab（2026-09-04 新增，承载原独立生图工作台全部逻辑，Teleport 注入顶栏操作按钮） |
| 组件 | `components/panel-gen/PanelContentEditor.vue` | 右栏「分镜内容」：生成分镜 + 逐镜编辑             |
| 组件 | `components/panel-gen/PanelGenAssetTab.vue`   | 资产 tab：提取入口 + 审核 + 资产生图工作台       |

### 2.2 修改（7）

- `types/index.ts`：TemplateType 增加 `analysis|script`；新增 `LongProjectChapterDoc`；候选增加 `panelAppearances`

- `services/storyboardService.ts`：分镜提示词改为 `{{script_content}}/{{analysis}}` 变量；template 可选（prompt 直传）

- `services/assetExtractionService.ts`：提取提示词追加分析/剧本/分镜概要；`countCandidatesAppearances` 按分镜计数

- `views/LongProject.vue`：双 tab 改造（\~1350 → 415 行）；2026-09-04 扩为三 tab（原文/剧本/分镜）

- ~~`views/LongProjectPanelGen.vue`~~：已删除，逻辑迁入 `LongProjectStoryboardTab.vue`；路由 `panel-gen` 同步移除

- `components/panel-gen/PanelListSidebar.vue`：去 hover 按钮，加 contextmenu

- `features/settings/components/PromptTemplateSettings.vue`：新增原文分析/漫画剧本模板类型

### 2.3 复用不动

LongProjectAssetExtractionReview、StoryboardContextMenu/MergeDialog/SplitDialog、LongProjectAssetTab、PanelPromptPanel、PanelPreview、PanelAssetTabs。

## 三、进度记录

| 日期         | 任务                      | 状态 | 结果说明                                                                                       |
| ---------- | ----------------------- | -- | ------------------------------------------------------------------------------------------ |
| 2026-09-03 | 阶段一：类型层 + 设置页 + 公共基础件   | ✅  | 新类型/模板设置/MarkdownView/PromptPreviewDialog/PromptRunBar/useLongProjectPersistence 完成        |
| 2026-09-03 | 阶段二：服务层新管线              | ✅  | chapterDocService/storyboardService/assetExtractionService/assetExtractionConfirm 完成，旧签名兼容 |
| 2026-09-03 | 阶段三：主页面改造               | ✅  | LongProject.vue 双 tab（原文/剧本），行数 1350→415，legacy `?tab=storyboard` 映射 script                |
| 2026-09-03 | 阶段四：生图页面改造              | ✅  | 页面级 分镜/资产 tab；右栏 分镜内容/提示词 切换；左栏右键合并/拆分/复制/撤销；持久化换 composable                               |
| 2026-09-03 | 阶段五：死代码清理 + 说明文档 + 全量回归 | ✅  | 无死代码残留（noUnusedLocals 通过）；`npx vue-tsc --noEmit` 全量通过；说明文档建立                               |
| 2026-09-04 | 分镜生图工作台并入主页面「分镜」tab | ✅  | 新增 LongProjectStoryboardTab（Teleport 注入顶栏按钮 + 资产切换按钮）；删除 panel-gen 路由与旧页面；vue-tsc + vitest 全量通过 |
| 2026-09-09 | 长篇工作台三项改造 | ✅  | ①新建章节可选「从原文/从剧本开始」（剧本起稿隐藏原文 tab，剧本可直接手写，落库 handwritten）；②资产升为顶级 tab（原文/剧本/分镜右侧，分镜 tab 瘦身为纯分镜）；③资产提取底稿兜底（无原文时以剧本兜底 + 说明头 + 顶栏提示）、阶段顺序统一 LONG_CHAPTER_STAGE_ORDER（assets-ready 移至 storyboard-ready 后）、无分镜提示条、设置页输出协议提示按类型区分；vue-tsc 全量通过 |
| 2026-09-18 | 资产视觉状态→分镜绑定全面改造（阶段A/B/C 一次到位） | ✅  | ①管线重排：原文→剧本→**资产**→分镜，assets-ready 移到 storyboard-ready 前，资产顶级 tab 移到分镜左侧；②提示词体系：新注册 `{{本章资产}}` 变量（renderChapterAssetsText 含剧情锚点），分镜格式加「出场资产」字段（14 字段），extract 模板加「剧情锚点」必写项、删{{分镜概要}}（新管线无分镜时提取），两套推荐模板与默认提示词同步；③绑定双通道+手动通道：model 通道（分镜逐格声明出场资产→pushCellAssets 格级→summarizeCellBindings 页级→bindingsFromValue 三级状态匹配）+ auto-text 双保险 + manual 通道（PanelAssetTabs 状态 pill 手选，matchSource:'manual'）；④手动锚点延续：reapplyVariantContinuation 纯函数（其后 auto 绑定跟随锚点，model/manual 不动）；⑤savePanelEdit 合流（manual > 格级声明 > auto-text 重算）；anchor 贯穿 state→variant→提示词→延续重算；存量模板缺 {{本章资产}} 自动退化；vue-tsc + vitest 208/208 通过 |
| 2026-09-18 | 删除 extract 模板「分镜概要」变量（用户确认） | ✅  | 变量注册/panelsOutline 字段/buildPanelsOutline 函数/两处调用/推荐模板残留/相关注释全链删除；panelCastNames 保留（出现次数统计在用）；存量自定义模板里的 `{{分镜概要}}` 渲染时按未注册占位符清理；panel-prompt 的 `{{本章分镜概要}}`（画面描述推导）不受影响；vue-tsc 零错误 + vitest 207/207 通过 |
| 2026-09-18 | 格级资产状态精确绑定（镜内多状态支持） | ✅  | ①panelPromptService 新增 resolveCellBindings（格级声明按格解析）+ resolvePanelAssetStates（页级∪格级展开：同资产多状态各成条目带 cellIndexes，页级主状态附 binding）；buildPanelAssetsContext 多状态按格分行标注；②storyboardService summarizeCellBindings 同资产多格取最后一格（镜末状态=延续链起点）；formatCellsForPrompt 每格补出场资产行；③currentRefGroups/panelRefImages 改用格级并集（同资产多状态各带一张参考图）；④PanelAssetTabs 绑定卡显示各格状态差异明细（镜末高亮）；⑤出场资产格式规则与推荐模板补「跨格状态分开声明」；⑥新增 panelAssetStates.test.ts（12 测试）；vue-tsc 零错误 + vitest 219/219 通过 |
| 2026-09-18 | 画面描述参考图清单 + 整章一次生成 | ✅  | ①**参考图清单（唯一图号来源）**：新建 `services/panelRefManifest.ts`，编号顺序固定为「插入最前的共用属性图 → 人物 → 场景 → 道具」，**不做截断**；分镜页取图 `panelRefImages`、右栏分组 `currentRefGroups`、提示词、资产引用统计 `buildAssetUsageIndex` 四处全部改读同一份清单（纯函数实时算，改图/换状态立刻反映，无需刷新按钮）；②**「插入最后」的共用属性去掉参考图能力**：`getSharedRefImages`/`computeBlockImageNumbers` 只算 front 组，卡片隐藏开关与图区并提示，图片数据保留（切回 front 即恢复）；③**停用「按图号覆盖描述」**：删除 `refreshBlockDescriptionWithNumbers`，图号行改由 `buildBlockText` 在拼装时实时生成（`属性名` + `图N、图M：属性名。` + 用户描述正文，三者互不覆盖）；④**新增模板类型 `panel-prompt-chapter`（整章一次生成）**：一次调用产出全章描述，按【分镜N】分段，`parseChapterPanelPrompts` 容错对位（标记 / 顺序兜底 / 漏段提示），弹窗加「生成方式：逐镜依次 / 整章一次」二选一；变量与逐镜不同（全章分镜 / 全章资产设定 / 全章参考图清单，去掉镜头 / 前文分镜 / 本章分镜概要）；⑤**移除 `{{风格上下文}}`**，新增 `{{前置共用属性}}` / `{{参考图清单}}` / `{{后置共用属性}}`；⑥**三层拼接** `composeFinalPrompt`（前置共用属性 + 画面描述 + 后置共用属性）只作用于生图 prompt，`imagePrompt` 字段保持纯净；⑦右栏新增图号速览与「复制」完整提示词（可贴外部 AI），工具栏新增「导入描述」（按【分镜N】对位写入）；⑧新增 panelRefManifest.test.ts（21 用例）；vue-tsc 零错误 + vitest 257/257 通过 |
| 2026-09-18 | 画面描述 v2：共用属性退出推导输入 + 模板改为用户自建 | ✅  | ①**共用属性不再进推导提示词**：`panel-prompt` / `panel-prompt-chapter` 两个类型删除 `{{前置共用属性}}` / `{{后置共用属性}}` 变量与推荐模板里的对应段落，只由 `composeFinalPrompt` 在生图时前后拼接（此前模型会把画风抄进描述、生图再拼一遍 → 重复；且描述被画风污染后换画风必须重跑）；②**图号「只列资产图、号不变」**：`buildRefManifestText(manifest, { assetsOnly: true })` 只列资产条目并附一行「图N 为前置共用属性参考图，系统另行拼接，无需引用」，图号仍沿用生图真实序号（共用属性图先占号）；③**去掉内置默认模板**：删 `DEFAULT_PANEL_PROMPT_TEMPLATE` / `DEFAULT_CHAPTER_PANEL_PROMPT_TEMPLATE`，无模板不再兜底（返回 null + 提示）；④**弹窗改「用户自建」**：去掉「内置默认模板」选项、模板必选（单镜模式允许直接手写 prompt），列表为空时给「新建模板（填入推荐内容）」一键落库该类型推荐模板，分镜页 emit `templates-changed` → `LongProject.vue` 重载模板列表；⑤**输出要求补两条**：必须用清单图号声明参考图用途、**不要**写全局画风与共用属性内容（系统生成后拼接）；⑥两个推荐模板按 v2 重写；新增 5 个用例；vue-tsc 零错误 + vitest 262/262 通过 |
| 2026-09-21 | 资产视觉状态绑定：状态编号通道 | ✅  | ①**问题**：分镜「出场资产」是模型另写一遍的字符串，与资产库状态名措辞差一点（「少年」vs「少年期」）就断绑，且 `findAsset` 要求唯一精确命中、`defaultVariant` 在同章多状态时按设计返回 undefined，累积成大面积漏绑；②**编号通道**：新增纯函数 `buildVariantCodeMap`（本章资产 → `A1/A2/B1`，资产按清单次序取字母、状态按资产内取数字、无状态资产不占位），`renderChapterAssetsText` 清单改为 `编号 状态名（锚点）`，`bindingsFromValue`/`parseStoryboardResponse` 加可选 `variantCodes` 参数；③**判定口径**：名字优先 → 编号救场 → 冲突以名字为准（编号依赖当次清单会漂移，采用它反而会引入新错绑；此规则保证不劣化现状）；④**回归防护**：`splitVariantCode` 无条件剥离编号前缀（编辑器 `parsePanelBlock` 无资产上下文，不剥离会让 `A1 林小雨` 整体成为 assetName 导致保存后绑定失效），无编号表时以「剩余部分为中文」为安全闸，不误伤 `R2D2` 这类资产名；⑤**协议同步**：`OUTPUT_FORMAT_SPECS.storyboard` + 推荐模板「出场资产」两处改为 `编号 资产名（状态名）` 并补示例；⑥**调用点**：分镜生成 / 手动导入 / 导入预览三处补传编号表；⑦文档同步（格式定稿 2.2 示例、2.3 字段表补出场资产、新增 2.3.1 编号机制、4.4 协议）；⑧新增 10 用例；vue-tsc 零错误 + vitest 300/300 通过。**存量模板需在设置页重新「填入推荐模板」才启用编号，旧模板行为不变** |
| 2026-09-21 | 修复：分镜内容编辑器保存后丢资产状态 | ✅  | ①**现象**：导入分镜后中栏资产正常，但在右栏「分镜内容」输入框编辑一次并失焦保存，`出场资产：测验魔石碑（萧炎测验·三段显示）` 被改写为 `出场资产：测验魔石碑`，同章多状态资产随之丢状态、取图清单与中栏里该资产消失（看起来像「导入没绑定成功」）；②**根因**：`parsePanelBlock` 不带资产上下文（空 assets / 空 chapterId）→ `bindingsFromValue` 在资产未命中时把 `visualVersionName` 置空 → `serializeBindings` 只能写回资产名 → 保存重解析只能靠 `defaultVariant()`，而它在同章多状态且无唯一默认时返回 `undefined`；③**修复**：`bindingsFromValue` 资产整体未命中时**保留文本原样的状态名**（资产命中时口径不变，仍只保存实际匹配到的状态名，保证「显示状态」与「发送图片」一致）；④编号不参与持久化，编辑器保存后 `A1` 前缀被剥掉属预期行为；⑤新增 `tests/modules/panelEditRoundTrip.test.ts`（7 用例，锁死「文本写了什么往返后还是什么」）；vue-tsc 零错误 + vitest 313/313 通过 |

### 待办 / 后续手测清单

- [ ] 原文录入 → 分析（发送前确认改 prompt）→ 编辑分析 → 原文改动后「原文已变更」徽标

- [ ] 剧本生成 → 编辑 → 点击「进入资产」切到资产页签

- [ ] 资产页签（顶级 tab，分镜左侧）：提取（候选含剧情锚点、无分镜时按剧本行统计）→ 改候选重算次数 → 确认（状态带锚点入库）→ 资产生图工作台

- [ ] 分镜页签：生成分镜（提示词含【本章资产】清单，逐格「出场资产」声明→绑定回填；无剧本兜底）→ 右键合并（成图归属）/拆分/复制/撤销 → 分镜内容逐镜编辑标 stale（出场资产行增删即声明增删；manual 绑定不被文本冲掉）→ 提示词推导 → 批量生图 → 导出；切换页签/章节后批量任务与状态保持

- [ ] 视觉状态手动锚定：分镜中栏绑定卡**点资产 Tag 内的状态名**（`林小雨 »` 右侧那个 `战斗服 ▾`）展开状态菜单选一个（本镜 manual）→ 其后各镜 auto 绑定自动延续新状态 → model/manual 绑定不受影响；状态悬空（资产删该状态）时跳过不悬空。菜单只列状态名（剧情锚点/章节范围信息保留在按钮 title 上，悬停可见）

- [ ] 资产引用口径：格级独有的视觉状态（换装页某格才出现的那种）在资产生图工作台上也要显示「N 镜引用」与参考图角标；删掉某视觉状态后，页级与格级绑定**同时**回落到章节默认状态（界面显示的 = 生图实际用的）

- [ ] 章节阶段只升不降：已生成过分镜的章节，回原文页签改一个字触发自动保存，`stage` 不得从 `storyboard-ready` 掉回 `source-ready`

- [ ] 旧项目兼容：无分析/剧本的旧章节原文兜底；旧 storyboardRuns 正常推导/生图；存量模板缺 {{本章资产}} 自动退化（auto-text 通道兜底）

- [ ] 画面描述参考图清单：右栏「参考图设置」展开后应看到一行**图号速览**（共用属性 图1、图2 · 人物 图3 · 场景 图4 · 道具 图5），且生图实际发送的图顺序与之完全一致（共用属性图 → 人物 → 场景 → 道具）；把某资产的图换序或删掉第一张，图号与取图应立刻跟随变化，**不需要任何刷新按钮**；参考图数量超过原来的 14 张上限时不再被截断

- [ ] 共用属性「插入最后」组：绘图配置里切到右侧后置属性的卡片，应看不到「需要参考图」开关与图区（改为一行灰字提示）；此前传过的图不会被删，切回「插入最前」即恢复；改图/删图**不会再冲掉手填的描述正文**

- [ ] 提示词共用属性拼接：**推导提示词里不应出现共用属性正文**（前置/后置都不出现，模型只写画面与资产图号）；共用属性只在生图时由 `composeFinalPrompt` 拼到描述前后（前置：属性名 + `图N、图M：属性名。` + 描述正文；后置：无图号行）；变量 tag 里已无 `{{风格上下文}}` / `{{前置共用属性}}` / `{{后置共用属性}}`；存量模板里的这些占位符会按未注册占位符清理并在设置页红色告警
- [ ] 画面描述模板改为用户自建：设置页新建模板 → 类型选「分镜画面描述」（或「整章一次生成」）→ 点「填入推荐模板」应拿到完整底稿；分镜页弹窗里模板下拉**没有「内置默认模板」选项**，未选模板时无法点「开始推导」，列表为空时点「新建模板（填入推荐内容）」应直接建好并选中

- [ ] 整章一次生成：分镜页「批量推导描述」→ 生成方式选「整章一次」→ 模板下拉应只列 `panel-prompt-chapter` 类模板 → 预览即全章提示词（含各镜资产图号清单：清单只列资产图，图号沿用生图真实序号并注明「图1、图2 为前置共用属性参考图…」）→ 开始推导仅 1 次调用；模型漏段时提示「未对上 N」，未对上的镜保留原描述

- [ ] 复制到外部 AI 再导入：右栏「复制」按钮复制的是**完整提示词**（含前置/后置共用属性，与生图实际发送一致）；批量弹窗里「复制整章提示词」带走的是**推导用文本**（不含共用属性，可整章丢给外部 AI）；外部 AI 产出后点工具栏「导入描述」粘贴，应能按【分镜N】对位预览（无标记时提示「按顺序对位，请核对序号」）并写入各镜描述

