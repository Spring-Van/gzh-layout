# 说明文档

> 项目全生命周期管理载体：项目规划 / 实施方案 / 进度记录。每次打开项目前先阅读本文档确认当前进度。

## 一、项目规划

### 1.1 项目定位

面向公众号排版与长篇漫画创作的桌面应用（Electron + Vue 3 + TypeScript + Pinia + Tailwind CSS）。

### 1.2 长篇漫画 AI 管线（本次改造目标）

将长篇故事创作管线改造为清晰的四环节 AI 管线：

```text
原小说 → 【原文分析 AI】 → 【剧本生成 AI】 → 【分镜生成 AI】 → 【资产提取 AI】 → 绘画 Prompt → 生图
```

| 产物   | 主要输入                  | 解决的问题            |
| ---- | --------------------- | ---------------- |
| 原文分析 | 原小说 + 分析规则            | 小说里有什么           |
| 漫画剧本 | 原小说 + 原文分析 + 剧本规则     | 这一章怎么改编成漫画剧情     |
| 分镜   | 剧本（主）+ 原文分析（辅）+ 分镜规则  | 每一段剧情具体怎么拍、怎么画   |
| 资产   | 原文分析 + 剧本 + 分镜 + 提取规则 | 哪些人物/场景/道具需要固定一致 |

### 1.3 页面结构

- **主页面** `/comic/long-project/:projectId`：tab「原文｜剧本」；原文 tab 左编辑右分析，剧本 tab 左原文只读右剧本编辑；底部 PromptRunBar（模型/模板/发送前确认）。

- **生图工作台** `/comic/panel-gen/:projectId/:chapterId`：页面级 tab「分镜｜资产」；分镜 tab 左列表（右键合并/拆分/复制/撤销）+ 中预览 + 右 `[分镜内容|提示词]` 切换；资产 tab 承载资产提取、审核与资产生图工作台。

### 1.4 关键决策

1. 分镜生成不再依赖资产库，改为剧本主输入 + 原文分析辅助；无剧本时原文兜底 + toast 提示。
2. 资产提取输入 = 原文 + 分析 + 剧本 + 分镜概要 + 已有资产；候选带分镜出现次数（确定性统计），确认后按文本自动回填分镜绑定（auto-text）。
3. 主页面移除分镜表格；合并/拆分等结构操作迁移到生图页左栏右键菜单（8 秒可撤销，成图归属随迁）。
4. 持久化统一走 useLongProjectPersistence（串行队列 + 队列内 read-modify-write），keep-alive 双页缓存不再互相覆盖；两页 onActivated 重载。

## 二、实施方案（已落地文件清单）

### 2.1 新建（14）

| 层  | 文件                                            | 职责                               |
| -- | --------------------------------------------- | -------------------------------- |
| 服务 | `services/chapterDocService.ts`               | 原文分析/剧本提示词组装与执行 + 推荐模板           |
| 服务 | `services/assetExtractionConfirm.ts`          | 提取确认数据变换 + 分镜绑定回填（纯函数）           |
| 组合 | `composables/useLongProjectPersistence.ts`    | 串行队列 + read-modify-write 持久化     |
| 组合 | `composables/useChapterDocRun.ts`             | 分析/剧本生成执行与持久化 + stage 推进         |
| 组合 | `composables/useStoryboardRun.ts`             | 分镜生成（剧本主输入）+ 异常恢复                |
| 组合 | `composables/useStoryboardOps.ts`             | 合并/拆分/复制/撤销 + panelArtworks 归属迁移 |
| 工具 | `utils/markdown.ts`                           | Markdown 渲染纯函数                   |
| 组件 | `components/common/MarkdownView.vue`          | 只读 Markdown 渲染                   |
| 组件 | `components/common/PromptPreviewDialog.vue`   | 发送前确认弹窗（可编辑最终提示词）                |
| 组件 | `components/common/PromptRunBar.vue`          | AI 任务执行底栏（模型/模板/确认/执行）           |
| 组件 | `components/LongProjectSourceTab.vue`         | 原文 tab（左编辑右分析）                   |
| 组件 | `components/LongProjectScriptTab.vue`         | 剧本 tab（左原文右剧本 + 进入分镜生图入口）        |
| 组件 | `components/panel-gen/PanelContentEditor.vue` | 右栏「分镜内容」：生成分镜 + 逐镜编辑             |
| 组件 | `components/panel-gen/PanelGenAssetTab.vue`   | 资产 tab：提取入口 + 审核 + 资产生图工作台       |

### 2.2 修改（7）

- `types/index.ts`：TemplateType 增加 `analysis|script`；新增 `LongProjectChapterDoc`；候选增加 `panelAppearances`

- `services/storyboardService.ts`：分镜提示词改为 `{{script_content}}/{{analysis}}` 变量；template 可选（prompt 直传）

- `services/assetExtractionService.ts`：提取提示词追加分析/剧本/分镜概要；`countCandidatesAppearances` 按分镜计数

- `views/LongProject.vue`：双 tab 改造（\~1350 → 415 行）

- `views/LongProjectPanelGen.vue`：页面级 tab + 右栏双模式 + 右键菜单 + 持久化 composable

- `components/panel-gen/PanelListSidebar.vue`：去 hover 按钮，加 contextmenu

- `features/settings/components/PromptTemplateSettings.vue`：新增原文分析/漫画剧本模板类型

### 2.3 复用不动

LongProjectAssetExtractionReview、StoryboardContextMenu/MergeDialog/SplitDialog、LongProjectAssetTab、PanelPromptPanel、PanelPreview、PanelAssetTabs。

## 三、进度记录

| 日期         | 任务                      | 状态 | 结果说明                                                                                       |
| ---------- | ----------------------- | -- | ------------------------------------------------------------------------------------------ |
| 2026-09-03 | 阶段一：类型层 + 设置页 + 公共基础件   | ✅  | 新类型/模板设置/MarkdownView/PromptPreviewDialog/PromptRunBar/useLongProjectPersistence 完成        |
| 2026-09-03 | 阶段二：服务层新管线              | ✅  | chapterDocService/storyboardService/assetExtractionService/assetExtractionConfirm 完成，旧签名兼容 |
| 2026-09-03 | 阶段三：主页面改造               | ✅  | LongProject.vue 双 tab（原文/剧本），行数 1350→415，legacy `?tab=storyboard` 映射 script                |
| 2026-09-03 | 阶段四：生图页面改造              | ✅  | 页面级 分镜/资产 tab；右栏 分镜内容/提示词 切换；左栏右键合并/拆分/复制/撤销；持久化换 composable                               |
| 2026-09-03 | 阶段五：死代码清理 + 说明文档 + 全量回归 | ✅  | 无死代码残留（noUnusedLocals 通过）；`npx vue-tsc --noEmit` 全量通过；说明文档建立                               |

### 待办 / 后续手测清单

- [ ] 原文录入 → 分析（发送前确认改 prompt）→ 编辑分析 → 原文改动后「原文已变更」徽标

- [ ] 剧本生成 → 编辑 → 进入分镜生图

- [ ] 生图页：生成分镜（有/无剧本）→ 右键合并（成图归属）/拆分/复制/撤销 → 分镜内容逐镜编辑标 stale → 提示词推导 → 批量生图 → 导出

- [ ] 资产 tab：提取（可见出现次数）→ 改候选重算次数 → 确认 → 分镜绑定自动回填 → 资产生图工作台

- [ ] 旧项目兼容：无分析/剧本的旧章节原文兜底；旧 storyboardRuns 正常推导/生图

